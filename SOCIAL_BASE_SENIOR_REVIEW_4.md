# SOCIAL BASE — Senior Engineer Review（第4回）

| | |
|---|---|
| 対象 | `SOCIAL_BASE_SYSTEM_DESIGN.md` **v4**（3,492行 / 29章 / commit `89af344`） |
| 前回 | `SOCIAL_BASE_SENIOR_REVIEW_3.md`（BLOCKER 0 / HIGH 3 / MEDIUM 7 / LATER 1） |
| 採否 | `SOCIAL_BASE_REVIEW_RESOLUTION.md` §9（ACCEPT 11 / PARTIAL 0 / REJECT 0） |
| 実測対象 | `ml-editing-board.html`（`f264170` から無変更であることを確認済み） |
| レビュー日 | 2026-08-29 |
| 判定 | **A. 実装開始可能** |
| 本レビューの指摘 | BLOCKER **0** / HIGH **0** / MEDIUM **5** / LATER **1** |

**Phase 1 の完了条件（unresolved BLOCKER = 0 かつ unresolved HIGH = 0）を満たしている。** 第3回の HIGH 3件はいずれも実物が v4 に存在し、かつ元の故障が起きなくなることを確認した。残る MEDIUM 5件は文言・波及・スクリプトの範囲で、いずれも実装を止めない。

---

## 0. 総評

### 0.1 v4 で確実に良くなった点

第3回の HIGH 3件は、いずれも「v3 で入れた修正そのものが誤っている」型だった。v4 はその3件とも**根拠まで書き直している**。

- **§12.4 の生成手順**（L1687-1699）で `assignEditors()` が手順7、`merge` が手順8 になり、L1701 に「**手順7は必ず merge の前に置く**」、L1703 に順序を逆にしたときの壊れ方が明記された。実装（`:4992` → `:4994` → `:5005`）と一致する。
- **§11.3 の undo Transition**（L1565）は `unique(template_id, from_state_id, to_state_id, kind)` を採用し、根拠を**実際に衝突している組**（`ACTION_TO["修正完了"] = "確認中"` と `PREV_STATUS["要修正"] = "確認中"`）で書き直した。第3回が「成立していない」と指摘した1文は消えている。
- **Q15**（L3200）の推奨案が「サンプルを削除してから抽出」から「サンプルのまま移し、`settings.sample_months` に記録して表示を残す」へ差し替わり、§14.3（L1902）も「廃止」から「残す」へ揃った。§24.6 に0件 Parity の禁止（L2841）、§25.7 に対応するテスト2件（L2988-2989）が入っている。

**Core 純度チェックと mermaid 重複チェックについては、実際に違反を注入して検出力を実測した（§0.4）。** 前者は目的の語を検出するようになった。後者は**追加されたが、第3回で実際に起きた破損の形を検出しない**（→ R4-MEDIUM-1）。

### 0.2 事実検証（`ml-editing-board.html` の実測）

v4 が新たに書いた、または書き直した主張を実装から数え直した。**すべて正しい。**

| v4 の主張 | 実測 | 結果 |
|---|---|---|
| §12.4 L1701：`generate()`（`:4992`）→ `assignEditors()`（`:4994`）→ `merge()`（`:5005`）の順 | `:4992` `:4993` `:4994` が連続、`merge` は CTA の `onclick`（`:5005`）内 | **正しい** |
| §12.4 L1703：`byClient[c].forEach(function(r){ r.editor = best; })`（`:1777`） | `:1777` に一字一句一致 | **正しい** |
| §12.4 L1698：merge は進行中・`date_locked` を動かさない | `:5021` `keptAll = ... (r.status!=="未着手" \|\| r.dateLocked)`、`:5031` で `kept` をそのまま `out` へ | **正しい** |
| §10.3 L1444：`assignEditors()` の同点処理は `names` の並び順に従う | `:1764` `var names=Object.keys(caps)`、`:1774` `if(score>bestScore)`（厳密大なので先勝ち） | **正しい**（v3 の「既定担当による優先」という架空の規則は撤回されている） |
| §11.3 L1565：`ACTION_TO["修正完了"] = "確認中"` と `PREV_STATUS["要修正"] = "確認中"` が両立 | `:1547-1548` / `:1553-1554` に両方実在 | **正しい** |
| §11.3 前進6遷移 / undo 5遷移 | `STATUS_ACTIONS`（`:1543-1546`）から6、`PREV_STATUS`（`:1553-1554`）から5。ボタン文言も一致 | **正しい** |
| §24.4 L2804：「サンプルを削除」は `month !== monthOf(TODAY)` で当月の全行を無条件削除（`:2287`） | `:2285` がボタン、`:2287` が `STATE.rows.filter(r => r.month!==monthOf(TODAY))` | **正しい**（ボタン `:2285` / フィルタ `:2287` の使い分けも正確） |
| §10.5 L1467：`scopeRows()` は `:3064` | `:3064` が `function scopeRows(){`。`:2217 myTasks` / `:3335 filteredRows` と同じ「関数宣言行」の付け方で統一された | **正しい**（第3回が使った `:3066` はフィルタ行。v4 の訂正のほうが揃っている） |
| §24.4 L2754：poster はなつみ9件 / りりか4件 | 全13契約のうちなつみ9・りりか4 | **正しい** |

**Q15 の推奨案の妥当性そのものも実装で裏が取れた。** 現行のサンプル警告文（`:2265`）は「表示中の 2026-08 分の**進捗ステータスと投稿完了日**はサンプルです。クライアントごとの本数とシフトは実データです」である。つまりサンプルなのは進捗値だけで行そのものではない。**「行を消す」ではなく「サンプルであることを表示し続ける」を選んだ v4 の判断は、現行データの実態と整合している。**

### 0.3 文字化け — 問題なし

意図しない文字コードの混入は**発見できなかった**。UTF-8 妥当性 / U+FFFD / latin1 二重エンコード痕 / 制御文字 / NBSP / ZWSP / 文中 BOM / 半角カナ / CRLF / 先頭 BOM のすべてで0件。`SOCIAL_BASE_REVIEW_RESOLUTION.md` と `check_doc_integrity.sh` も同様。

### 0.4 `check_doc_integrity.sh` の検出力の実証

スクリプトは実行して **PASS**（19項目）。今回は PASS の中身を疑い、**実際に違反を注入して FAIL するかを1件ずつ試した**（試行後はすべて復元し、`git status` がクリーンであること・バックアップと `diff -q` が一致することを確認済み）。

| # | 注入した違反 | 期待 | 実測 | 判定 |
|---|---|---|---|---|
| A | §7.2 ER の `text kind "module-registered"` を `text kind "publish_date or assignment"` に戻す（第2回で実在した混入） | FAIL | `FAIL Core の章に Module 固有語が混入`（`§7.2 Core: 129:` 付きで行を表示） | **検出できる** |
| B | §6.2 `task_assignments` の列に `editor_name` を追加 | FAIL | `OK Core の章に Module 固有語なし` | **検出できない** |
| C | §7.1 ER の `workflow_runs \|\|--\|\| tasks : "進行を持つ"` を**同一行のまま**複製 | FAIL | `FAIL mermaid に重複エッジ`（DUP 行を表示） | **検出できる** |
| D | §7.3 ER の `service_contracts }o--\|\| workflow_templates : "既定"` を同一行のまま複製 | FAIL | `OK mermaid の関連エッジに重複なし` | **検出できない** |
| E | **第3回で実際に起きた破損をそのまま再現** — `workflow_runs \|\|--\|\| tasks : "進行を持つ"` と `workflow_runs \|\|--\|\| tasks : "進行"` を並べる | FAIL | `OK mermaid の関連エッジに重複なし` → **PASS** | **検出できない** |

A は第2回 R2-MEDIUM-9 / 第3回 R3-MEDIUM-5 の目的が達成されたことの実証である。**一方 E は、v4 で追加したチェックが「それを作る理由になった破損」を検出しないことの実証である**（→ R4-MEDIUM-1）。

### 0.5 問題を発見できなかった領域（問題なし）

今回精査したが新規の問題を発見できなかった領域を明記する。**これらについては指摘なし。**

- §6.1 UNIQUE 規約（L563 / L565 / L588 が一貫。第3回 R3-MEDIUM-3 の自己矛盾は消えている）
- §6.5 RLS 運用規約、§8 Identity、§9 Permission Matrix、§20 Security
- §11.5 Workflow 変更時の7規則、§11.2 semantic、§9.4 Transition 単位の権限（`kind` 追加後も矛盾なし）
- §13 Automation、§15-17 Integration framework、§18 Recommendation、§19 Audit / Observability
- §21 API、§23 Background Jobs、§26 Deployment、§29 ADR ログ、§27 Phase 定義
- §3.3 Layer A の分割（`assignEditors()` を含む列挙は手順変更後も整合）、§6.6 expand/contract
- 章立て・目次・節番号・相互参照・コードフェンス・mermaid 宣言・末尾終端（スクリプトで機械確認、すべて OK）
- **`contract_version_hash` → `generation_input_hash` の改名は取りこぼしゼロ**（旧名の残存は変更履歴表の v3 行のみで、これは履歴として正しい）

---

## 1. 第3回指摘の解消状況

| ID | 判定 | 根拠 |
|---|---|---|
| **R3-HIGH-1** 手順8（`assignEditors()`）の実行順 | **解消** | 手順7 = `assignEditors()`（L1695-1696）、手順8 = `merge`（L1697-1698）へ入れ替え。L1701 に「手順7は必ず merge の前に置く」＋実装の実行順、L1703 に壊れ方（無条件上書き・無音）を明記。§10.3 L1444 が「§12.4 手順7」を参照、§25.1 L2912 に「進行中の案件の担当が生成後も変わっていない」テストを追加。**実装（`:4992`→`:4994`→`:5005`）および `merge()`（`:5021`/`:5031`）と一致することを実測で確認。**残るのは §24.6 L2859 の Parity 行が「同じ入力で」のままで比較対象が明示されていない点のみ（→ R4-LATER-1） |
| **R3-HIGH-2** Q15 の推奨案が当月全行の削除経路 | **解消** | Q15（L3200）を「サンプルのまま移し、新環境でも表示し続ける」へ差し替え。§24.4 L2804 に `settings.sample_months` と「『サンプルを削除』（`:2287`）は使わない」＋0件 Parity への注意を明記。§14.3 L1902 を「残す」へ変更し §24.4 と一致。§24.2 Step 2-1 を5項目に具体化（L2671-2676、`seeded` を含む）。§24.6 L2841 に0件 Parity 禁止行、§25.7 L2988-2989 にテスト2件。**「文書内で存在しない Step 2-1 を参照している」空振りは解消された。**残るのは Step 5-1（L2689）に「押さない」が無い点（→ R4-MEDIUM-4） |
| **R3-HIGH-3** undo Transition の UNIQUE 違反 | **解消** | (B) を選択。§6.2 L604 を `unique(template_id, from_state_id, to_state_id, kind)` に変更、§11.3 L1565 で**実際に衝突している組**（`ACTION_TO["修正完了"]` と `PREV_STATUS["要修正"]`）を根拠に書き直し。§7.1 ER に `kind "forward or undo"`（L825）と `archived_at`（L826）を追加。§25.4 に「同じ `(from, to)` の forward と undo が共存できること」（L2956）と「`is_terminal` からの undo」（L2957）を追加。**成立しない根拠の1文は削除されている。**前進6 / undo 5 の内容が実装（`:1543-1554`）と完全一致することも実測で確認 |
| **R3-MEDIUM-1** ER 重複エッジ / `kind` 欠落 / mermaid 重複検出 | **部分的** | 重複エッジ削除 ✓（L759 のみ）、`workflow_transitions.kind` と `archived_at` ✓（L825-826）、`workflow_states.archived_at` ✓（L816）。スクリプトに重複チェックも追加された（L56-57）。**ただし追加されたチェックは、第3回で実際に起きた破損の形（ラベルだけ違う同一エッジ）を検出しない。実測で PASS を返すことを確認**（§0.4 テスト E）。`}o--` / `}\|--` も対象外（テスト D）。第3回の推奨は `}o--` / `}\|--` を含めることだった（→ **R4-MEDIUM-1**） |
| **R3-MEDIUM-2** スキーマ変更3件の他章への波及 | **部分的** | `service_contracts.version` を §5.2 L455 に追加 ✓、§22.1 に「契約内容の変更 → `service_contracts.version`」行（L2476）と**インクリメント主体**（L2478）を明記 ✓、§25.2 L2931 を部分ユニーク2本の記述へ ✓。**未反映が5箇所残る** — §5.1 L410 に `tasks ... / version /` が残存（§6.2 L607 の「`version` を持たない」と §22.1 L2480 に真っ向から矛盾）／§7.3 ER の `service_contracts` ブロック（L1023-1036）に `version` が無い／§22.1 冒頭 L2459 が「`content_items` / `workflow_runs` / `daily_notes` に `version`」の3テーブルのまま（同じ節の L2476 が4つ目を追加している）／`period_key` の部分ユニーク2本が §5.1 L421・§11.6 L1609・§22.5 L2521 で旧「1本」のまま（→ **R4-MEDIUM-2**） |
| **R3-MEDIUM-3** §6.1 の UNIQUE 規約の自己矛盾 | **解消** | L563 を「`id` 列を持つ全テーブルに」へ書き換え ✓、L565 に `users` / `webhook_receipts` が対象外である理由を追加 ✓、L588 の結合テーブル段落と整合 ✓。**同一節内の矛盾は消えた** |
| **R3-MEDIUM-4** 既定担当の役割 / 架空のタイブレーク | **部分的（主目的は達成）** | §10.3 を役割ごとに分割 ✓（L1442 publisher / L1444 editor）。**v3 が加えた「Capacity が同点のときの優先」という現行に無い規則は撤回された** ✓（L1444「現行に無いタイブレーク規則を追加しない」）。実装（`:1764` `Object.keys(caps)` / `:1774` 厳密大）とも一致。**未反映** — §24.4 のマスターデータ表に `default_editor_member_id` の行が無い（移行元・初期値が未定義）。加えて L1444 が「既定値が使われるのは『動画追加』で1本ずつ作る場合だけ」と書くが、**現行の `openAddVideo()`（`:4874-4877`）は既定担当を読まず「未割当」が初期値**であり、これは現行に無い挙動である（→ **R4-MEDIUM-5**） |
| **R3-MEDIUM-5** Core 純度チェックの語彙 | **部分的** | MODWORDS に `publish_date` / `poster` / `material` / `tiktok` / `youtube` / `notion` を追加 ✓、対象外語の理由をコメントで残した ✓（L81-82）。**検出力を実測で確認した** — §7.2 ER に `publish_date` を戻すと正しく FAIL する（§0.4 テスト A）。**未反映** — 第3回が求めた `editor` / `meeting` が無く、実測で `editor_name` を Core 表に入れても PASS する（テスト B）。§6.2 L621 の `recommendations` 行に `kind` の注記も無い（`schedule_entries` L609 にはある。→ **R4-LATER-1**） |
| **R3-MEDIUM-6** 第2回が求めたテスト6件 | **部分的** | §25.1 に3件（L2911 補正の非対称性 / L2912 `assignEditors()` の実行順 / L2913 `generation_input_hash`）、§25.2 に1件（L2931 `period_key` NULL）、§25.4 に2件（L2956 / L2957）、§25.7 に2件（L2988 / L2989）を追加 ✓。**未反映3件** — `ends_on` を過ぎた契約が生成対象に入らないこと（§12.4 L1741 の絞り込みに対応するテスト）／`tasks.due_at` を古い `content_items.version` で更新すると 409 になること（L2929 は汎用の記述のみ）／`editor` Role 保持者の「今日」画面と動画一覧の既定表示が絞られること（§10.5 の3箇所差） |
| **R3-MEDIUM-7** 冪等キーの入力範囲 | **解消（範囲に不足あり）** | (A) を採用。`contract_version_hash` → `generation_input_hash` へ改名（L1724）、契約 + `business_calendar` + `working_schedules` の3入力を含む式を明記（L1733-1737）、§22.3 L2503 も改名済み、§25.1 L2913 にテスト追加。**「シフトだけ変えた再生成が無音で握り潰される」故障は塞がった。**ただしハッシュの対象が「対象月」に閉じており、リードタイム逆算が実際に読む前月分の営業日を含まない（→ **R4-MEDIUM-3**） |
| **R3-LATER-1** 参照の不揃い3件 | **部分的** | `settings.seeded_source` → `settings.sample_months` に統一 ✓（L1902 / L2673 / L2804 / L2988）、`scopeRows()` の行番号を `:3064` へ訂正 ✓（実測一致）。**未反映** — §5.5 L506 の `TodayTasksQuery` 行に `scopeRows()` が無い（§2.5 L171 と §10.5 L1470 は列挙している）／§6.2 L621 の注記／`settings` のキー一覧（`maintenance` / `sample_months` / `capacity.hours_per_item` / `LOAD_CFG` 相当）が1箇所にまとまっていない（→ **R4-LATER-1**） |

### 集計

| 区分 | 件数 | 解消 | 部分的 | 未解消 | 悪化 |
|---|---|---|---|---|---|
| HIGH | 3 | **3** | 0 | 0 | 0 |
| MEDIUM | 7 | 2 | 5 | 0 | 0 |
| LATER | 1 | 0 | 1 | 0 | 0 |

**「未解消」「悪化」はゼロ。HIGH 3件はすべて解消した。** 「部分的」6件はいずれも、主張された変更の中核が実在し目的も達しているが、**波及先または周辺の1〜3項目が残っている**という形である。第1〜3回のような「入れた修正そのものが誤っている」型は今回ゼロだった。

### 「主張と実体の乖離」の再発について

Resolution §9 が「反映した」と書いて**実物が無いもの**は、11件のうち1件も無かった。第3回までと同じく「主張は真だが目的を達していない」型を意図的に探し、**1件だけ見つけた**。

- **mermaid 重複エッジチェック**（R3-MEDIUM-1）— Resolution §9.2 は「**同種の破損を機械検出するため**、mermaid の関連エッジ重複チェックを追加した」と書いている。チェックは実在し、完全同一行の重複は検出する。しかし v3 で実際に起きた破損は `... : "進行を持つ"` と `... : "進行"` という**ラベルだけが違う同一エッジ**であり、行全体を `sort | uniq -d` にかける実装では検出できない。**実測で PASS を返すことを確認した**（§0.4 テスト E）。

一方、**Core 純度チェックについては Resolution が「実際に `publish_date` を Core の ER へ戻して FAIL することを確認済み」と書いており、これは再実測でも真だった。** 検出力を主張し、それが検証で裏付いたのは4回のレビューを通じて初めてである。

---

## 2. 新規および残存の指摘

### 2.1 BLOCKER

**なし。**

Modular Monolith / Core と Module の分離 / PostgreSQL を SoT / Outbox + Job / 段階移行という骨格は v4 でも維持されている。Workspace 分離のスキーマ層防御（複合 UNIQUE + 複合 FK）、RLS の運用規約、OIDC `sub` による同定、業務日付のタイムゾーン規約、スナップショット同期はいずれも第3回の確認から後退していない。v4 の変更は制約1本（`unique(..., kind)`）・手順の並べ替え・移行方針1件・文言に収まり、Architecture を揺らすものは無い。

### 2.2 HIGH

**なし。**

第3回の HIGH 3件はすべて解消した（§1）。今回新たに発見した問題のうち、**「実装開始前に直さないと手戻り・データ整合性・セキュリティの問題になる」ものは無い。** 以下の MEDIUM 5件はいずれも、(1) 正典となる章が正しく書けており誤りは別章の重複記述側にある、(2) 実装時にエラーとして表面化する、(3) Phase 10（移行）まで着手しない、のいずれかに該当する。

---

### 2.3 MEDIUM

#### R4-MEDIUM-1 — mermaid 重複エッジチェックが、それを作る理由になった破損の形を検出しない

**1. 該当設計** `check_doc_integrity.sh` L56-57。関連：Resolution §9.2 R3-MEDIUM-1。

**2. 問題**
v4 で追加されたのは次の1行である。

```sh
d=$(awk '/^```mermaid/{m=1;next} /^```$/{m=0} m && /^    [a-z_]+ \|\|--/ {print}' "$F" | sort | uniq -d)
```

比較しているのは**ラベルまで含めた行全体**であり、対象は `||--` で始まる4スペース字下げの行だけである。したがって次の2つを検出できない。

| 検出できない形 | 実測 |
|---|---|
| **同じ2実体・同じカーディナリティで、ラベルだけが違うエッジ** — v3 で実際に起きた `workflow_runs \|\|--\|\| tasks : "進行を持つ"` と `... : "進行"` | `OK mermaid の関連エッジに重複なし` → 全体 **PASS**（§0.4 テスト E） |
| `}o--` / `}\|--` で始まる関連行の重複（§7.3 ER の `service_contracts }o--\|\| workflow_templates` 等） | 同上（テスト D） |

第3回 R3-MEDIUM-1 の推奨は「mermaid の関連行（`||--` / `}o--` / `}|--` を含む行）を対象へ加える」だった。前者だけが実装され、しかも**正規化をしていないため、実際に起きた破損の形が素通りする。**

**3. 発生する具体的リスク**
実害は「ER 図に平行な線が2本描かれ、実体間の関係が2つあるように読める」ことであり、スキーマそのものは壊れない。問題は**期待値のほうである**。Resolution が「同種の破損を機械検出する」と宣言したことで、次回以降のレビューと編集作業が「mermaid の重複は機械で止まる」という前提で進む。**v3 と同じ破損がもう一度起きても PASS が返る。** 第3回が「PASS が偽の安心を与えるぶん、チェックが無いより悪い」と書いた状況が、対象を変えて再現している。

**4. 推奨変更**
比較前に**ラベルとカーディナリティを落として正規化する**。1行で足りる。

```sh
d=$(awk '/^```mermaid/{m=1;next} /^```$/{m=0}
         m && /^ +[a-z_]+ +[|}][|o{]?-[-o|{]* +[a-z_]+/ {
           sub(/ *:.*$/,""); gsub(/[|}o{]+--[|}o{]+/,"--"); print }' "$F" | sort | uniq -d)
```

これで `workflow_runs -- tasks` が2回現れれば、ラベルもカーディナリティも問わず FAIL する。**修正後は §0.4 のテスト C / D / E を再実行して、3つとも FAIL することを確認する**（テストの入れ方は本書 §0.4 に記載のとおり）。

あわせて MODWORDS に `editor` / `meeting` を足す（第3回 R3-MEDIUM-5 の残り。テスト B が FAIL に変わることで確認できる）。

**5. 過剰設計にならない最小修正** **awk 1行の書き換えと正規表現に2語。** 実装量ゼロ。**修正したら必ず注入テストで FAIL することを確かめる** — 今回の件は「チェックを足したが検証しなかった」ことが原因である。

---

#### R4-MEDIUM-2 — v3/v4 のスキーマ変更が §5 / §7 ER / §22 になお波及していない（5箇所）

**1. 該当設計** §5.1 L410 / L421、§7.3 ER L1023-1036、§22.1 L2459、§11.6 L1609、§22.5 L2521。

**2. 問題**
正典となるスキーマ表（§6.2 / §6.4）は正しい。誤っているのは**同じ事項を語る他章**である。第3回 R3-MEDIUM-2 で挙げた6項目のうち3つが残り、加えて §22.1 の冒頭文が新たに自節内で不整合になった。

| 事項 | 正典（正しい） | 残っている記述 |
|---|---|---|
| `tasks` は `version` を持たない | §6.2 L607「**`version` を持たない**」、§22.1 L2480、§7.1 ER（L838-848 に無し） | **§5.1 L410** — `... / created_by / version / created_at / updated_at` |
| `service_contracts.version` | §6.4 L657、§5.2 L455、§22.1 L2476 / L2478、§12.4 L1726 | **§7.3 ER L1023-1036** に `version` が無い／**§22.1 L2459** が「`content_items` / `workflow_runs` / `daily_notes` に `version integer` を持つ」の3テーブルのまま（同じ節の L2476 が4つ目を追加している） |
| `workflow_runs` の部分ユニーク**2本** | §6.2 L605、§25.2 L2931 | **§5.1 L421** / **§11.6 L1609** / **§22.5 L2521** が旧「1本」の記述 |

`tasks.version` の残り方がとくに悪い。§5.1 が「持つ」、§6.2 と §22.1 が「持たない」と真逆を書いており、**しかも §5.1 は Domain Model の章で、実装者が最初に読む場所である。**

**3. 発生する具体的リスク**
- §5.1 L410 から `tasks.version` を作った場合、第2回 R2-MEDIUM-2 が潰した「どの version を送るか不定」がそのまま復活する。§22.1 の対応表は `tasks` を1行も持たないので、送るべき version が決まらない。
- §7.3 ER に `version` が無いため、ER からスキーマを起こすと `service_contracts.version` が Phase 2 の初回 migration から落ちる。**後から足すと既存契約の `generation_input_hash` が全件変わる**（§12.4 L1726 が `(id, version)` を材料にしている）。
- `period_key` の部分ユニークについては、§22.5 L2521 の upsert を「1本の UNIQUE」前提で書くと `ON CONFLICT (workspace_id, template_id, subject_type, subject_id, period_key)` になる。部分ユニークインデックスに対して述語なしの推論は PostgreSQL が受理せず、`there is no unique or exclusion constraint matching the ON CONFLICT specification` で**初回実行時にエラーになる**。無音の故障ではないので HIGH には該当しないが、Phase 6 の実装を止める。

**4. 推奨変更**
- §5.1 L410 から `version` を削除する。
- §7.3 ER の `service_contracts` ブロックに `int version` を追加する（`text status` も §6.4 にあって ER に無いので、あわせて足すとよい）。
- §22.1 L2459 を「`content_items` / `workflow_runs` / `daily_notes` / `service_contracts` に `version integer` を持つ」に改める。
- §5.1 L421 / §11.6 L1609 を §6.2 L605 と同じ「部分ユニーク2本」の記述に揃える。§22.5 L2521 に1文足す — 「`period_key` が NULL の Run を upsert する場合は、`ON CONFLICT ... WHERE period_key is null` 側の部分ユニークインデックスを対象にする」。

**5. 過剰設計にならない最小修正** **列の削除1・追加1、文言修正4箇所、1文追加。** 実装量ゼロ。ただし `service_contracts.version` は Phase 2 の初回 migration に入るため、**スキーマ確定前に済ませる。**

---

#### R4-MEDIUM-3 — `generation_input_hash` の対象範囲が「対象月」に閉じており、リードタイム逆算が読む営業日と `assignEditors()` が読む Capacity を取りこぼす

**1. 該当設計** §12.4 L1728 / L1733-1737。

**2. 問題**
v4 が定義したハッシュはこうである。

```
generation_input_hash =
    hash( 対象契約の (id, version) 昇順連結
        + 対象月の business_calendar の (date, kind) 昇順連結
        + 対象月の working_schedules の (member_id, date, starts_at, ends_at) 昇順連結 )
```

`generate()` の出力は、**対象月の営業日だけでは決まらない。**

- **手順6（リードタイム逆算、§12.3）** は `internal_due = subBiz(営業日リスト, publish_planned, lead_time_business_days)` である。`lead_time_business_days` は最大7営業日なので、対象月**初旬**の投稿予定日に対する社内締切は**前月**に落ちる。前月の祝日・臨時休業日を直せば `internal_due` が変わるが、ハッシュは変わらない。
- 同じく `floor = addBiz(営業日リスト, 生成日, 1)` の `生成日` は通常**前月**である。前月の営業日が変われば `schedule_squeezed` の判定が変わる。
- **手順7** の Capacity は `capacityOf(SEED_SHIFTS, ..., target)`（`:4993`）で、**編集担当**の勤務予定を読む。式（L1736）は全メンバーの `working_schedules` を含むので結果的に正しいが、その理由を説明した L1728 は「**投稿担当の**勤務予定（手順1・手順5）」としか書いていない。**式を「投稿担当ぶんだけでよい」と読んで最適化すると、`assignEditors()` の入力変化がハッシュから落ちる。**

**3. 発生する具体的リスク**
R3-MEDIUM-7 が塞いだのと**同じ故障が、狭い入口から戻る**。「9月分を生成したあとに8月末の臨時休業日を `business_calendar` に登録し、9月分を作り直す」という運用で、契約もシフトも変えていなければハッシュが同一になり、§22.3 手順2（同じ key + 同じ request_hash → **保存済みのレスポンスをそのまま返す**）に入って**再生成が実行されないまま成功が返る**。社内締切だけがずれた状態で放置され、画面上は何も起こらない。

`business_calendar` は §13.5 の Rule で日常的に保守される（祝日テーブル期限の通知、会社休業日の追加）ため、この経路は理論上のものではない。

**4. 推奨変更**
L1733-1737 の式を2箇所直すだけでよい。

- `対象月の business_calendar` → **`前月初日から対象月末日までの business_calendar`**（リードタイム逆算と `floor` が読む範囲を含む）。
- L1728 の「投稿担当の勤務予定（手順1・手順5）」を「**勤務予定（手順5の投稿担当の寄せと、手順7の Capacity 比例配分の両方が読む）**」に直す。式そのもの（全メンバーぶん）は既に正しいので変えない。

§25.1 L2913 のテストに「**前月の営業日だけを変えても再生成が通ること**」を1行足すと、範囲の取り違えが回帰で止まる。

**5. 過剰設計にならない最小修正** **式の1語と説明1文、テスト1行。** 実装量ゼロ。範囲を広げてもハッシュの計算コストは変わらない。

---

#### R4-MEDIUM-4 — §24.2 の Step 5 チェックリストに「サンプルを削除を押さない」が無い

**1. 該当設計** §24.2 Step 5-1（L2689）/ Step 5-3（L2691）。関連：§24.4 L2804、§24.6 L2841。

**2. 問題**
v4 は Q15 の推奨案を安全側へ差し替え、§24.4 L2804 に「画面の『サンプルを削除』（`:2287`）は使わない」と明記した。しかし**カットオーバー当日の手順書である §24.2 Step 5 には、この禁止事項が入っていない。**

```
Step 5  カットオーバー
        5-1 全員の画面で未同期バナー（`:2255`）が出ていないことを確認する
        ...
        5-3 最終 STATE を再抽出し、スナップショット同期を流す
```

第3回 R3-HIGH-2 が求めた3点のうち、Step 2-1 への `seeded` 追加は反映されたが、**Step 5-1 の禁止事項と Step 5-3 の `seeded` 分岐は入っていない。**

§24.6 の0件ガード（L2841）は最終防衛線として有効だが、**0件のときにしか効かない。** カットオーバー時点で翌月分を先に生成していれば、当月だけ消えても抽出 `rows` は0件にならない。§24.6 の件数比較は DB と**再抽出後の STATE** を突き合わせるため、両方が同じだけ減っていれば一致する（Step 3-4 の期間中は `merge()` で行が正当に消えるため、Step 2-2 の期待値と厳密比較することはできない）。

**3. 発生する具体的リスク**
金曜夜に Step 5 を実行する担当者が読むのは §24.2 の手順ブロックである。警告は §24.4 の Migration Matrix の1セルにしかない。**当月の全行が消えた状態で 5-3 のスナップショット同期が走ると、当月の `content_items` が archive され、翌月分だけが残る。** Parity は0件ガードに掛からず通過する。artifact に undo は無い。

発生確率は v3 より大幅に下がった（推奨案が「押す」から「押さない」に変わったため）が、**手順書と Matrix で情報が分かれている限り、実行者が読まない側に警告が置かれている。**

**4. 推奨変更**
- Step 5-1 に1行足す — 「**カットオーバー前後に『サンプルを削除』（`:2285`）を押さない。押すと `monthOf(TODAY)` の全行が消え、artifact に undo は無い**（§24.4）」。
- Step 5-3 に1文足す — 「再抽出した `rows` の件数が Step 2-1 の実測値から**大きく減っている場合は同期を流さず原因を確認する**（`merge()` による正当な入れ替えでは総数は契約本数を下回らない）」。

**5. 過剰設計にならない最小修正** **手順に2行。** 実装量ゼロ。**Phase 10 の移行着手前で間に合う**（Q15 の推奨案そのものは既に安全なので、オーナーへの提示は現状のままで問題ない）。

---

#### R4-MEDIUM-5 — `default_editor_member_id` の移行元が §24.4 に無く、§10.3 が現行に無い挙動を1つ持ち込んでいる

**1. 該当設計** §10.3 L1444、§24.4 マスターデータ表（L2746-2769）。

**2. 問題**
v4 は「Capacity が同点のときの優先」という**現行に無い規則を正しく撤回した**（第3回 R3-MEDIUM-4 の主目的は達成）。しかし同じ文が、別の現行に無い挙動を1つ導入している。

> **`default_editor_member_id` は月次生成では使わない。** … **既定値が使われるのは「動画追加」で1本ずつ作る場合だけ。**（L1444）

現行の `openAddVideo()` を実測すると、担当編集者のセレクトは `[""].concat(Object.keys(STAFF_MAP))` で構成され（`:4875-4877`）、**初期値は `""`（未割当）である**。契約の既定担当は読まれない。そもそも `SEED_CONTRACTS` に editor 相当のフィールドが無いため、移行時点の `default_editor_member_id` は全件 NULL になる。

加えて **§24.4 のマスターデータ表に `default_editor_member_id` の行が無い**（第3回 R3-MEDIUM-4 が明示的に求めた項目）。`SEED_CONTRACTS[].poster` → `default_publisher_member_id` の行（L2754）はあるが、editor 側は移行元も初期値も検証条件も書かれていない。

**3. 発生する具体的リスク**
- 移行直後は `default_editor_member_id` が全件 NULL なので、L1444 の規則は「NULL を使う」＝未割当となり現行と一致する。**したがって Parity は落ちない。** 問題が出るのは Phase 5 で契約マスタが画面から編集可能になった後で、誰かが既定担当を設定した瞬間に「動画追加」ダイアログの初期値が未割当から変わる。§25.6 は「Backend 化を理由に現在の UI を変えない。意図しない差分は Regression として扱う」としているが、この差分は意図されたものなのか判断できる記述がない。
- §24.4 に行が無いため、移行スクリプトの担当者は `default_editor_member_id` を「何で埋めるか」を自分で決める。NULL でよいことがどこにも書かれていない。

**4. 推奨変更**
- §10.3 L1444 に1文足す — 「**移行時点では `default_editor_member_id` は全件 NULL であり、『動画追加』の初期値は現行どおり『未割当』になる**（`:4875`）。既定担当が入力されるのは Phase 5 以降に契約マスタで設定した場合だけである」。
- §24.4 のマスターデータ表に1行足す — `default_editor_member_id` / 移行元「なし（`SEED_CONTRACTS` に相当フィールドが無い）」/ Method **E** / 検証「全件 NULL であること」。

**5. 過剰設計にならない最小修正** **1文と表1行。** 実装量ゼロ。Phase 5 着手前で間に合う。

---

### 2.4 LATER

#### R4-LATER-1 — 参照・注記・文言の不揃い7件（実害なし）

| # | 箇所 | 内容 |
|---|---|---|
| 1 | §5.5 L506 | `TodayTasksQuery` の「置き換える現行関数」に `scopeRows()` が無い。§2.5 L171 と §10.5 L1470 は列挙している（第3回 R3-LATER-1 の残り） |
| 2 | §6.2 L621 | `recommendations` 行に「`kind` は Module が登録する不透明な文字列。Core は具体値を知らない（値は §18.3）」の注記が無い。`schedule_entries` L609 にはある |
| 3 | §6.2 L596 ほか | `workspaces.settings` のキー（`maintenance` / `sample_months` / `capacity.hours_per_item` / `LOAD_CFG` 相当）が4箇所に散っており、一覧が無い |
| 4 | §24.6 L2859 | `assignEditors()` の Parity 行が「同じ入力で同じ担当割当になる」のままで、**比較対象が merge 前の生成案であること**が書かれていない（§12.4 L1701 を読めば決まるが、Parity 章だけを読む人には決まらない） |
| 5 | §12.4 L1697 | 手順8 の「**生成案のうち**『未着手かつ未ロックの ContentItem』だけを既存と差し替える」は、実際には**既存行**が `未着手 かつ 未ロック` で絞られる（`:5021`）。生成案は全行が候補である。直後の「進行中・`date_locked` は動かさない」で意図は伝わるが、係り先が逆になっている |
| 6 | `check_doc_integrity.sh` L83 | MODWORDS に `editor` / `meeting` が無い（実測で非検出を確認。→ R4-MEDIUM-1 で同時に直せる） |
| 7 | §24.4 L2763-2764 | `workflow_transitions` に投入する行数が「6遷移」（前進のみ）としか書かれていない。§11.3 の undo 5行を合わせて **11行**であることが移行表からは読めない |

**推奨** — いずれも1行以内の修正。**Phase 1 の完了条件には影響しない。** #4 と #5 は §12.4 / §24.6 を触るときに、#1〜#3 と #7 は次に該当章を編集するときにまとめて直せばよい。

---

## 3. 判定

# **A. 実装開始可能**

**Phase 1 の完了条件（unresolved BLOCKER = 0 かつ unresolved HIGH = 0）を満たしている。**

第3回で挙げた HIGH 3件は、いずれも**実物が v4 に存在し、かつ元の故障が起きなくなること**を確認した。3件とも「実物はあるが目的を達していない」型ではない。

- **R3-HIGH-1** — `assignEditors()` が手順7へ移り、「必ず merge の前」という規則と、順序を逆にしたときの壊れ方が本文に書かれた。実装の実行順（`:4992`→`:4994`→`:5005`）と `merge()` の保持条件（`:5021`）を実測して一致を確認した。§25.1 に「進行中の案件の担当が生成後も変わっていない」テストが入り、規則が回帰で固定された。
- **R3-HIGH-2** — Q15 の推奨案が破壊経路から外れた。§14.3 と §24.4 の矛盾は解消し、§24.4 が参照する Step 2-1 は実在するようになった。**現行のサンプル警告文（`:2265`）が「サンプルなのは進捗ステータスと投稿完了日だけで、本数とシフトは実データ」と書いていることを実測で確認しており、v4 の「消さずに表示を残す」という判断はデータの実態と整合している。**
- **R3-HIGH-3** — `unique(template_id, from_state_id, to_state_id, kind)` を採用し、成立しない根拠の1文は削除され、実際に衝突している組で書き直された。§6.2 / §7.1 ER / §25.4 の3層に波及している。

**Architecture の再設計は不要であり、BLOCKER は4回連続で0件である。** 骨格（Modular Monolith、Core と Module の分離、PostgreSQL を SoT、Outbox + Job、段階移行）と、Workspace 分離・Identity・RLS・`predicates`・`external_event_key`・業務日付のタイムゾーンといった中核の判断は、本文・スキーマ表・ER・テスト章・ADR の各層で一貫している。事実誤認も残っていない（§0.2 で9項目を実測、すべて一致）。

**残る MEDIUM 5件は、いずれも実装開始をブロックしない。**

- **R4-MEDIUM-2** は「正典が正しく、重複記述側が古い」形である。§6.2 / §6.4 を読んで migration を書けば正しいものができ、`period_key` の `ON CONFLICT` は初回実行時にエラーとして表面化する。**ただし `service_contracts.version` の ER 欠落だけは Phase 2 の初回 migration に効くため、スキーマ確定前に直す。**
- **R4-MEDIUM-3** は式の範囲を1語広げる話で、実装量ゼロ。Phase 5 着手前で間に合う。
- **R4-MEDIUM-4** は移行手順書の2行で、Phase 10 まで着手しない。Q15 の推奨案自体は既に安全なので、**オーナーへの提示は現状のままで差し支えない。**
- **R4-MEDIUM-5** は移行時点では挙動差が出ない（全件 NULL）。Phase 5 着手前で間に合う。
- **R4-MEDIUM-1** はスクリプトの awk 1行である。設計書の内容に影響しない。

### 着手順序の目安

| 時期 | 項目 |
|---|---|
| **Phase 2 のスキーマ確定前** | R4-MEDIUM-2 のうち §7.3 ER への `version` 追加と §5.1 L410 の `tasks.version` 削除。列が1つ落ちる／余分に増えるのはこの2つだけ |
| **Phase 2 着手前（実装量ゼロ・最も安い）** | R4-MEDIUM-1（awk 1行 + 2語）。**直したら §0.4 のテスト C / D / E を再実行して3つとも FAIL することを確かめる。** R4-MEDIUM-2 の残り4箇所の文言 |
| **Phase 5 着手前** | R4-MEDIUM-3（ハッシュ範囲）／ R4-MEDIUM-5（既定担当の初期値と §24.4 の行） |
| **Phase 6 着手前** | R4-MEDIUM-2 の §22.5 `ON CONFLICT` の1文 |
| **Phase 10（移行）着手前** | R4-MEDIUM-4（Step 5 の2行） |
| **随時** | R4-LATER-1 の7件 |

### 4回のレビューを通じた所見

第1回 BLOCKER 3 / HIGH 13 から始まり、v4 で BLOCKER 0 / HIGH 0 に到達した。**指摘の性質も変わっている。**

- 第1回：骨格の欠落（権限テーブルの Workspace 境界、RLS の運用、Identity の同定キー）
- 第2回：反映が途中で止まっている型
- 第3回：**入れた修正そのものが誤っている型**（実行順が逆、根拠が別のペア、推奨案が破壊経路）
- 第4回：**波及先の取りこぼしと、検証していないチェック** — 誤りではなく不足

第3回が指摘した「反映の主張は、実物の存在だけでなく『それで元の問題が起きなくなるか』まで確認する」という方針は、Core 純度チェックについては**実際に守られた**（Resolution §9.2 が検出力の実証を主張し、再実測でも真だった）。一方 mermaid 重複チェックは**同じ節で追加されながら検証されず、実際に起きた破損の形を検出しない**。**チェックを足したときは、必ず違反を注入して FAIL することを確かめる** — これが4回のレビューで唯一まだ定着していない習慣である。

以上により、**Phase 2 から実装に入って差し支えない。**

---

## 付録 — 指摘ID一覧

| ID | 区分 | 章 | 要旨 | 由来 |
|---|---|---|---|---|
| R4-MEDIUM-1 | MEDIUM | スクリプト | mermaid 重複エッジチェックが、v3 で実際に起きた破損の形（ラベル違いの同一エッジ）と `}o--` 系を検出しない。実測で PASS を確認 | R3-MEDIUM-1 の修正起因 |
| R4-MEDIUM-2 | MEDIUM | §5.1 / §7.3 / §11.6 / §22.1 / §22.5 | `tasks.version` の残存、ER の `service_contracts.version` 欠落、§22.1 冒頭の3テーブル、`period_key` 部分ユニークの3箇所 | R3-MEDIUM-2 残存 |
| R4-MEDIUM-3 | MEDIUM | §12.4 | `generation_input_hash` が「対象月」に閉じ、リードタイム逆算が読む前月の営業日を含まない。説明文も Capacity 側の依存を落としている | R3-MEDIUM-7 の修正起因 |
| R4-MEDIUM-4 | MEDIUM | §24.2 | Step 5 の手順書に「サンプルを削除を押さない」が無い（警告は §24.4 のセルのみ） | R3-HIGH-2 残存 |
| R4-MEDIUM-5 | MEDIUM | §10.3 / §24.4 | `default_editor_member_id` の移行元・初期値が §24.4 に無く、§10.3 が現行に無い「動画追加での既定担当」を導入 | R3-MEDIUM-4 残存 + 修正起因 |
| R4-LATER-1 | LATER | §5.5 / §6.2 / §12.4 / §24.4 / §24.6 / スクリプト | 参照・注記・文言の不揃い7件 | R3-LATER-1 残存 + 軽微 |

---

## 付録 — 検証で行った一時変更について

§0.4 の検出力テストのため `SOCIAL_BASE_SYSTEM_DESIGN.md` を5回一時的に書き換えたが、**すべて元に戻している。**

- 変更前にスクラッチパッドへ全文バックアップを取得（md5 一致を確認）
- 各テスト後に復元し、最終的に `diff -q` でバックアップと一致することを確認
- `git status --porcelain` が空、`git diff --stat` が空、`check_doc_integrity.sh` が **PASS**（19項目）
- `ml-editing-board.html` は一切変更していない（スクリプトの `f264170` 無変更チェックも OK）

**本レビューで新規作成したのは本ファイル（`SOCIAL_BASE_SENIOR_REVIEW_4.md`）のみである。**
