# SOCIAL BASE — Senior Engineer Review（第3回）

| | |
|---|---|
| 対象 | `SOCIAL_BASE_SYSTEM_DESIGN.md` **v3**（3,450行 / 29章 / commit `9194c97`） |
| 前回 | `SOCIAL_BASE_SENIOR_REVIEW_2.md`（BLOCKER 0 / HIGH 3 / MEDIUM 9 / LATER 1） |
| 採否 | `SOCIAL_BASE_REVIEW_RESOLUTION.md` §8（ACCEPT 13 / PARTIAL 0 / REJECT 0） |
| 実測対象 | `ml-editing-board.html`（`f264170` から無変更であることを確認済み） |
| レビュー日 | 2026-08-29 |
| 判定 | **B. 修正後に実装開始可能** |
| 本レビューの指摘 | BLOCKER **0** / HIGH **3** / MEDIUM **7** / LATER **1** |

**Phase 1 の完了条件（unresolved BLOCKER = 0 かつ unresolved HIGH = 0）は満たしていない。** HIGH 3件はいずれも「v3 で入れた修正そのものが誤っている」型であり、Architecture の変更は不要。合計しても文言修正と制約定義1本に収まる。

---

## 0. 総評

### 0.1 v3 で確実に良くなった点

第1回・第2回で指摘した骨格の問題は、v3 でほぼすべて閉じている。とくに次は主張どおりに実在することを確認した。

- `conditions` は文書全体から消え、`predicates` に統一されている（`grep 'conditions'` の結果 **0件**）。§6.2 / §7.2 ER / §13.1 / ADR-023 の4層で一致。
- §11.5 の規則数は §11.5 本文・§28.2 R8 ともに **7規則**で一致。
- §28.1 のオーナー向け説明は「**15個 / 15項目**」に更新され、Q1〜Q15 の表と一致。
- §24.4 の `version` 行は表内（L2775、表の最終行）へ戻っており、第2回が指摘した「表外への脱落」は解消。
- §26.5 に Job Runner のハートビート行（L3011）、§26.6 に対応する記述（L3025）、§19.4 に根拠（L2261）。3箇所そろっている。
- §12.4 が書いた数値（poster がなつみ = **9契約 / 月34本**、りりか = **16本**）は実測と完全一致（§0.3）。

### 0.2 文字化け — 問題なし

意図しない文字コードの混入は**発見できなかった**。次をすべて機械的に確認した。

| 検査 | 結果 |
|---|---|
| UTF-8 妥当性（`iconv -f UTF-8 -t UTF-8`） | valid |
| 置換文字 U+FFFD | 0件 |
| latin1 二重エンコード痕（`Ã` / `Â` / `â€` / `ãƒ`） | 0件 |
| 制御文字（`\x00-\x08` `\x0B` `\x0C` `\x0E-\x1F` `\x7F`） | 0件 |
| NBSP（U+00A0）/ ZWSP（U+200B）/ 文中 BOM | 0件 |
| 半角カナ | 0件 |
| CRLF 混入 | 0件（LF のみ） |
| 先頭 BOM | 無し |

`SOCIAL_BASE_REVIEW_RESOLUTION.md` と `check_doc_integrity.sh` も UTF-8 として妥当。

### 0.3 事実検証（`ml-editing-board.html` の実測）

設計書 v3 が新たに書いた数値を `SEED_CONTRACTS`（`:1846-1859`）から数え直した。

| v3 の主張 | 実測 | 結果 |
|---|---|---|
| poster がなつみの契約は **9本** | `ncn-v` `sbs-v` `kino-v` `kino-s` `grace-v` `kaki-v` `kaki-s` `ibako-v` `ibako-s` = **9契約** | **正しい** |
| なつみ担当は月 **34本**（補正なし） | 6+6+4+2+4+3+3+3+3 = **34** | **正しい** |
| りりか担当 **16本**（補正あり） | `assist-v` `hiro-v` `metro-v` `nano-v` = 4契約 × 4 = **16** | **正しい** |
| 全50本 | 34 + 16 = **50** | **正しい** |
| 13契約 / 動画10・静止画3 / 10社 | distinct client 10、`kind:"動画"` 10 / `kind:"静止画"` 3 | **正しい** |
| `:1714` の `cfg.staff[poster] ? ... : null` により社員 poster には補正が掛からない | `STAFF_MAP = {"りりか","ゆかり","つかさ"}`（`:1861`）。なつみは不在 | **正しい** |

引用している行番号もすべて実装と一致した（`:1553` `PREV_STATUS` / `:1697` `generate` / `:1708` / `:1713` / `:1714` / `:1763` `assignEditors` / `:2217` `myTasks` / `:2255` / `:2285` サンプル削除 / `:3064` `scopeRows` / `:3333` `filteredRows`・`:3335` フィルタ / `:4383` `materialLate` / `:5019` `merge` / `:5180` `TODAY`）。

**§10.5 の3箇所表も実測で裏が取れた。** `myTasks()`（`:2219`）と `scopeRows()`（`:3066`）はどちらも `r.editor===ME || (contractById(r.contractId)||{}).poster===ME`、`filteredRows()`（`:3335`）だけが `r.editor===ME` のみ。`FILTER.editor` の初期値は `"__me__"`（`:2000`）なので、動画一覧の**既定表示も**スタッフでは絞られる。設計書の記述は正確である。

### 0.4 `check_doc_integrity.sh` の検証

スクリプトは実行して **PASS**（18項目すべて OK）。第2回 R2-MEDIUM-9 で指摘した「Resolution が書いた Core 純度チェックが実在しない」問題は、**チェック自体は実装された**（L77-87）。ただし後述 R3-MEDIUM-5 のとおり、**本件の対象語だった `publish_date` を検出しない**ことを実測で確認した。

### 0.5 問題を発見できなかった領域（問題なし）

次の領域は今回精査したが、新規の問題を発見できなかった。

- §6.5 RLS 運用規約（所有者ロール分離 / `force row level security` / `set local` / `nullif` の fail closed / プーラ構成テスト）
- §8 Identity（OIDC `sub` による同定、招待と email の役割分離）、§9 Permission Matrix、§20 Security
- §13 Automation（`predicates` の AND 結合、式 DSL を作らない方針、§13.6 実行保証）
- §15-17 Integration framework（`dedupeKey()` の Adapter 閉じ込め、`external_event_key` の provider 別表、接続主体の Workspace 化）
- §21 API / §23 Background Jobs / §26 Deployment / §29 ADR ログ
- §3.3 Layer A の分割（`assignEditors()` は v2 の時点で列挙済み）、§6.6 expand/contract
- 章立て・目次・節番号・相互参照・コードフェンス・mermaid 宣言・末尾終端（スクリプトで機械確認、すべて OK）

---

## 1. 第2回指摘の解消状況

| ID | 判定 | 根拠 |
|---|---|---|
| **R2-HIGH-1** `generate()` の投稿担当補正 | **部分的** | §12.4 手順5 を条件付きに変更（L1683）、専用小節「投稿担当の補正は『勤務予定がある人』にだけかかる」を新設（L1694-1702）。数値は実測一致。**未反映**：(a) §24.6 の `generate()` 行（L2824）に注記なし、(b) §25.1 に「補正されないこと」のテスト無し、(c) 契約単位ループ化で静止画契約の `poster` が読まれるようになる挙動変更の明記なし |
| **R2-HIGH-2** `seeded` の扱い | **部分的（新たな矛盾を生成）** | Q15 追加（L3159）、§24.4 の `seeded` を Method E → **A**（L2771）。**未反映**：§24.2 Step 2-1（L2643）は「件数・月・キーの有無」のままで `seeded` が無い／Step 5-3（L2658）に分岐なし／Step 5-1（L2656）にサンプル削除禁止のチェックなし。**さらに** Q15 の推奨案が第2回の警告した破壊経路そのもの、§14.3 と §24.4 が矛盾（→ **R3-HIGH-2**） |
| **R2-HIGH-3** `contract_version_hash` / 有効期間 | **部分的** | 定義を §12.4 に追加（L1714）、生成対象の絞り込みを追加（L1716）、`service_contracts.version` を §6.4 に追加（L655）。**未反映**：§5.2（L455）と §7.3 ER（L1019-1032）に `version` が無い／§22.1（L2434）の version 保有テーブル一覧に `service_contracts` が入っていない／§25.1・§25.5 にテスト無し（→ **R3-MEDIUM-2** / **R3-MEDIUM-6**） |
| **R2-MEDIUM-1** 章またぎ矛盾7箇所 + 破損2件 | **部分的（1件が別の破損に置換）** | #1 §5.4 `status`（L485）✓ / #2 §5.4 `trans{}`（L489）✓ / #4 §7.1 ER `tasks.version` 削除（L841-844）✓ / #5 §7.2 ER `predicates`（L928）✓ / #6 §13.1（L1754）✓ / #7 §28.2 R8「7規則」（L3172）✓ / 「12個→15個」（L3187・L3197）✓ / §24.4 `version` 行の表内復帰（L2775）✓。**#3 は「削除」ではなく「置換」され、§7.1 ER に重複エッジが発生**（→ **R3-MEDIUM-1**）。表破損検出チェックのスクリプト追加は行われていない |
| **R2-MEDIUM-2** `tasks.version` の矛盾 | **部分的** | §6.2（L605）から削除し「`version` を持たない」と明記 ✓、§7.1 ER（L841-844）から削除 ✓。**§5.1 L410 に `version` が残存**。要求された集約規約（`tasks` / `task_assignments` の変更時に `content_items.version` をインクリメントする）は未追加。§25.2 のテストも未追加（→ **R3-MEDIUM-2**） |
| **R2-MEDIUM-3** `period_key` NULL の UNIQUE | **部分的** | §6.2 L603 に部分ユニーク2本 ✓（理由も明記）。**§5.1 L421 / §11.6 L1599 / §22.5 L2493 / §25.2 L2894 は旧「1本」の記述のまま**（→ **R3-MEDIUM-2**） |
| **R2-MEDIUM-4** `unique(id, workspace_id)` の適用範囲 | **部分的** | §6.2 前書き（L590）を「`id` 列を持つ全テーブル」に ✓、§6.1 に結合テーブル除外の段落（L586）を追加 ✓。**同じ §6.1 の L563 が「全テーブルに機械的に適用する規約である」のまま残り、23行後の L586 と自己矛盾**。`users` / `webhook_receipts` の UNIQUE 例外は依然未記載（→ **R3-MEDIUM-3**） |
| **R2-MEDIUM-5** `assignEditors()` の欠落 | **部分的（実行順が誤り）** | §12.4 に手順8 を追加（L1687-1690）✓、§25.1 に `assignEditors` ✓（L2876）、§24.6 に Parity 行 ✓（L2825）。**手順8 が merge（手順7）の後に置かれており、現行の実行順（`:4992` → `:4994` → `:5005`）と逆**。§10.3 L1438 との矛盾も新規発生（→ **R3-HIGH-1** / **R3-MEDIUM-4**） |
| **R2-MEDIUM-6** undo Transition | **未解消（誤った根拠が追加された）** | §11.3 に undo 表5行（L1547-1553）✓、`workflow_transitions.kind` を §6.2 に追加（L602）✓、`is_terminal` の意味を明文化（L1557）✓。**しかし undo 行 `要修正 → 確認中`（L1551）は前進行 `要修正 → 確認中`（L1540）と (from, to) が完全一致し、L602 の `unique(template_id, from_state_id, to_state_id)` に違反する。** L1555 は「方向が逆なので衝突しない」と書くが、引き合いに出しているのは別のペア（`確認中 → 要修正` と `要修正 → 確認中`）であり、根拠が成立していない（→ **R3-HIGH-3**） |
| **R2-MEDIUM-7** ハートビート | **解消** | §26.5 に指標行（L3011、閾値10分）✓、§26.6 の Job Runner 停止行を書き換え（L3025）✓、§19.4（L2261）✓。Phase 10 完了条件への追加は無いが、Resolution もそれは主張していない |
| **R2-MEDIUM-8** 絞り込み3箇所 | **解消（軽微な残り）** | §10.5 に3箇所の表（L1456-1460）✓、`:3335` だけ poster を含まない差も明記 ✓、Read Model との対応も1文で追加（L1462）✓。実測とも一致。§5.5 の `TodayTasksQuery` 行に `scopeRows()` が入っていない点だけ残る（→ **R3-LATER-1**） |
| **R2-MEDIUM-9** `recommendations.kind` / Core 純度チェック | **部分的** | §7.2 ER を `"module-registered"` に修正（L981）✓、§18.3 に具体値の表あり（L2178-2181）✓。純度チェックはスクリプトに実装された（L77-87）が、**MODWORDS に `publish` / `publish_date` / `poster` / `editor` / `meeting` が無く、本件の対象語 `publish_date` を検出しない**（実測確認済み）。§6.2 の `recommendations` 行（L619）に §18.3 への注記も無い（→ **R3-MEDIUM-5**） |
| **R2-LATER-1** LATER-1 / 3 / 4 | **解消** | §28.3 に3件とも追記（L3182-3184）。Resolution の主張どおり |

### 集計

| 区分 | 件数 | 解消 | 部分的 | 未解消 | 悪化 |
|---|---|---|---|---|---|
| HIGH | 3 | 0 | 3 | 0 | 0 |
| MEDIUM | 9 | 2 | 6 | 1 | 0 |
| LATER | 1 | 1 | 0 | 0 | 0 |

**「悪化」に分類した項目は無いが、R2-MEDIUM-6 は『誤った根拠を設計書に書き込んだ』という点で単なる未解消より質が悪い。** 第2回が「(from, to) が重複する」と具体的に指摘した箇所に対し、v3 は別のペアを引いて「衝突しない」と断定している。実装者はこの1文を読んで検証をやめる。

### 「主張と実体の乖離」の再発について

第2回 R2-MEDIUM-9 と同種の乖離を意図的に探した。**Resolution §8 が「入れた」と書いて実物が無いものは無かった。** 13件すべてについて、主張された変更の中核部分は v3 に実在する。ただし次の3つは「主張は真だが、それだけでは目的を達しない」型の乖離である。

1. **Core 純度チェック**（R2-MEDIUM-9） — 実装されたが、作った理由である `publish_date` を検出しない。実測で `text kind "publish_date or assignment"` を §7.2 ER に戻しても `OK Core の章に Module 固有語なし` を返す。`動画` に置き換えると正しく FAIL するので、範囲の走査自体は効いている。**語彙が目的に届いていない。**
2. **§24.4 の `seeded` 行**（R2-HIGH-2） — 「Step 2-1 の実測で `seeded` の値と行数を確認し」と書くが、**参照先の §24.2 Step 2-1 に `seeded` の項目が無い**。文書内で存在しない手順を参照している。
3. **§12.4 手順8**（R2-MEDIUM-5） — 追加されたが実行順が現行と逆。「移植する」と書きながら移植になっていない。

---

## 2. 新規および残存の指摘

### 2.1 BLOCKER

**なし。**

Modular Monolith / Core と Module の分離 / PostgreSQL を SoT / Outbox + Job / 段階移行という骨格は v3 でも維持されており、Workspace 分離のスキーマ層・実行環境の防御も第2回の確認から後退していない。v3 の変更はいずれも列・制約・手順・文言の範囲に収まっており、Architecture を揺らすものは無い。

---

### 2.2 HIGH

#### R3-HIGH-1 — §12.4 の手順8（`assignEditors()`）が merge の後に置かれており、実行順が現行と逆になっている

**1. 該当設計**
§12.4 の生成手順（L1677-1691）、とくに手順7（L1685-1686、`merge`）と手順8（L1687-1690、`assignEditors()`）。関連：§24.6 の `assignEditors()` Parity 行（L2825）、§10.3（L1438）。

**2. 問題**
現行の翌月生成は次の順で走る。実測した3行は連続しており、間に merge は無い。

```js
:4992  var plan=generate({contracts:SEED_CONTRACTS, ym:target, genDate:TODAY, shifts:SEED_SHIFTS, staff:STAFF_MAP});
:4993  var caps={}; Object.keys(STAFF_MAP).forEach(function(n){ caps[n]=capacityOf(SEED_SHIFTS,STAFF_MAP[n],target).cap||1; });
:4994  assignEditors(plan, caps);
       ...
:5005  var r=merge(target, plan);   // ← CTA の onclick。assignEditors より後
```

つまり **`assignEditors()` が触るのは `plan`（新規生成された行）だけ**であり、`merge()` が保持する既存行（`status !== "未着手"` または `dateLocked`）の `editor` には一切触れない。

§12.4 は手順7で merge を済ませたあとに手順8で `assignEditors()` を実行する順で書いている。**素直に実装すると、`assignEditors()` が merge 後の全行を対象に走る。**

`assignEditors()`（`:1763-1781`）はクライアント単位でまとめて `r.editor = best` を上書きする実装である。

```js
:1778  byClient[c].forEach(function(r){ r.editor=best; });
```

対象が「その月の全行」になると、**進行中・ロック済みの行の担当も毎月書き換わる**。

**3. 発生する具体的リスク**
- §12.4 手順7 が自ら宣言している「進行中・`date_locked` は動かさない」という不変条件を、手順8 が直後に破る。**設計書が1つの手順ブロックの中で自己矛盾している。**
- §10.3 L1438 の「契約の既定を変えても既存 Task の担当は動かない（現行の `merge()` が進行中の行を動かさないのと同じ思想）」も同時に破れる。
- 業務上の症状は「翌月分を生成したら、今月まだ編集中の案件の担当者が別人に変わっていた」。**エラーにならず、通知もされない。** 担当を手で戻すしかないが、翌月また同じことが起きる。
- Capacity 比例配分の入力集合も変わる（新規行だけ → 既存行を含む全行）ため、**§24.6 に v3 で追加した `assignEditors()` の Parity は落ちる。** 落ちる理由が「順序」であることは、担当割当アルゴリズムを見比べても分からない。

**4. 推奨変更**
- **手順8 を手順6と7の間へ移す。** 番号を振り直すか、`6.5` として挿入する（第2回の推奨もこの位置だった）。手順の直後に「**`assignEditors()` は `merge()` の前に、生成された新規行だけを対象に実行する。既存行の担当は変更しない**」を1文添える。
- §24.6 の `assignEditors()` 行に「**merge 前の `plan` に対する出力を期待値にする**」を追記する。比較対象が曖昧なままだと Parity テストが書けない。
- §25.1 に「進行中の ContentItem を含む月で生成を実行しても、進行中の行の担当が変わらないこと」を1ケース追加する。

**5. 過剰設計にならない最小修正**
**手順の行を1つ上へ移動し、注記1文と Parity 1行とテスト1ケース。** 実装量はゼロ。後から気付くと、本番で担当が書き換わった月のデータを手で復元することになる（`task_assignments` の履歴は `audit_logs` にしか残らない）。

---

#### R3-HIGH-2 — Q15 の推奨案が「当月の全行を削除する」経路を選んでおり、Parity がこれを検出しない。§24.2 の3箇所も未反映で、§14.3 と矛盾する

**1. 該当設計**
§28.1 Q15（L3159）、§24.4 の `seeded` 行（L2771）、§24.2 Step 2-1（L2643）/ Step 5-1（L2656）/ Step 5-3（L2658）、§24.3（L2689）、§14.3（L1877）、§24.6 件数の一致（L2804）。

**2. 問題**
第2回 R2-HIGH-2 は取り得る3経路を挙げ、**3つ目（移行前に「サンプルを削除」を押す）を「Parity が 0件 対 0件 で通過してしまう」経路として警告した**。v3 の Q15 は、その3つ目を**推奨案として採用している**。

> Q15 … 推奨案：**artifact 側で「サンプルを削除」を実行してから抽出し、実データのみで始める**

実装を読むと、このボタンは「サンプル行」を選んで消しているのではない。

```js
:2285  if(kind==="unseed") return h("button",{class:"mini",text:"サンプルを削除",onclick:function(e){
:2287    STATE.rows=STATE.rows.filter(function(r){ return r.month!==monthOf(TODAY); });
:2288    STATE.seeded=false; SELECTED=null; POP=false;
```

**`monthOf(TODAY)` の全行を無条件に削除する。** 設計書自身が §24.3 L2689 で「『サンプルを削除』（`:2285`）— **当月の全行を削除する**」と正しく書いている。にもかかわらず Q15 はそれを推奨案にしており、**§24.3 と §28.1 が同じ操作について反対の評価を与えている。**

さらに §24.2 には第2回が求めた3点がいずれも入っていない。

| 求められた反映 | v3 の実態 |
|---|---|
| Step 2-1 の実測項目に `seeded` を加える | L2643 は「件数・月・キーの有無を実測する」のまま。**`seeded` が無い** |
| Step 5-3 に `seeded` による分岐を書く | L2658 は「最終 STATE を再抽出し、スナップショット同期を流す」のまま |
| Step 5-1 に「サンプルを削除を押さないこと」を加える | L2656 は未同期バナーの確認のみ |

**§24.4 L2771 が「Step 2-1 の実測で `seeded` の値と行数を確認し」と書いているのに、その Step 2-1 が存在しない。** 文書内参照が空振りしている。

加えて **§14.3 L1877 と §24.4 L2771 が矛盾する。**

- §14.3：`サンプル表示中` → **廃止**（移行時に実データのみになる）
- §24.4：（Q15 で (a) を選び）移した場合は**新環境にも「この月はサンプルです」の表示を残す**

Q15 の (a) を選んだ瞬間、§14.3 が「作らない」と決めた UI を §24.4 が要求する。どちらが正典か決まっていない。

**3. 発生する具体的リスク**
- **推奨案どおりに進めた場合。** 金曜夜、カットオーバー直前に artifact で「サンプルを削除」を押す。`STATE.rows` から当月分（実測では50行すべてが 2026-08）が消える。Step 5-3 でこの STATE を抽出し、§24.3 のスナップショット同期が「抽出した `rows` の `legacy_id` 集合に含まれない `content_items` を archive」する。**当月の `content_items` が全件 archive される。** Step 5-4 の Parity（§24.6 L2804「`content_items` の件数 = `STATE.rows` と一致」）は **0件 対 0件で通過する。** 月曜の朝、全員の「今日」画面が空になる。artifact に undo は無い。
- 削除の対象が `monthOf(TODAY)` だけである点も効く。翌月分を先に生成していた場合、翌月の行だけが残り、**当月が空・翌月だけ存在するという状態で本番が始まる。**
- (a) を選んだ場合は §14.3 との矛盾により、「サンプルです」の警告表示を作る担当が決まらない。§14.3 を実装仕様として読む人は作らないので、**架空の進捗が注意書き無しで本番になる**（第2回が指摘した2つ目の経路がそのまま残る）。

**4. 推奨変更**
- **Q15 の推奨案を差し替える。** 第2回の推奨どおり「**進捗（`status` / `postedAt` / `trans` / `actor`）だけを初期化し、`publish_date` と担当は引き継ぐ**」を推奨とする。artifact の「サンプルを削除」は押さない。進捗の初期化は import 側で行う（`workflow_runs.current_state_id` を初期 State に、`published_at` を NULL に）ほうが、行が消えないぶん安全で Parity も効く。
- **Q15 の「決めないとどうなるか」を書き換える。** 現在の「Step 2-1 の抽出方法が決まらない」は軽すぎる。「**サンプルの進捗が本番の初期状態になるか、当月の全案件が消えるかが切り替え日に決まる。どちらも Parity では検出できない**」とする。
- **§24.2 に3点を反映する。** Step 2-1 の実測項目に `seeded` を明記／Step 5-1 のチェックに「カットオーバー前に『サンプルを削除』（`:2285`）を押さない。押すと当月の全行が消え、復元できない」を追加／Step 5-3 に `seeded` による分岐を1段落。
- **§24.6 に空振り防止のガードを1行足す。** 「`content_items` が0件、または当月の件数が Step 2-2 で確定した期待値を大きく下回る場合は、一致していても Parity を失敗とする」。件数一致だけを見る Parity は、両方が0のとき最終防衛線にならない。
- **§14.3 の `サンプル表示中` 行を書き換える。** 「廃止。ただし Q15 で (a) を選んだ場合のみ、`workspaces.settings.seeded_source` を根拠に同等の表示を残す」。§24.4 と一致させる。

**5. 過剰設計にならない最小修正**
**Q15 の推奨案1セル、§24.2 に3行、§24.6 にガード1行、§14.3 に但し書き1文。** 実装はいずれも import スクリプト側で、新しい仕組みは不要。決めずに切り替え日を迎えると、その場の判断が戻せない結果を生む。

---

#### R3-HIGH-3 — §11.3 の undo Transition `要修正 → 確認中` が `unique(template_id, from_state_id, to_state_id)` に違反する。設計書は成立しない根拠でこれを否定している

**1. 該当設計**
§11.3 の前進 Transition 表（L1534-1541）、undo Transition 表（L1547-1553）、L1555 の注記、§6.2 `workflow_transitions` 行（L602）、§24.4 の `PREV_STATUS` 行、§25.4。

**2. 問題**
2つの表を並べると、次の1組が (from, to) で完全に一致する。

| 表 | from | to | ボタン文言 | `kind` |
|---|---|---|---|---|
| 前進（L1540） | 要修正 | 確認中 | 修正完了 | `forward` |
| undo（L1551） | 要修正 | 確認中 | 要修正を取り消す | `undo` |

§6.2 L602 の制約は `unique(template_id, from_state_id, to_state_id)` である。**`kind` は UNIQUE に含まれていない。** したがってこの2行を同じ Template に投入できない。

v3 はこれを明示的に否定している。

> L1555 … `unique(template_id, from_state_id, to_state_id)` は方向が逆なので前進側と衝突しない（「確認中 -> 要修正」と「要修正 -> 確認中」は別の行）。

この説明が扱っているのは `確認中 → 要修正`（前進、L1539）と `要修正 → 確認中`（undo、L1551）の組であり、確かにこの2つは別の行である。**しかし衝突しているのは L1540 と L1551 であって、この組ではない。** 第2回 R2-MEDIUM-6 の 2-(a) が指摘したのはまさに L1540 との重複であり、v3 は指摘された組を検証せずに別の組で反証している。

なお undo の残り4行（`編集中→未着手` / `確認中→編集中` / `投稿待ち→確認中` / `投稿済み→投稿待ち`）は前進表に対応する (from, to) が無いため衝突しない。**問題は1行だけである。**

**3. 発生する具体的リスク**
- Phase 4 で `workflow_transitions` を seed する時点で、11行目の INSERT が unique violation で失敗する。**Phase 2 で確定させた制約を Phase 4 で作り直すことになる。** `workflow_transitions` は `workflow_run_transitions.transition_id` の参照先であり、§11.5 規則2 で物理削除を禁じている表なので、制約の張り替えは後になるほど面倒になる。
- L1555 を読んだ実装者は「検証済み」と受け取り、`unique(template_id, from_state_id, to_state_id)` をそのまま migration に書く。**設計書が誤りを保証してしまっている**ため、レビューでも気付かれにくい。
- 仮に `kind` を UNIQUE に含める方向で回避すると、今度は「同じ (from, to) に前進と undo の2つの遷移が存在する」状態になる。§9.4 の Transition 単位の権限、§24.4 の検証項目「1段戻すが現行と同じ遷移先」、`workflow_run_transitions.transition_id` からの逆引きがすべて2値になり、**どちらの遷移だったかを UI とログで区別する必要が生じる。** これは設計判断であり、暗黙に決めてよいものではない。

**4. 推奨変更**
次のどちらかを **§11.3 で明示的に選ぶ**。第2回の推奨と同じく (A) を推す。

- **(A) undo 表から `要修正 → 確認中` を削除し、既存の `修正完了`（L1540）を流用する。** §11.3 に「**要修正からの『1段戻す』は前進の `修正完了` Transition を共用する。`unique(template_id, from_state_id, to_state_id)` があるため専用行は作れない**」と1文書く。現行 UI も `PREV_STATUS["要修正"] === "確認中"` で遷移先が同じなので、挙動は変わらない。監査上は `修正完了` として記録される旨も添える。
- **(B) 制約を `unique(template_id, from_state_id, to_state_id, kind)` に変更する。** その場合は §6.2 L602 を書き換え、UI とログで前進と undo を区別する規約を §11.3 に足す。

**いずれを選んでも L1555 の注記は書き換える必要がある。** 現在の文は成立していない。

- §25.4 に第2回が求めた2ケース（「終端 State からの `undo` 遷移が通ること」「`forward` として終端から出られないこと」）を追加する。
- §24.4 の `PREV_STATUS` 行の検証「1段戻すが現行と同じ遷移先」に、比較対象として §11.3 の undo 表を指す参照を入れる。

**5. 過剰設計にならない最小修正**
**表から1行削除し、注記を1文差し替え、テスト2ケース。** (A) ならスキーマは一切変わらない。ただし決定は Phase 2 の migration に入るため、**スキーマ確定前に済ませる必要がある。**

---

### 2.3 MEDIUM

#### R3-MEDIUM-1 — §7.1 ER に `workflow_runs ||--|| tasks` の重複エッジが生まれ、`workflow_transitions.kind` が ER に無い

**1. 該当設計** §7.1 ER（L757-758、L816-824）。

**2. 問題**
第2回 R2-MEDIUM-1 の #3 は「`workflow_templates ||--o{ tasks : "適用"` の行を**削除する**」だった。v3 はこれを**置換**した。

```
L757    workflow_runs ||--|| tasks : "進行を持つ"    ← v3 が置いた行
L758    workflow_runs ||--|| tasks : "進行"          ← 元からあった行
```

同じ2実体の間に同じカーディナリティのエッジが2本ある。Mermaid は構文エラーにならず、平行な線を2本描く。図としては読めるが、**「Run と Task の関係が2つある」と誤読される**。

同じ ER の `workflow_transitions` ブロック（L816-824）には、v3 で §6.2 に追加した **`kind` 列が入っていない**。第2回自身が「ER 図は実装時に最も参照される図であり、スキーマ表より ER のほうが読まれる」と書いた根拠がそのまま当てはまる。

**3. 発生する具体的リスク**
重複エッジは実害が小さいが、**この破損は `check_doc_integrity.sh` の重複行チェックを素通りした**（重複行検査は ` ``` ` で囲まれたブロックをスキップするため、mermaid 内の重複は原理的に検出できない）。第2回で「今回の破損は既存のどのチェックにも掛からなかった」と書いた状況が、v3 でそのまま再現している。

`kind` の欠落は、`workflow_transitions` を ER から起こす実装者が undo と forward の区別を持たない表を作る経路を残す。R3-HIGH-3 の (B) を選んだ場合は制約にも関わる。

**4. 推奨変更**
- L757 を削除する（L758 だけ残す）。
- `workflow_transitions` の ER ブロックに `text kind "forward or undo"` を追加する。
- `check_doc_integrity.sh` の重複行チェックを、**mermaid ブロック内でも走らせる**（`erDiagram` / 実体ブロックの `{` `}` は除外し、`||--` を含む関連行だけを対象にすれば誤検出しない）。

**5. 過剰設計にならない最小修正** **ER の1行削除・1行追加と、スクリプトに awk 1本。**

---

#### R3-MEDIUM-2 — v3 のスキーマ変更3件が §5 / §7 ER / §22 / §25 に波及していない

**1. 該当設計** 下表のとおり。

**2. 問題**
v3 は §6.2 / §6.4 のスキーマ表を正しく直したが、同じ事項を語る他章が旧記述のまま残っている。第2回 R2-MEDIUM-1 とまったく同じ形の取りこぼしである。

| 変更した事項 | 直した箇所 | 残っている箇所 |
|---|---|---|
| `tasks` から `version` を削除 | §6.2 L605 / §7.1 ER L841-844 / §22.1 L2452 | **§5.1 L410**（`... / created_by / version / created_at / updated_at`） |
| `service_contracts` に `version` を追加 | §6.4 L655 / §12.4 L1714 | **§5.2 L455**（列一覧に `version` 無し）/ **§7.3 ER L1019-1032**（`version` 無し）/ **§22.1 L2434**（version を持つのは `content_items` / `workflow_runs` / `daily_notes` の3つと明記。`service_contracts` が入っていない） |
| `workflow_runs` の部分ユニーク2本 | §6.2 L603 | **§5.1 L421** / **§11.6 L1599** / **§22.5 L2493** / **§25.2 L2894**（いずれも旧「`unique(workspace_id, template_id, subject_type, subject_id, period_key)` 1本」の記述） |

`service_contracts.version` の残り方はとくに具合が悪い。**`contract_version_hash` は `(id, version)` だけを材料にする**（§12.4 L1714）ので、`version` が上がらなければ翌月生成の冪等キーは永久に同じ値になる。§12.4 は「契約内容を変えるたびに上がる」と書くが、§6.1 は `version` を「更新競合を扱うテーブル」に限定し、§22.1 はその対象を3テーブルに列挙して `service_contracts` を含めていない。**誰がどの操作でインクリメントするかを定めた文が、文書のどこにも無い。**

同じく、第2回 R2-MEDIUM-2 が求めた集約規約（`tasks` / `task_assignments` を変更する Command が同一トランザクションで `content_items.version` をインクリメントする）も未追加のままである。§22.1 の対応表は「担当変更 → `content_items.version`」と書くが、**誰がその version を進めるかを書いていない。** 検証は通るが version が進まない実装になると、楽観ロックが何も守らない。

**3. 発生する具体的リスク**
- §5.1 / §7.3 ER を見た実装者が `tasks.version` を作る／`service_contracts.version` を作らない。前者は「どの version を送るか」の不定が復活し、後者は Phase 2 の migration から列が落ちる（**後から足すと既存契約の hash が全部変わる**）。
- `version` を上げる主体が未定義のまま実装されると、契約を変更しても `contract_version_hash` が変わらず、**再生成が冪等キーで握り潰されて変更が反映されない。** 第2回 R2-HIGH-3 が「実装したが効いていない型の故障」と呼んだものがそのまま残る。
- `period_key` NULL の部分ユニークが §22.5 と §25.2 に反映されていないため、**`ON CONFLICT` の対象インデックスをどちらにするかが未定義**で、§25.2 のテストも NULL ケースを通らない。第2回 R2-MEDIUM-3 が指摘した孤児 Run の経路は塞がっていない。

**4. 推奨変更**
- §5.1 L410 から `version` を削除する。
- §5.2 L455 と §7.3 ER の `service_contracts` ブロックに `version` を追加する。
- **§22.1 L2434 を「`content_items` / `workflow_runs` / `daily_notes` / `service_contracts` に `version integer` を持つ」に改め、直後に2文足す。** 「`service_contracts.version` は生成結果に影響する列（`kind` / `monthly_count` / `step_count` / `lead_time_business_days` / `default_publisher_member_id` / `workflow_template_id` / `starts_on` / `ends_on` / `status`）を変更する Command が同一トランザクションでインクリメントする」「**`tasks` および `task_assignments` を変更する Command は、同一トランザクションで対応する `content_items.version` をインクリメントする**（ContentItem を集約のルートとして扱う）」。
- §5.1 L421 / §11.6 L1599 / §22.5 L2493 / §25.2 L2894 を部分ユニーク2本の記述に揃える。§22.5 には「`period_key` が NULL の場合は部分ユニークインデックス側を `ON CONFLICT` の対象にする」を1文。

**5. 過剰設計にならない最小修正** **列の追加削除3箇所、規約2文、既存文の文言修正4箇所。** 実装量はゼロだが、`service_contracts.version` だけは Phase 2 の初回 migration に入る。

---

#### R3-MEDIUM-3 — §6.1 の UNIQUE 規約が同一節の中で自己矛盾している

**1. 該当設計** §6.1 L556-561（例外表）、L563、L586。

**2. 問題**
L563 は第2回の指摘後も原文のまま残っている。

> **全テーブルに `unique(id, workspace_id)` を張り**、すべての親子参照を複合外部キーにする。… これは「例」ではなく**全テーブルに機械的に適用する規約である**。

その23行後に、v3 が追加した段落がこれを否定する。

> **`id` を持たない結合テーブルは対象外。**（L586）

同じ節の中で「全テーブルに機械的に」と「対象外がある」が併存している。加えて **`users` / `webhook_receipts` の扱いが依然として未記載** である。L556-561 の例外表は `workspace_id` を持たないことの例外を説明したもので、この2表には `unique(id, workspace_id)` を張りようがないが、UNIQUE 規約側の例外としては明示されていない。第2回 R2-MEDIUM-4 が「例外表は `workspace_id` についての例外しか扱っていない」と書いた点はそのまま残っている。

**3. 発生する具体的リスク**
実害は低いが、**この規約は「機械的に適用できること」を売りにしている**。Phase 2 の migration をループで生成する担当者は L563 と L586 のどちらを規約と読むかを自分で決めることになり、BLOCKER-1 で潰した「判断の余地」が入口に戻る。

**4. 推奨変更**
L563 を書き換える。

> **`id` を主キーとするテーブルには、例外なく `unique(id, workspace_id)` を張り、すべての親子参照を複合外部キーにする。** UNIQUE の無い親は複合外部キーで参照できないため、これは「例」ではなく機械的に適用する規約である。対象外は2種類だけで、他に例外を作らない — (1) `id` を持たない結合テーブル（下記）、(2) `workspace_id` を持たない `users` / `webhook_receipts`（上の例外表）。

L586 の段落は残し、冒頭を「(1) の詳細」として繋ぐ。

**5. 過剰設計にならない最小修正** **1段落の書き換え。** 実装量ゼロ。

---

#### R3-MEDIUM-4 — 手順8 の担当割当規則が §10.3 と矛盾し、`default_editor_member_id` の移行元が §24.4 に無い

**1. 該当設計** §12.4 手順8（L1687-1690）、§10.3（L1438）、§24.4 マスターデータ表（L2713-2727）。

**2. 問題**
2つの章が既定担当の役割について反対のことを言っている。

- **§10.3 L1438** — 「`default_editor_member_id` / `default_publisher_member_id` は**既定値**。**生成時に `task_assignments` へコピーされ**、以後は Task 側が正」
- **§12.4 L1689-1690** — 「割当は ServiceContract の既定担当ではなく毎月の Capacity で決まるため、既定担当（`default_editor_member_id`）は**「Capacity が同点のときの優先」として使う**」

editor については §12.4 が正しい（現行は `assignEditors()` が決める）。publisher については §10.3 が正しい（契約の `poster` をコピーする）。**しかしどちらの章も、その役割ごとの違いを書いていない。**

さらに、v3 が定めた「Capacity が同点のときの優先」というタイブレーク規則は**現行実装に存在しない**。`assignEditors()`（`:1770-1777`）は `Object.keys(caps)` の順序で走査し、`score` が厳密に大きいときだけ `best` を更新するので、同点なら**先に現れた名前が勝つ**。既定担当は一切参照しない。

移行時点では `SEED_CONTRACTS` に `editor` フィールドが無いため `default_editor_member_id` はすべて NULL であり（§24.4 のマスターデータ表にも**移行元の行が無い**）、実際の差分は出ない。だが §24.6 は `assignEditors()` の完全一致を Parity に入れているため、**規則の書き換えは Parity の前提を変える。**

**3. 発生する具体的リスク**
- Phase 5 で `default_editor_member_id` を画面から設定できるようにした瞬間、タイブレークが変わって**先月と今月で担当配分が変わる**。仕様として明示されていないため、原因の切り分けが Capacity 計算側へ向かう。
- §10.3 を読んだ実装者は「生成時に既定担当をコピーする」を editor にも適用する。§12.4 を読んだ実装者は Capacity で決める。**同じ操作に2つの実装が並ぶ。**
- §24.4 に移行元が無いため、`default_editor_member_id` を「何で埋めるか」が決まらない。NULL のままでよいことが明記されていない。

**4. 推奨変更**
- **§10.3 L1438 を役割ごとに分ける。** 「`default_publisher_member_id` は既定値として生成時に `task_assignments(role=publisher)` へコピーする。**`default_editor_member_id` は移行時点では未設定であり、編集担当は §12.4 の `assignEditors()` が毎月の Capacity で決める。** 既定担当は Capacity が同点のときの優先としてのみ使う」。
- **§12.4 手順8 に現行のタイブレークを明記する。** 「現行（`:1770-1777`）は同点のとき `caps` のキー順で先に現れた担当が勝つ。既定担当による優先は Phase 5 以降の追加であり、移行時点では `default_editor_member_id` が全件 NULL のため挙動は変わらない」。**§24.6 の Parity はこの但し書きがあって初めて成立する。**
- §24.4 のマスターデータ表に `default_editor_member_id` の行を1つ足す（移行元「なし」/ Method E / 検証「全件 NULL であること」）。第2回 R2-MEDIUM-5 が求めた §10.3 への注記はこれで満たされる。

**5. 過剰設計にならない最小修正** **§10.3 の1文を2文に分け、手順8 に但し書き1文、§24.4 に1行。**

---

#### R3-MEDIUM-5 — Core 純度チェックが目的の語を検出せず、mermaid 内の破損も素通りする

**1. 該当設計** `check_doc_integrity.sh` L77-87、§6.2 `recommendations` 行（L619）。

**2. 問題**
第2回 R2-MEDIUM-9 は、`recommendations.kind` に `publish_date` が literal で書かれていた事実を根拠に「`client` / `video` / `instagram` / `drive` / `slack` / **`publish`** / `shoot` / **`meeting`** / **`poster`** / **`editor`** の語が現れたら FAIL にする」チェックを求めた。v3 が実装した語彙は次のとおりである。

```sh
MODWORDS='client|video|instagram|drive|slack|publish_planned|shoot|client_meeting|planning_due|analysis_due|動画|クライアント'
```

`publish` ではなく `publish_planned`、`meeting` ではなく `client_meeting`。`poster` と `editor` は無い。**このチェックは `publish_date` を検出しない。**

実測で確認した。§7.2 ER の該当行を v2 の状態（`text kind "publish_date or assignment"`）に戻してスクリプトを走らせると、

```
  OK   Core の章に Module 固有語なし
```

同じ位置を `動画 or assignment` に置き換えると正しく FAIL するので、走査範囲（`5.1 Core:5.2` / `6.2 Core:6.3` / `7.1 Core:7.2` / `7.2 Core:7.3`）そのものは効いている。**語彙が目的に届いていない。**

同じく、R3-MEDIUM-1 の ER 重複エッジは、重複行チェックが ` ``` ` ブロックを丸ごとスキップするため検出されない。**分割追記による破損を機械的に捕まえるという、このスクリプトの目的が2箇所で達成されていない。**

§6.2 の `recommendations` 行（L619）には、`schedule_entries` 行（L607）と違って「`kind` は Module が登録する不透明な文字列。Core は具体値を知らない」の注記が無い。規約の適用が表の中で不揃いになっている。

**3. 発生する具体的リスク**
「機械チェックがあるから同じ混入は再発しない」という前提が成立しない。次に `publish_date` / `poster` / `editor` が Core の章へ混入しても、スクリプトは PASS を返す。**PASS が偽の安心を与えるぶん、チェックが無いより悪い。**

**4. 推奨変更**
- MODWORDS に `publish_date` / `poster` / `editor` / `meeting` を足す。`publish` と `meeting` を素で入れると `publish_planned` / `client_meeting` を含む正当な参照文にも当たるので、**語のリストは維持したまま不足分を列挙する**のが安全（`publish_date|poster|editor|meeting` を追加）。
- 重複行チェックに mermaid の関連行（`||--` / `}o--` / `}|--` を含む行）を対象へ加える。
- §6.2 L619 に「`kind` は Module が登録する不透明な文字列。Core は具体値を知らない（値は §18.3）」を追記する。

**5. 過剰設計にならない最小修正** **正規表現に4語、awk 1行、表に注記1つ。**

---

#### R3-MEDIUM-6 — 第2回が求めたテストケースが §25 にほぼ反映されていない

**1. 該当設計** §25.1（L2871-2882）、§25.2（L2884-2895）、§25.3（L2897-2909）、§25.4（L2911-2919）、§25.5（L2921-2927）。

**2. 問題**
v3 で §25 に入った変更は、§25.1 の生成ロジック一覧へ `assignEditors` を足した1箇所だけである。第2回が各指摘の「推奨変更」で具体的に挙げたテストは、次のとおり未追加である。

| 由来 | 求められたテスト | v3 |
|---|---|---|
| R2-HIGH-1 | poster が勤務予定を持たない契約で投稿予定日が補正されないこと | 無し |
| R2-HIGH-3 | `ends_on` を過ぎた契約が生成対象に入らないこと／契約を変えずに2回実行して `contract_version_hash` が同一であること | 無し |
| R2-MEDIUM-2 | `tasks.due_at` を古い `content_items.version` で更新すると 409 になること | 無し |
| R2-MEDIUM-3 | `period_key = NULL` の Run が同一対象に2件作れないこと | 無し（L2894 は旧制約のまま） |
| R2-MEDIUM-6 | 終端 State からの `undo` が通ること／`forward` として終端から出られないこと | 無し |
| R2-MEDIUM-8 | `editor` Role 保持者の「今日」画面と動画一覧の既定表示が絞られること | 無し |

**3. 発生する具体的リスク**
v3 で入れた仕様のうち、**非対称な挙動・NULL の扱い・undo・スコープ既定値という「読み落としやすい側」がテストで固定されない。** §25.1 の「現行 JS の出力を期待値として使う」方針は強力だが、`generate()` の入出力ペアを固定しても、`working_schedules` が空の場合という**入力の作り方**まではテストにならない。設計書に条件を1文書いても、テストが無ければ Phase 5 の改修で静かに戻る。

**4. 推奨変更**
上表の6ケースを §25.1 / §25.2 / §25.3 / §25.4 / §25.5 の該当箇所へそれぞれ1行ずつ追加する。あわせて §25.2 L2894 の制約名を部分ユニーク2本に合わせる（R3-MEDIUM-2）。

**5. 過剰設計にならない最小修正** **箇条書き6行。** テスト自体は Phase ごとに書くので、設計書側の作業は列挙だけである。

---

#### R3-MEDIUM-7 — `contract_version_hash` が生成結果を左右する他の入力を含まない

**1. 該当設計** §12.4 冪等性（L1712-1714）、§22.3（L2475）。

**2. 問題**
翌月生成の出力は3つの入力で決まる。

| 入力 | 影響する手順 |
|---|---|
| `service_contracts` | 手順2（本数）/ 手順6（リードタイム）/ 手順5（poster） |
| `working_schedules` | 手順5（非稼働日の寄せ）/ 手順8（Capacity 比例の担当割当） |
| `business_calendar` / `time_offs` | 手順1（営業日集合） |

`idempotency_key = workspace_id + target_month + contract_version_hash` は**1つ目しか見ていない**。

**3. 発生する具体的リスク**
「りりかのシフトを直したので翌月分を作り直す」という運用が成立しない。契約を変えていなければ hash は同じなので、§22.3 の手順2（同じ key + 同じ request_hash → **保存済みのレスポンスをそのまま返す**）に入り、**再生成が実行されないまま成功が返る。** 画面の配置は変わらないので、利用者は「反映されない」としか分からない。第2回 R2-HIGH-3 が (a) の帰結として挙げた「変更したのに同じキーになり、変更が反映されない」と同じ故障である。

`idempotency_keys` に `expires_at` があるので永久ではないが、**保持期間は §28.3 で未決**であり、現時点では「いつ解けるか分からない」。

**4. 推奨変更**
次のどちらかを §12.4 の冪等性に明記する。

- **(A)** hash の対象を「生成に使う入力のスナップショット」に広げる。`contract_version_hash` を `generation_input_hash` に改め、対象契約の `(id, version)` に加えて、対象月の `working_schedules` の `(workspace_member_id, date, starts_at, ends_at)` と `business_calendar` の `(date, kind)` を含める。
- **(B)** 冪等キーは連打防止に限ると割り切り、`idempotency_keys.expires_at` を**短く固定する**（例：10分）。§12.4 に「シフトや営業日カレンダーを変えてからの再生成は、冪等キーの有効期限が切れてから、または画面から明示的に『再生成』を選んで行う」と書く。

推奨は **(B)**。(A) は入力が増えるたびに hash 定義を直す必要があり、P5（過剰設計をしない）に反する。翌月生成は月1回の操作なので、連打防止に必要な窓は数分で足りる。

あわせて §28.3 の保持期間の項目に `idempotency_keys` を加える。

**5. 過剰設計にならない最小修正** **§12.4 に1段落、§28.3 に1語。** (B) なら実装は既存の `expires_at` 設定値だけである。

---

### 2.4 LATER

#### R3-LATER-1 — 参照の不揃い3件（実害なし・文言のみ）

| # | 箇所 | 内容 |
|---|---|---|
| 1 | §5.5 L506 | `TodayTasksQuery` の「置き換える現行関数」に `scopeRows()` が無い。§2.5 L170 と §10.5 L1459 は列挙しているので、§5.5 だけが欠けている |
| 2 | §6.2 L619 | `recommendations` 行に `kind` の注記が無い（R3-MEDIUM-5 と同根。単独では LATER） |
| 3 | §24.4 L2771 | `workspaces.settings.seeded_source` が §6.2 の `workspaces.settings` の説明や §26 に一度も出てこない。`settings.capacity`（§18.1）/ `settings.maintenance`（§14.3）と違い、定義箇所が Migration Matrix のセルの中だけになっている |

**推奨** — §5.5 の1セルに `scopeRows()` を足し、§6.2 L619 に注記1つ、`settings` のキー一覧を §6.2 の `workspaces` 行か §26 のどこかに1箇所まとめる。**Phase 1 の完了条件には影響しない。**

---

## 3. 判定

# **B. 修正後に実装開始可能**

**Architecture の再設計は不要である。** 第1回・第2回で妥当と判断した骨格は v3 でも維持されており、BLOCKER は3回連続で0件。Workspace 分離、Identity、RLS、Automation の `predicates` 化、`external_event_key`、業務日付のタイムゾーン、スナップショット同期といった中核の判断は、本文・スキーマ表・ER・テスト章・ADR の各層で一貫している。事実誤認も残っていない（v3 が新たに書いた `9契約 / 34本 / 16本` はすべて実装と一致した）。

一方、**Phase 1 の完了条件（unresolved BLOCKER = 0 かつ unresolved HIGH = 0）はまだ満たしていない。** 残る HIGH 3件は、いずれも「v3 で入れた修正そのものが間違っている」型である。第2回までの HIGH が「反映が途中で止まっている」型だったのに対し、今回は**書かれた内容が誤っている**ため、実装者が設計書を信頼するほど失敗する。

- **R3-HIGH-1**（手順8 の実行順）— 設計書どおりに実装すると、進行中の案件の担当が毎月書き換わる。エラーも通知も出ない。§12.4 手順7 が自ら宣言した不変条件を手順8 が破っており、**1つの手順ブロックの中で矛盾している。**
- **R3-HIGH-2**（Q15 の推奨案）— 第2回が「Parity が 0件 対 0件 で通過する」と警告した破壊経路を、そのまま推奨案として採用している。設計書自身が §24.3 で「当月の全行を削除する」と正しく書いているのに、§28.1 がそれを推している。**§24.4 が参照する Step 2-1 の実測項目も存在しない。**
- **R3-HIGH-3**（undo Transition の UNIQUE 違反）— 第2回が具体的に示した衝突が解消されていないうえ、**成立しない根拠で「衝突しない」と断定する1文が加わった。** 実装者はこの1文で検証をやめる。

MEDIUM 7件のうち **R3-MEDIUM-2 と R3-MEDIUM-5 は性質が異なる。** 前者は「スキーマ表だけ直して §5 / §7 ER / §22 / §25 に波及していない」という、第2回 R2-MEDIUM-1 とまったく同じ形の再発である。後者は、その再発を機械的に止めるために作られたチェックが、目的の語も mermaid の破損も捕まえられないという問題である。**分割追記で長文を編集する運用を続ける以上、この2件を先に閉じないと第4回でも同じ表が並ぶ。**

### 実装着手の条件

**Phase 2 のスキーマ確定前に必須**
R3-HIGH-3（undo Transition の扱いを (A)/(B) から選ぶ）/ R3-MEDIUM-2 の `service_contracts.version`（§5.2 / §7.3 ER / §22.1 への波及と、インクリメント主体の規約）/ R3-MEDIUM-2 の `period_key` 部分ユニーク（§22.5 の `ON CONFLICT` 対象）/ R3-MEDIUM-3（§6.1 の規約文）。いずれも列・制約・文言で、後付けは既存データの掃除を伴う。

**Phase 2 着手前に済ませるべき（文言のみ・実装量ゼロ）**
R3-MEDIUM-1（ER の重複エッジと `kind` 欠落）/ R3-MEDIUM-5（純度チェックの語彙と mermaid 重複検出）。**最も安く、次回以降の再発を止める効果が最も大きい。** 純度チェックは PASS が偽の安心を与えている状態なので、優先度は見た目より高い。

**Phase 5 着手前に必須**
R3-HIGH-1（手順8 の位置）/ R3-MEDIUM-4（既定担当の役割分離）/ R3-MEDIUM-7（冪等キーの範囲）。3件とも §24.6 の Parity か Phase 5 の完了条件に直結する。

**移行フェーズ着手前に必須**
R3-HIGH-2（Q15 の推奨案の差し替え、§24.2 の3点、§24.6 の0件ガード、§14.3 との整合）。**オーナーへ Q15 を出す前に直す。** 現在の推奨案のまま承認されると、その判断が切り替え日に実行されてしまう。

**Phase 10 で対応**
R3-MEDIUM-6 のうち §25.3 の1件 / R3-LATER-1。

以上の修正はいずれも Architecture の変更を伴わない。**合計しても、設計書の文言修正と、制約定義1本（R3-HIGH-3 でどちらを選ぶか）に収まる。** これらを反映して unresolved BLOCKER = 0 / unresolved HIGH = 0 を満たせば、Phase 2 から実装に入って差し支えない。

---

## 付録 — 指摘ID一覧

| ID | 区分 | 章 | 要旨 | 由来 |
|---|---|---|---|---|
| R3-HIGH-1 | HIGH | §12.4 / §10.3 / §24.6 | 手順8（`assignEditors()`）が merge の後にあり、現行と実行順が逆。進行中の担当が書き換わる | R2-MEDIUM-5 の修正起因 |
| R3-HIGH-2 | HIGH | §28.1 / §24.2 / §24.4 / §14.3 | Q15 の推奨案が当月全行の削除経路。Parity が 0件対0件で通過する。§24.2 の3点未反映、§14.3 と矛盾 | R2-HIGH-2 残存 + 修正起因 |
| R3-HIGH-3 | HIGH | §11.3 / §6.2 | undo `要修正 → 確認中` が `unique(template_id, from, to)` に違反。設計書は成立しない根拠で否定 | R2-MEDIUM-6 未解消 |
| R3-MEDIUM-1 | MEDIUM | §7.1 ER | `workflow_runs \|\|--\|\| tasks` の重複エッジ。`workflow_transitions.kind` が ER に無い | R2-MEDIUM-1 の修正起因 |
| R3-MEDIUM-2 | MEDIUM | §5.1 / §5.2 / §7.3 / §11.6 / §22.1 / §22.5 / §25.2 | v3 のスキーマ変更3件が他章に波及していない。`version` のインクリメント主体が未定義 | R2-HIGH-3 / R2-MEDIUM-2 / R2-MEDIUM-3 残存 |
| R3-MEDIUM-3 | MEDIUM | §6.1 | UNIQUE 規約が同一節内で自己矛盾。`users` / `webhook_receipts` の例外が未記載 | R2-MEDIUM-4 残存 |
| R3-MEDIUM-4 | MEDIUM | §10.3 / §12.4 / §24.4 | 既定担当の役割が §10.3 と手順8 で矛盾。現行に無いタイブレークが追加された | R2-MEDIUM-5 の修正起因 |
| R3-MEDIUM-5 | MEDIUM | スクリプト / §6.2 | Core 純度チェックが `publish_date` を検出しない。mermaid 内の重複も素通り | R2-MEDIUM-9 残存 |
| R3-MEDIUM-6 | MEDIUM | §25.1-25.5 | 第2回が求めたテスト6件が未追加 | R2 各項の残存 |
| R3-MEDIUM-7 | MEDIUM | §12.4 / §22.3 | `contract_version_hash` が勤務予定・営業日を含まず、シフト変更後の再生成が無音で握り潰される | 新規 |
| R3-LATER-1 | LATER | §5.5 / §6.2 / §24.4 | 参照の不揃い3件（`scopeRows()` / `recommendations.kind` 注記 / `settings.seeded_source` の定義箇所） | 新規・軽微 |
