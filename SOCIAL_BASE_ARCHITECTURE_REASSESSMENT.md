# SOCIAL BASE Architecture Reassessment Report

- 作成日：2026-09-03
- 対象：`SOCIAL_BASE_SYSTEM_DESIGN.md`（v5 / 29章 / 3,499行）の前提の再評価
- きっかけ：オーナーから「PostgreSQL を SoT とする前提は自分が指定したものではない。本来の利用目的から逆算して再評価してほしい」との指示
- **この文書は判定と根拠だけを書く。`SOCIAL_BASE_SYSTEM_DESIGN.md` は変更していない。** 改訂するかはオーナーの確認後に判断する
- **コード実装は開始していない**

## 0. この再評価で実際に確認したもの

推測で書かないため、先に実物を見た。以下はすべて実地で確認した事実である。

| # | 確認したもの | 手段 | 分かったこと |
|---|---|---|---|
| 1 | 進捗管理スプレッドシート | Google Drive 連携で本文取得（771行・3タブ） | 5状態の列は存在しない。実体は「✓（着手済）」1個と手数え |
| 2 | Notion `DB_顧客マスター` | Notion 連携で取得 | 会社名・業種・セグメント・社内担当・**企画の素になる本文**（USP／ターゲット／訴求／不安） |
| 3 | Notion `DB_案件` | 全15行を SQL で取得 | **動画投稿本数・静止画投稿本数が構造化された数値プロパティとして実在** |
| 4 | Notion `DB_タスク` | Notion 連携で取得 | ステータス・期日・担当・発生元・関連案件を持つ**社員用タスクDBが既に稼働** |
| 5 | Artifact ランタイムの能力一覧 | 実行環境から取得 | 使える能力は `artifact` / `db` / `downloads` / `mcp` / `room` / `sample` の6種。**viewer の本人特定（`user`）は無い** |
| 6 | FIX済み UI の項目 | `ml-editing-board.html` を検索 | 5状態はある。**緊急度・依頼者・ネタストックは存在しない** |
| 7 | **Artifact ランタイムの型定義**（contract 0.2.39） | 実行環境が配る `claude.d.ts` / `db.d.ts` を直接読んだ | 「書き込みは last-writer-wins。トランザクションは無い」「再送された書き込みは2回適用され得る」が**明文で書かれている** |
| 8 | **Notion の更新API** | 公式リファレンス `PATCH /pages/{page_id}` | **条件付き更新の手段が無い**。version / ETag / If-Match / 期待 last_edited_time のいずれも受け付けず、更新は無条件に適用される |
| 9 | **Notion のレート制限** | 公式リファレンス | 「接続あたり平均 毎秒3リクエスト（バースト可）」。超過は HTTP 429 + `rate_limited`。1リクエスト上限 500KB・relation 100件 |
| 10 | **Notion の編集者記録** | 公式リファレンス | `last_edited_by` はシステム管理でありAPIから設定できない。API 経由の書き込みは**その連携（bot）として記録される** |
| 11 | **Cloudflare Access を Worker に適用する方式** | 公式ドキュメント（2026-08-14 に提供開始） | 認証済みリクエストには **`ctx.access`** が付き、**`ctx.access.getIdentity()`** で **email / name / groups** が取れる。**JWT の検証は不要。** ただし**認証のみで認可はアプリの責任**。Service Binding / RPC を越えて伝播しない |
| 12 | 各サービスの公式料金 | Cloudflare / Vercel / Neon / Supabase の料金ページ | §12 に内訳 |

1〜6 は 2026-09-03 の初回調査、7〜12 はオーナー指摘を受けた再確認で取得した。

**このうち 2・3・4・7・8・10 は、既存の設計書が書かれた時点では確認されていなかった事実**であり、判定を変える力を持つ。

> **表記の約束** — 以降、**［事実］**は公式仕様または実データで確認できたもの、**［評価］**はそれを踏まえた設計上の判断を指す。両者を混ぜない。

---

## 1. 私の SOCIAL BASE 利用目的の要約

### 1.1 オーナーが求めていること

1. **社内の同じチーム**（SNS運用の社員・動画編集スタッフ・社長）が、互いの進捗を簡単に・視覚的に確認できる
2. 進捗を進める入力とその管理の手間を、今より減らす
3. クライアントごとに違う運用ルール（月の投稿数・コンテンツ種類）を一元管理し、遅れと漏れをなくす
4. 投稿以外の業務（企画立て・台本作成・素材共有・タイトル入力と共有）も AI で工数を削減する
5. あくまで社内ツール。アプリとして配信はしない。他部署へ流用できれば良いが最優先ではない

### 1.2 優先順位（この再評価で従う順序）

1. SNS運用チームで本当に便利に使える
2. 今の手入力・管理工数を減らす
3. 進捗漏れ・遅れを防止する
4. 保守・運用を簡単にする
5. そのうえで他用途へ流用しやすくする

**この順序を、以降のすべての判定基準とする。** 5 のために 1〜4 を犠牲にしない。

### 1.3 いちばん重要な要件（言い換え）

> 「進捗を管理するために別途入力する」のではなく、
> 「普段の仕事を SOCIAL BASE 上で進めた結果として、進捗が自然に記録・共有される」

これは単なる要望ではなく **設計の合否判定に使える基準**である。ある機能について「人が新しく入力する場所が増えるか」を問い、増えるなら設計が間違っている。

**この基準を当てると、既存設計に1つ明確な誤りが見つかった。** §9 で述べる。

---

## 2. 現在運用の問題

### 2.1 進捗管理スプレッドシートの実態

実際の中身は3タブ構成だった。

| タブ | 列 | 実データ行数 |
|---|---|---|
| 作業リスト | 担当 / ✓ / クライアント名 / 動画内容（仮タイトル） / 投稿予定日 / 編集完了希望日 / 月/数 / 進捗 / 緊急度 / 依頼者 / 備考・参考動画 | 203 |
| ネタストック① | ✓ / クライアント名 / 動画内容 / 緊急度 / 依頼者 / 備考 | 84 |
| ネタストック② | ✓ / クライアント名 / 動画内容 / 緊急度 / 備考 | 114 |

**重要な発見が3つある。**

**(1) 5状態（未着手・編集中・確認中・投稿待ち・投稿済み）の列は存在しない。**
進捗の記録手段は `✓` の TRUE / FALSE ひとつだけで、実質「着手したか、していないか」の2値である。5状態は FIX済み UI が持ち込んだ**新しい表現**であって、シートから移すデータではない。

**(2) 数を人が数えている。** シート先頭に運用ルールがそのまま書かれていた。

> ①なつみ・ともみ：クライアントごとの月本数を記入。スタッフ：月本数の何本目か把握し、「進捗」に何本目か数字を入れる。

つまり **「今月4本のうち何本目か」を人間が毎回数えて入力している。** これは計算であって入力ではない。機械がやるべき仕事が人に降りている典型である。

**(3) 素材が「ファイル名の文字列」で共有されている。** 備考欄の実例は次の形をしている。

```
カバー画像：05_02.png  未編集動画：INTERVIEW #04  参考動画：05_01.mp4
```

スタッフはこの文字列を読み、Google Drive で名前を頼りに探す。**リンクではないので1クリックで開けない。**

加えて、ネタストック2タブのヘッダは「最終更新：2/10」で止まっていた。手入力の一覧は更新が止まる。

### 2.2 Notion の実態

Notion は「クライアント情報の置き場」ではなく、**すでに構造化された業務データベース**として動いていた。

| DB | 場所 | 主なプロパティ |
|---|---|---|
| `DB_顧客マスター` | メイクリード全社 / 顧客・案件 | 会社名・ステータス・セグメント・業種・先方担当者・社内担当・案件（relation）＋本文に USP／ターゲット像／刺さる訴求／応募者の不安 |
| `DB_案件` | メイクリード全社 / 顧客・案件 | 案件名・顧客（relation）・事業ライン・目的・ステータス・**動画投稿本数**・**静止画投稿本数**・契約金額・期間・担当・メモ・タスク（relation）・定例議事録・意思決定 |
| `DB_タスク` | メイクリード全社 / 全社運営 | タスク名・ステータス・期日・完了日時・担当・メモ・発生元・関連案件（relation） |

`DB_案件` 15行の実データを確認した結果：

- **運用中は12件。うち SNS運用（動画本数あり）は11件**、Lステップ運用が2件、ステータス未設定が1件
- 月あたりの合計は **動画54本・静止画8本**（11案件の合計）
- 1案件あたりの動画は 3〜6本、1件だけ16本
- **11案件すべてで `動画投稿本数` が埋まっている**

つまり「クライアントごとに月本数が違う」という一元管理したい情報は、**すでに機械が読める形で Notion にある。**

### 2.3 問題の構造

| 問題 | 実際に起きていること |
|---|---|
| 同じことを2箇所以上に書いている | 案件・担当は Notion に、進捗はスプレッドシートに、工程は UI に。突き合わせは人の目 |
| 計算を人がしている | 「何本目か」を数える。締切と遅れを目で見る |
| 素材が名前で共有されている | Drive を名前で探す。リンクで届かない |
| 進捗の粒度が粗い | ✓ 1個では「編集中」と「確認待ち」の区別がつかない |
| 気づく仕組みが人依存 | 遅れは金曜15分の会議でヒアリングして初めて分かる |
| 誰がやったか残らない | シートも Artifact も操作者の記録がない |

**これらはどれも「管理画面が足りない」問題ではない。「人が転記と計算と発見を担っている」問題である。** よって解決策も画面の追加ではなく、転記・計算・発見の自動化になる。

---

## 3. Target 業務フロー

### 3.1 進捗が「結果として記録される」形

工程を進める操作そのものが記録になる。**進捗を書く画面を別に開かない。**

```
編集スタッフ：自分の「今日」を開く
   → 担当の動画が締切順に並んでいる（並べる作業なし）
   → 素材の Drive リンクをその場で開く（探す作業なし）
   → 編集し終わったら「編集完了」を押す
        ↓（ここから先は人が何もしない）
   進捗が「確認中」になる
   社員の「今日」画面に「確認待ち」として出る
   同時に何本目かが数え直される（4本中3本）
        ↓
社員：確認して「承認」を押す
   → 「投稿待ち」になる
   → 投稿予定日が近づけば自動で通知が出る
```

### 3.2 主要業務の 現在 → 導入後

**評価基準：人が新しく入力する場所が増えていないか。**

| # | 業務 | 現在 | SOCIAL BASE 導入後 | 入力の増減 |
|---|---|---|---|---|
| 1 | 編集の完了報告 | シートの ✓ を手で立て、Slack でも伝える | 「編集完了」を1回押す。社員側に自動で出る | **減る**（転記とSlack連絡が消える） |
| 2 | 何本目かの管理 | 人が数えて「進捗」欄へ入力 | Notion の本数を読み、自動で「4本中3本」 | **消える** |
| 3 | 確認・承認 | シートを見に行って気づく | 「今日」画面に確認待ちが出る。押すだけ | **減る** |
| 4 | 素材の共有 | 備考にファイル名を文字で書く → 名前で探す | 動画に Drive を紐づけ、リンクから直接開く | **減る** |
| 5 | 新規クライアント追加 | HTML 5,226行の編集（実質不可）／シートに手で行追加 | **Notion `DB_案件` に1行足すだけ**（今と同じ作業）。翌日には画面に出る | **増えない**（今の作業のまま） |
| 6 | 月本数・種類の変更 | シートとHTMLの両方 | Notion の数値を直すだけ | **減る** |
| 7 | 締切と遅れの把握 | 目で見る＋金曜15分のヒアリング | 営業日ベースで自動判定。遅れ・停滞を画面と通知で提示 | **消える** |
| 8 | 翌月分の準備 | シートに手で行を作る | 契約本数から自動生成（当面は手動ボタン） | **減る** |
| 9 | 担当の割り振り | 金曜にヒアリングして人が計画 | シフトと本数から負荷を自動計算し偏りを提示。決定は人 | **減る** |
| 10 | 社員の業務タスク（企画・台本・指示出し） | Notion `DB_タスク` に入力 | **Notion のまま。SOCIAL BASE は読んで表示するだけ** | **増えない** |
| 11 | 台本・企画の作成 | 手作業 | Notion の顧客マスター（USP・ターゲット・訴求・不安）を材料に AI が下書き → 人が直す | **減る** |
| 12 | ネタストック | 別タブに手入力（更新が止まりがち。198行） | **Notion へ移す。** SOCIAL BASE の画面には当面追加しない（§17-C） | 場所が Notion に変わるだけ |

10 が重要である。**社員のタスクを SOCIAL BASE 側に作り直すと、Notion と SOCIAL BASE の二重入力になる。** それは §1.3 の基準に反するので、しない。

### 3.3 誰がどこを触るか（触る場所を増やさない）

| 人 | 触る場所 | 触らない場所 |
|---|---|---|
| 編集スタッフ | SOCIAL BASE のみ | Notion / Drive の管理 / スプレッドシート |
| 社員（なつみ・友美） | SOCIAL BASE ＋ Notion（今と同じ） | スプレッドシート（廃止） |
| 社長 | SOCIAL BASE を見るだけ | — |

**編集スタッフに Notion アカウントは要らない。** これは §7 の Artifact 判定と §12 の費用に直結する。

---

## 4. 推奨 Architecture（1案）

### 4.1 構成

選択肢を並べずに1案で示す。

| 層 | 採用するもの | 役割 |
|---|---|---|
| 画面 | **現行 `ml-editing-board.html` をそのまま静的配信** | 見た目・操作は変えない。同一オリジンから配る |
| 入口の認証 | **Cloudflare Access（Google Workspace ログイン）** | 会社アカウントでないと画面に到達できない。**認証を自前で作らない** |
| API・自動処理 | **Cloudflare Workers 1つ ＋ Cron Triggers** | 画面の要求に応える。誰も開いていない時刻にも自動処理を動かす |
| 内部データ | **Cloudflare D1**（SQL・トランザクションあり・30日巻き戻し） | 進捗・担当・締切・権限・操作履歴の正典 |
| マスタ | **Notion**（`DB_顧客マスター` / `DB_案件` / `DB_タスク`） | 顧客・契約・運用ルール・社員タスクの正典。**片方向で読むだけ** |
| ファイル | **Google Drive**（共有の運用アカウント） | 素材・完成物の正典。ID を内部に保持しリンクで開く |
| 通知 | Slack（後段） | 督促・申請の受付 |
| AI | Claude API を Worker から呼ぶ | 台本・企画の下書き、タイトル整形 |
| 環境 | local / staging / production | local はローカルの SQLite ファイル。**Docker も PostgreSQL の導入も不要** |
| CI | GitHub Actions | lint / 型 / テスト / 権限テスト / 画面差分 |

### 4.2 なぜこの構成か（優先順位 4「保守が少ない」に対する回答）

1. **認証を作らない。** Google ログインは Cloudflare Access が担う。OAuth の実装・セッション Cookie・招待フロー・トークン更新をこちら側に持たない。**設計書 Phase 3 の作業量が大きく減る**
2. **管理画面が1つで済む。** DB・定期実行・静的配信・秘密情報の保管が同じ場所にある。障害時に見る場所が分散しない
3. **Windows の開発環境で詰まらない。** ローカル開発は SQLite ファイル1つ。Docker も PostgreSQL のインストールも要らない（この環境には Python も無い）
4. **バックアップが標準で付く。** 30日ぶんの巻き戻しが無料。設計書 §26.4 の要求を追加費用なしで満たす
5. **staging を増やしても追加費用ゼロ**
6. **月額が最も安い（約 $6 / 月）。** §12 で比較する

### 4.3 採用しなかった構成と理由

| 構成 | 不採用の理由 |
|---|---|
| Vercel + Neon（PostgreSQL） | **認証を自前で実装する必要が残る**（Access に相当するものが無い）。Serverless から PostgreSQL へ繋ぐ際の接続方式（session / transaction pooling）の判断と、それに伴う RLS 運用規約が付いてくる。料金は §12.5 に公表価格のみ記載（同条件比較はしない） |
| Supabase | ［事実］時点復元が追加オプション（7日 $100 / 月）。［評価］バックアップの要求（設計書 §26.4）を満たすのに追加課金が要る点が、この規模には見合わない |
| Google Cloud（Cloud Run + Cloud SQL） | Google 連携との相性は良いが、DB が常時課金で環境2面ぶん必要。設定項目が最も多く、保守（優先順位4）で不利 |
| 常時稼働サーバー1台 | 一番安く見えるが、OS更新・証明書・バックアップ・監視をすべて自前で持つ。保守が最も重い |
| Notion だけ（内部DBなし） | §6 で判定する。**不可** |
| Artifact のまま | §7 で判定する。**不可** |

### 4.4 UI への影響

**なし。** `ml-editing-board.html` の見た目・操作・レスポンシブ挙動を変えない（設計書 §3.4 の Visual / Interaction Contract を維持）。データの取り方が `STATE` の直読みから API 呼び出しに変わるのみで、判定は画素差0の画面差分テストで行う。

§17-C の3項目（緊急度・依頼者・ネタストック）についても、**画面に項目を足さない方針で解ける**（緊急度は自動判定、依頼者は自動記録、ネタストックは Notion へ移す）。したがって**この構成では UI 変更は発生しない。** 将来それらを画面に出したくなった場合のみ、UI 変更の相談として別に扱う。


### 4.5 ログインユーザーの識別と Audit への自動記録

**共有ログインは禁止する**（オーナー承認済み）。社員・業務委託とも1人1アカウントとする。

#### ［事実］Cloudflare Access を Worker に直接適用する

［事実］2026-08-14 に、**Access を Worker 自体へ適用できる**ようになった。Access のポリシーはホスト名ではなく Worker に結びつき、**リクエストがコードへ届く前に Cloudflare のエッジで評価される。**

| 項目 | 内容 |
|---|---|
| 認証済みリクエストの目印 | **`ctx.access` が存在する** |
| 本人情報の取得 | **`ctx.access.getIdentity()`**。返るのは **email / name / groups** |
| アプリの識別 | `ctx.access.aud`（Access アプリの audience タグ） |
| JWT の検証 | **不要。** 公開鍵の取得・`kid` の突き合わせ・RS256 検証・`iss` / `aud` の確認をアプリで実装しない |
| 役割 | **認証（authentication）だけを行う。認可（authorization）はアプリの責任** |
| ローカル開発 | `wrangler.jsonc` の `dev` ブロックに Access アプリの `aud` と擬似 identity を書くと、`wrangler dev` でも `ctx.access` が届く |
| 制約 | **Access のコンテキストは Service Binding / RPC を越えて伝播しない。** 呼び出し先の Worker は呼び出し元の Access 情報を受け取らない |

［評価］**Access JWT の検証ロジックを SOCIAL BASE 側で独自実装しない。** 暗号処理を自前で持たないほうが、実装量も間違える余地も小さい。上の制約から、**Worker は1つに保つ**（§4.1 のとおり）。

#### ［設計］原則フロー

```
Cloudflare Access で認証（エッジで完了。未認証はコードに到達しない）
  ↓
Worker で認証済み Identity を取得（ctx.access.getIdentity()）
  ↓
D1 の Membership を特定（内部の不変IDで人を確定する）
  ↓
D1 の Permission / Capability を確認（fail closed）
  ↓
業務操作
  ↓
D1 へ更新（版番号で同時更新を検出）
  ↓
Audit へ 操作者・日時・変更前後 を記録
```

**この7段のうち、認証だけが Cloudflare の担当で、残り6段は SOCIAL BASE の責任である。** `ctx.access` があることは「社内の誰かである」ことしか意味しない。**何をしてよいかは D1 の権限データで判定する**（設計書 §8.4 の判定順序をそのまま維持）。

- `ctx.access` が無いリクエストは **401**（fail closed）
- **定期実行（cron）には `ctx.access` が無い。** その経路は操作者を `system` として記録し、**人向けの権限を要する操作を実行させない**
- **操作者を UI から受け取らない。** 画面が送ってきた名前は信用せず、必ず Identity 由来の本人を使う

#### ［設計］Member の同定は D1 の不変な内部IDで行う

［事実］`getIdentity()` が返すと確認できたのは **email / name / groups** であり、**IdP 側の不変なユーザーIDが含まれるかは公式ドキュメントで確認できていない。**

［設計］したがって **SOCIAL BASE の人の同定は、外部から来る値ではなく D1 側の不変な内部ID（`members.id`）で行う。**

| 規約 | 内容 |
|---|---|
| 主キー | `members.id`（発行後に変えない内部ID）。**email を主キーにしない** |
| email の位置 | 初回ログイン時に内部IDへ結びつける照合用の属性。以後は表示と照合のみ |
| 参照の持ち方 | `task_assignments` / `audit_logs` など**人を指す全ての列は `members.id` を持つ**。email を保存して人を指さない |
| 退職・契約終了 | Access からアカウントを外す。D1 の member 行は残し、状態を無効にする（履歴を壊さない） |
| 無効化済みの email での新規ログイン | **自動で既存 member に結びつけない。** 新しい member を自動作成もせず、「要確認」として止める（fail closed） |
| 実装時の確認 | `getIdentity()` に IdP 側の不変IDが含まれるなら、それも保存して照合の強度を上げる。**含まれるかは実測で確認する** |

［運用ルール（オーナー承認済み）］**退職者等のメールアドレスを別人へ安易に再利用しない。** これは SOCIAL BASE のためだけでなく、他サービスを含めた本人取り違えの防止のためである。

#### 共有ログインを禁止するための扱い

- Access 側でアカウントを1人1つにする。共有アカウントを作らない
- 業務委託の方も本人の個別アカウントで入る
- 退職・契約終了時は Access からそのアカウントを外す。**それだけで SOCIAL BASE も閉じる**
- ［評価］「2人が同じアカウントを使っている」ことを技術的に完全に検出はできない。ただし全書き込みに本人が残るため、共有すると**その人の名前で他人の操作が記録される**。運用で禁止し、本人が自分の操作履歴を見られるようにして抑止する

#### ［設計］Audit へ自動記録するもの

書き込み系 API はすべて Audit に1行残す。記録は後から書き換えない。

| 操作 | Audit に自動で残るもの |
|---|---|
| 編集完了にした | 誰が（`members.id`）/ いつ / どの動画 / 前の工程 → 後の工程 / `request_id` |
| 確認（承認）した | 誰が / いつ / どの動画 / 前の工程 → 後の工程 |
| 担当を変更した | 誰が / いつ / どの動画 / 前の編集担当 → 後の編集担当 |
| 投稿予定日・締切を変えた | 誰が / いつ / 変更前 → 変更後 |
| タイトル・URL・メモ・修正指示を書いた | 誰が / いつ / 変更前 → 変更後 |
| 動画を追加した | 誰が / いつ / どのクライアント |
| 翌月分を生成した | 誰が / いつ / 生成件数 |
| Notion 同期・自動通知（cron 経由） | **`system` として記録し、人の操作と区別する** |

---

## 5. データごとの Source of Truth

### 5.1 SoT 表（最終）

原則は2つ。**人が保守するものは人が使っている場所に置く。機械が保守するものだけを内部に持つ。**

| データ | Source of Truth | 人が更新する場所 | SOCIAL BASE での扱い |
|---|---|---|---|
| Client（顧客） | **Notion `DB_顧客マスター`** | Notion のみ | 読み取り同期。画面に表示 |
| Contract / 運用ルール（月本数・種類・事業ライン） | **Notion `DB_案件`** | Notion のみ | 読み取り同期。翌月生成と本数管理の入力になる |
| 企画の素（USP・ターゲット・訴求・不安） | **Notion `DB_顧客マスター` の本文** | Notion のみ | 読み取り。AI の材料にする |
| Google Drive Asset 本体（素材・完成物） | **Google Drive** | Drive のみ | ファイルは Drive のまま。ID とリンクだけ内部に持つ（機械が書く） |
| 動画・投稿の制作進捗（5状態） | **D1** | SOCIAL BASE のみ | 正典 |
| Content（動画・投稿1本） | **D1** | SOCIAL BASE のみ | 正典 |
| 制作工程上の Task | **D1** | SOCIAL BASE のみ | 正典。Content と 1:1 |
| Notion で管理している一般社員 Task（企画・台本・指示出し） | **Notion `DB_タスク`** | Notion のみ | **原則表示のみ。SOCIAL BASE から書かない** |
| Assignment（担当編集者） | **D1** | SOCIAL BASE のみ | 正典 |
| 業務 Schedule（投稿予定日・編集完了希望日・締切） | **D1** | SOCIAL BASE のみ | 正典 |
| Google Calendar | **正典にしない** | Calendar（人の予定として） | **将来連携する場合は D1 から同期する外部表示先**。SOCIAL BASE の業務判断の根拠にはしない |
| 勤務予定（シフト） | **D1** | SOCIAL BASE（本人申請＋社員承認） | 正典。Slack から受ける（後段） |
| Membership（誰がいるか） | **Access の認証情報**（＋D1 の表示名・所属） | Google Workspace / Access | 認証は Access。D1 は表示名と Role だけを持つ |
| Permission（誰が何をできるか） | **D1**（Access の本人情報と組み合わせる） | SOCIAL BASE | 正典。**判定はサーバー側** |
| Workflow（5状態と遷移） | **D1（固定値）** | 更新しない | v1 は固定。画面から編集できるようにしない |
| Automation 設定（9ルール） | **D1** | SOCIAL BASE | 正典 |
| Audit / 操作履歴 | **D1** | 更新しない（機械が追記） | 正典。書き換えない |
| idempotency / concurrency / ジョブ / 同期状態 | **D1** | 人は触らない | 正典。システム内部データ |
| 営業日・祝日 | **D1** | SOCIAL BASE（年1回） | 正典 |
| **ネタストック（198行）** | **Notion** | Notion のみ | **移行して保存する。FIX済み UI に画面追加はしない** |
| 緊急度 | **持たない（導出値）** | どこにも入力欄を作らない | 締切・現在工程・遅延状態から**SOCIAL BASE が自動判定** |
| 依頼者 | **D1（自動記録）** | どこにも入力欄を作らない | 作成者・操作ユーザーから**自動で記録**（§4.5） |

### 5.2 Notion を正典にできると判断した根拠

［事実］「Notion に入っているはず」ではなく、実データで確認した。

- `動画投稿本数` `静止画投稿本数` は数値プロパティであり、SNS運用11案件すべてで埋まっている
- `顧客` は relation で `DB_顧客マスター` に繋がっている（名前の文字列一致に頼らない）
- `事業ライン` が「新卒採用_インスタグラム」「中途採用_インスタグラム・TikTok」「集客_インスタグラム・TikTok」「採用_Lステップ構築」に分かれており、**SNS運用の対象だけを機械的に絞れる**
- 社員はすでにここを更新している（最終更新が直近日付）

［評価］**設計書 §2.6 が「新規クライアント1社の追加に5,226行のHTML編集が要る」と書いた問題は、新しいクライアント管理画面を作ることではなく Notion を読むことで解ける。**

### 5.3 同期の規則

片方向（Notion → SOCIAL BASE）。1日1回の定期同期＋画面操作時の必要分読み取り。**SOCIAL BASE から Notion のマスタへは書き戻さない。**

［事実］実データに実在した不整合から、次の規則が必要だと分かった。想定ではない。

| # | 規則 | 実際に見つかったもの |
|---|---|---|
| 1 | `事業ライン` が SNS運用系のものだけを対象にする | `採用_Lステップ構築` が2件混ざっており、動画本数が空 |
| 2 | 記入例・テンプレート行を除外する | 「サンプル案件（記入例）」がステータス「運用中」で入っている |
| 3 | `ステータス` が空の行は同期せず「要確認」として通知する | ステータスも事業ラインも空の行が1件ある。黙って無視すると案件が抜ける |
| 4 | `静止画投稿本数` の空欄は 0 として扱う | 0 と空欄が混在している |
| 5 | **読むプロパティを列挙し、それ以外は取得しない** | 同じ DB に `契約金額` がある |
| 6 | 必須プロパティが欠けたら同期を止めて通知する（fail closed） | Notion 側でプロパティ名を変えると連携が黙って壊れる |

規則5について。［事実］契約金額は動画本数と同じ `DB_案件` にある。［評価］「案件情報を読む」を素朴に実装すると金額まで運んでしまう。**SOCIAL BASE が必要としない個人情報・契約金額は、最初からシステムへ持ち込まない。** 取得プロパティを明示的に列挙し、`契約金額` と `先方担当者` は取得対象に入れない。

### 5.4 ［確認］Notion と D1 で人間が二重に更新する箇所は無いか

**「同じ情報を両方で人間が更新する」構造が残っていないかを1件ずつ確認した。結果：残っていない。**

| 一見重なって見えるもの | 実際は | 二重更新か |
|---|---|---|
| Client / Contract | Notion が唯一の入力先。D1 側は読み取りキャッシュで、**人は触らない・画面から編集させない** | ならない |
| 一般社員 Task | Notion が唯一の入力先。SOCIAL BASE は表示のみで書かない | ならない |
| ネタストック | Notion が唯一の入力先。SOCIAL BASE は当面参照しない | ならない |
| **担当** | Notion `DB_案件.担当` は「その案件を持つ社員」。D1 の Assignment は「この動画を編集するスタッフ」。**別の概念** | ならない（§5.5 で名称を分ける） |
| **ステータス** | Notion `DB_案件.ステータス` は案件単位（運用中／契約中）。D1 の5状態は動画1本単位。**別の粒度** | ならない |
| 期日 | Notion `DB_タスク.期日` は社員タスクの期日。D1 の締切は動画1本の締切。**別の対象** | ならない |
| 投稿予定日 | Notion 側に該当プロパティが無い（`期間` は契約期間） | ならない |
| Drive のファイル | 実体は Drive のみ。D1 は ID を持つが**機械が書く**ので人の二重入力にならない | ならない |

［評価］唯一注意が要るのは**「担当」という同じ言葉が違う意味で両側にあること**である。画面と設計書で呼び分ける（Notion 側＝**案件担当**、D1 側＝**編集担当**）。

### 5.5 やらないこと

- **双方向同期をしない。** 同じ項目を両側から書けるようにすると、どちらが正しいか決められなくなる
- **進捗を Notion に持たせない**（v1）。理由は §6。ただし「社員・社長が Notion でも進捗を見たい」場合は、**D1 を正典にしたまま Notion へ片方向で写す**ことは後から足せる
- **`DB_タスク` に SOCIAL BASE から書かない。** 読んで表示するだけにする
- **Google Calendar を業務判断の正典にしない。** 書き出すかどうかは設計書 Q6 のまま未決（推奨は v1 では書き出さない）
- **緊急度・依頼者の入力欄を新設しない。** 手入力項目を増やすことは §1.3 の基準に反する

---
## 6. PostgreSQL 必要 / 不要の判定

### 6.1 判定

| 問い | 判定 |
|---|---|
| Notion ＋ Drive ＋ 最小限の Backend だけで成立するか | **成立しない。** トランザクショナルな内部データの置き場が必要 |
| その置き場は PostgreSQL でなければならないか | **いいえ。** 本件の規模では SQLite 系（Cloudflare D1）で足りる |
| 結論 | **「トランザクショナルなデータベースは必要。ただし PostgreSQL である必要はない」** |

**「既に PostgreSQL 前提で設計したから」という理由は採用していない。** 設計書 §3.2 は SoT を PostgreSQL と決めていたが、これを差し替える。

### 6.2 ［事実］Notion の公式仕様で確認できたこと

**ここに書くのは Notion 公式リファレンスで確認できた内容だけである。設計上の評価は §6.3 に分ける。**

| # | 事実 | 出典 |
|---|---|---|
| F1 | **`PATCH /pages/{page_id}` は条件付き更新の手段を持たない。** version / ETag / If-Match / 期待 `last_edited_time` のいずれのパラメータもヘッダも無く、更新は無条件に適用される。競合検出の仕組みは提供されない | Update page リファレンス |
| F2 | **`last_edited_by` はシステム管理でありAPIから設定できない。** 連携（integration）のトークンで書き込むと、その連携の bot として記録される | Update page / User リファレンス |
| F3 | **レート制限は接続あたり平均 毎秒3リクエスト**（バーストは許容）。超過すると HTTP 429 と `rate_limited`。ワークスペース単位の上限は全接続で共有されプランに応じて増減する | Request limits |
| F4 | 1リクエストの上限は 500KB、relation は1プロパティ100件、block 配列は100要素、rich text は2000文字 | Request limits |
| F5 | トランザクション（複数ページの更新をまとめて成功／失敗させる仕組み）は公開APIに存在しない | API リファレンス全体に該当機能なし |

F1 と F5 から、**「まだ無ければ作る」を不可分に行うこと（原子的な upsert）は、Notion API 単体では構成できない**ことが導かれる。これは推測ではなく F1・F5 の帰結である。

### 6.3 ［評価］事実から導かれる設計判断

**同じ事実でも、対象データによって評価が変わる。それが §5.1 で役割を分けた理由である。**

| # | 事実 | 人間向けマスタ（Client / Contract / 運用ルール）への影響 | トランザクションデータ（進捗・承認・自動処理・Audit）への影響 |
|---|---|---|---|
| E1 | F1（条件付き更新なし） | **許容できる。** 更新するのは社員1〜2名で、Notion の画面が人同士の同時編集を扱う。API から機械が書き換えないので競合しない | **許容できない。** 社員が担当を変更している最中にスタッフが「編集完了」を押すと、**後の書き込みが前を上書きし、画面にもエラーが出ない**。進捗が黙って巻き戻る事故は、このシステムがなくそうとしている「進捗漏れ」そのもの |
| E2 | F2（bot として記録される） | **許容できる。** 誰が契約本数を直したかは Notion の履歴に人として残る（人が Notion の画面で直すため） | **許容できない。** SOCIAL BASE が代行して書くため、履歴は全部「SOCIAL BASE」になる。「誰が承認したか」に答えられず、設計書 §19 の要件が成立しない |
| E3 | F1・F5（原子的 upsert ができない） | **影響しない。** 読むだけ | **compensating が必要になる。** 二重督促や翌月分の二重生成を防ぐには、別にトランザクショナルな台帳（冪等キー）を持つしかない。**［重要］「Notion では防げない」ではなく「Notion 単体では防げず、別の台帳が必要になる」が正確** |
| E4 | F3（毎秒3リクエスト） | **影響しない。** 1日1回の同期で足りる | **画面表示に耐えない。** 「今日」画面は数十本の動画・契約・担当・シフトをまとめて見る。毎回問い合わせれば待ち時間が出る。**結局ローカルの読み取り用の置き場が必要になる** |
| E5 | Notion にスキーマ契約が無い（プロパティ名を変えると連携が黙って壊れる） | **許容できる。** 読むプロパティを列挙し、欠けたら停止して通知する（§5.3 規則6）ことで検知できる | **許容しにくい。** 書き込みの主経路が壊れると業務が止まる |

#### この評価から出る決定的な論点

E3 と E4 は「Notion では不可能」ではない。**どちらも回避策がある。** ただし回避策はどちらも「**トランザクショナルな内部の置き場を別に持つこと**」に帰着する。

- 二重実行を防ぐには冪等キーの台帳が必要 → 内部DBが必要
- 画面を速く出すには読み取り用の置き場が必要 → 内部DBが必要
- 誰が承認したかを残すには操作者の記録が必要 → 内部DBが必要

［評価］**内部DBを持つことが避けられないなら、進捗そのものを Notion にも置く理由が無くなる。** 置けば、正典が2つになり、同期の向きと競合の扱いを決めなければならず、E1 の「黙って上書き」の危険だけが残る。**得るものがなく、リスクと複雑さだけが増える。**

### 6.4 結論の表現

オーナーからのご提案どおりの表現が**妥当である。** 採用する。

> **Notion は Client / Contract / 運用ルール等の人間向けマスタには適しているが、制作進捗・承認・自動処理・Audit などのトランザクションデータの正典には採用しない。**

前回の表現（「Notion では代替できない」「二重督促を防げない」）は**強すぎた。** 正確には次のとおりである。

| 前回の表現 | 正確な表現 |
|---|---|
| 同時更新すると黙って消える | ［事実］条件付き更新の手段が無く競合検出が提供されない（F1）。［評価］複数の人と機械が書く進捗では、上書きが起きても検知できない |
| 誰が承認したか残らない | ［事実］API 経由の書き込みは連携（bot）として記録される（F2）。［評価］操作者を残すには別に記録が必要 |
| 二重督促を防げない | ［事実］原子的 upsert を構成できない（F1・F5）。［評価］**Notion 単体では防げない。防ぐには別のトランザクショナルな台帳が必要** |
| 画面が遅くなる | ［事実］接続あたり平均 毎秒3リクエスト（F3）。［評価］画面表示は内部の置き場から返す必要がある |

**Notion が劣った道具だという主張ではない。** 人が読み書きする資料とマスタには適しており、実際に §5.1 でその役割の正典に据えている。

### 6.5 PostgreSQL でなくてよい理由

設計書が PostgreSQL に求めていたものを一つずつ確認した。

| PostgreSQL に期待していたこと | 本件で必要か | D1（SQLite）で足りるか |
|---|---|---|
| トランザクション（途中で失敗したら全部戻す） | **必要** | 足りる |
| 同時更新の検出（版番号での衝突検出） | **必要** | 足りる |
| 重複を許さない制約（冪等性・二重生成防止） | **必要** | 足りる |
| 自動バックアップと時点復元 | **必要** | 足りる（30日巻き戻しが標準・追加費用なし） |
| SQL とスキーマ移行 | **必要** | 足りる |
| RLS（データベース自身が行単位で遮断する機能） | **不要**（§11-1 で理由） | 無い |
| jsonb・特殊な索引・大規模データ | **不要**（年間 750件程度・数MB） | 無い／不要 |
| 多数の同時接続 | **不要**（25人程度） | 足りる |

**必要な5つはすべて満たす。不要な3つのために月額と保守を増やす理由がない。**

### 6.6 将来 PostgreSQL へ移る場合

- SQL とドメイン層を分離し、SQLite 固有の書き方を避けておけば移行は可能（**多少大変**）
- 移る動機になるのは「他部署へ本格展開して Workspace が増える」「データ量が数GBを超える」のどちらか
- どちらも今は起きていないので、**起きたときに移す**

---
## 7. Artifact 継続利用の判定

### 7.1 判定

| 用途 | 判定 |
|---|---|
| 最終的な本番 SOCIAL BASE として使う | **不可** |
| カットオーバーまでの現行運用として使い続ける | **可**（今そうしている） |
| UI の見た目・操作の正典として使う | **可**（FIX済みのものを維持） |

### 7.2 混同しやすい2つのものを分ける

**前回のレポートはこの2つを十分に区別していなかった。分けて記述する。**

| | **(A) Claude の Connector / MCP 機能** | **(B) 公開 Artifact 内で動くアプリの Runtime 能力** |
|---|---|---|
| 誰が使うか | **人が Claude と会話するとき**に Claude が使う | **公開されたページの JavaScript** が使う |
| 何に繋がるか | Notion / Google Drive / Google Calendar / Slack など、その人が接続したコネクタ | `claude.use(名前)` で得られる能力だけ |
| 資格情報 | その人（会話している本人）のもの | 能力ごとに異なる（`mcp` は**閲覧者本人**のコネクタを借りる） |
| 本件での位置 | **今回の調査はこれで行った。**Notion の実データもこれで読んだ | **本番運用の可否を決めるのはこちら** |

［事実］この環境で Artifact に宣言できる能力は **`artifact` / `db` / `downloads` / `mcp` / `room` / `sample`**（＋ `artifact` の旧名 `self`）の6種であり、**一覧に無い名前は利用できない。**

**重要な区別** — (B) の `mcp` は「アプリが外部APIを持つ」ことではない。**閲覧している人の Claude コネクタを、その人の資格と同意で借りる**仕組みである。したがって「アプリのサーバーが Notion に繋がる」のとは性質が違い、**閲覧者ごとにコネクタの接続状況と権限が変わる。**

### 7.3 ［事実］4つの軸での判定

**Artifact 単体で本番 SOCIAL BASE を成立させられるか。** 根拠は実行環境が配る型定義（contract 0.2.39 の `claude.d.ts` / `db.d.ts`）と能力一覧である。

| # | 軸 | 判定 | 根拠（事実） |
|---|---|---|---|
| 1 | **外部第三者APIへの直接通信** | **不可**（間接のみ条件付きで可） | ページからの fetch / XHR / WebSocket は許可リスト外の全ホストへ遮断される（許可されるのは限定CDNからのスクリプトとフォントの読み込みのみ）。`mcp` を使えば通信できるが、それは**閲覧者本人のコネクタを借りる間接経路**であり、アプリ自身が第三者APIへ直接繋ぐことはできない |
| 2 | **永続ストレージ** | **可（ただし制約付き）** | `db` はサーバー側に保存され閲覧者間で共有される JSON ドキュメントストア。`artifact` はページ自身が新しい版を保存できる。**ただし型定義に「書き込みは last-writer-wins。トランザクションは無い」「再送された書き込みは2回適用され得る」と明文で書かれている。** 協調ロック（`acquire`）はあるが、型定義に「他の閲覧者は直接書き込める。ロックは `acquire` を使う呼び出し側同士しか調整しない」と明記された助言的なものである |
| 3 | **認証された利用者本人の識別** | **不可** | 本人識別は `user` 能力に属し、**この環境で宣言できる能力一覧に `user` は含まれない。** `db` の閲覧者ごとの私有領域（`data/users/<id>`）も `user` を前提とする。`room` の presence は「誰でも設定できるので権限の根拠にならない」と型定義に明記されている |
| 4 | **定期実行** | **不可** | 能力一覧に定期実行に相当するものがない。`db` の変更通知（`onSnapshot`）はページが開いている間の反映のみ |

補足：**権限（アクセス制御）は「無い」ではなく「粗い」。** ［事実］`db` はパスごとにルールを宣言でき、割り当てられる区分は閲覧者の共有レベル（`interact`＝見られる／`admin`＝編集できる／所有者）の3つ、ルールは最大64件。

### 7.4 ［評価］本番にできない理由

**決定的なのは軸3と軸4である。** 軸1と軸2は回避策があるが、3と4には無い。

**(1) 定期実行が無い（軸4）— これが最も重い**

このシステムの中核は「人が気づく前に機械が気づくこと」である。ところが Artifact ではページが開かれている間しか処理が動かない。

- 投稿予定日の14日前・7日前・3日前の督促
- 毎月12日の分析サイクル
- 締切超過の検知
- 翌月分の生成
- Notion からの同期

**すべてが「誰かが画面を開いていたら動く」になる。** 優先順位3（進捗漏れ・遅れの防止）が成立しない。

**(2) 本人識別が無い（軸3）— ここから3つの要件が連鎖して崩れる**

本人が分からないと、次が順に成立しなくなる。

```
本人が識別できない
  → 「誰が承認したか」を記録できない（Audit が成立しない）
  → 「自分の担当だけ書ける」を判定できない（行単位の権限が作れない）
  → 権限表（35項目）を守れない。画面で名前を選ぶ現状の方式に戻る
```

［事実］軸3のとおり `user` 能力は使えない。［評価］共有レベル3段階（軸3の補足）で表現できるのは「見るだけ／編集できる」までであり、**「本人かどうか」を判定できないため §9.2 の権限表は原理的に表現できない。**

**(3) 同時更新で片方が消える（軸2の制約）**

［事実］型定義に last-writer-wins・トランザクション無し・再送で2回適用され得ると明記されている。［評価］§6.3 E1 と同じ事故が起きる。`acquire` は助言的で、使わない書き込みを止められない。

**(4) 外部連携が閲覧者依存になる（軸1）**

［評価］`mcp` は閲覧者本人のコネクタを借りるため、**編集スタッフ全員に claude.ai アカウントと各コネクタの接続が必要になる。** さらに Notion を読ませるには Notion 側の権限が要る。［事実］`契約金額` は動画本数と同じ `DB_案件` にある。［評価］スタッフに Notion の案件データへの到達手段を与えると金額まで見える経路ができ、§3.3 で決めた「スタッフに Notion アカウントは要らない」が崩れる。

### 7.5 前回の記述の訂正

| 訂正対象 | 前回の記述 | 正確な記述 |
|---|---|---|
| 設計書 §2.6-4 | 「外部サービスと繋がらない。CSP が遮断する。設定で外せる制約ではない」 | **直接通信は今も遮断される**（この部分は正しい）。ただし `mcp` による**間接経路が存在する**ため「繋がらない」は不正確。正しくは「アプリ自身は第三者APIへ直接繋げず、繋ぐ場合は閲覧者本人のコネクタを借りる形になる」 |
| 前回レポート §7.3 | 「権限をサーバー側で守れない」 | **守れる範囲がある。** パス単位に共有レベル3段を割り当てるところまではサーバー側で効く。守れないのは「本人かどうか」に依存する権限（本人識別が無いため） |
| 前回レポート §7.3 | 「Artifact の共有データに設定できる権限は3段階しかない」 | パス単位のルール（最大64件）× 共有レベル3段。**粒度はパスにもある** |

**訂正しても結論は変わらない。** 軸3（本人識別）と軸4（定期実行）が本番の必須要件であり、どちらも回避策が無い。

### 7.6 Artifact の今後の役目

不可という判定は「捨てる」ではない。

| 役目 | 内容 |
|---|---|
| カットオーバーまでの本番 | 現在の運用を続ける。触らない |
| UI の正典 | FIX済みの見た目・操作を保持する基準。新環境はこれと画素差0を目指す |
| 移行後 | 2週間ほど読み取り専用で残し、その後停止（設計書 §24.2 のまま） |

---
## 8. 既存 SYSTEM DESIGN への影響

### 8.1 影響する章の一覧

29章を A（そのまま使える）/ B（修正が必要）/ C（過剰・後回し）に分類した結果は **A = 12章、B = 16章、C = 1章** である。

> **［訂正］** 前回レポートは「影響を受けるのは15章、残る14章はそのまま使える」と書いていたが、内訳を数え直すと **B は 16章**（§16 Calendar Integration を A から B に変更したため）、**A は 12章**、**C は 1章**である。§15.1 で「13章」と書いていた箇所も誤りで、正しくは **16章**。

| 章 | 分類 | 影響 |
|---|---|---|
| §1 Design Principles | A | そのまま。ただし優先順位（§1.2）を明記して追加する |
| §2 Current Architecture | B（軽） | §2.6-4「外部と繋がらない」を訂正。スプレッドシートと Notion の実態を追記 |
| §3 Target Architecture | **B（重）** | SoT を「内部DB ＋ Notion」に。§3.1 の図と §3.2 の決定表を差し替え |
| §4 Module Boundaries | A | Core / Module の分離はそのまま。Notion Adapter を Integration 側に置く |
| §5 Domain Model | B（軽） | Client / ServiceContract を「Notion 由来のキャッシュ」に変更。他はそのまま |
| §6 Database Schema | **B（重）** | PostgreSQL 固有の記述を SQLite へ。テーブル数を削減。§6.5 の RLS を削除 |
| §7 ER Diagram | B | §6 に追随 |
| §8 Workspace / Membership | B（軽） | §8.2 の認証実装を Cloudflare Access に置換。同定の規約（`sub`）は残す |
| §9 Permission Matrix | A | 権限表と現行判定の対応表（§9.3）はそのまま使う。Permission Test の入力になる |
| §10 Task / Assignment | A | そのまま |
| §11 Workflow | B（軽） | 5状態を固定値に。画面から編集する機能（§11.5 の7規則）は削除 |
| §12 Schedule / Recurrence | A | 営業日計算・生成ロジックはそのまま |
| §13 Automation | A | 9ルールはそのまま。実行基盤が cron トリガーになるだけ |
| §14 Notification | A | そのまま |
| §15 Drive Integration | B（軽） | 接続主体を共有の運用アカウントに確定（Q14 を先に決める） |
| §16 Calendar Integration | **B（軽）** | **正典にしない。** D1 を正典とし、連携する場合は D1 から Calendar へ書き出す外部表示先として扱う。取り込みを行う場合も業務判断の根拠にはしない |
| §17 Slack Integration | A | そのまま（後段） |
| §18 Recommendation / Capacity | C（後回し） | 負荷計算は残す。推薦機能は Phase を後ろへ |
| §19 Audit | A | そのまま。**内部DBが必要な最大の理由** |
| §20 Security | A | そのまま |
| §21 API | B（軽） | Notion 同期の経路を追加 |
| §22 Concurrency / Idempotency | A | **そのまま。むしろ本再評価の結論を支える中核** |
| §23 Background Jobs | A | そのまま。cron トリガーに載る |
| §24 Migration | **B（重）** | 移行元が「Artifact の STATE」＋「スプレッドシート」＋「Notion」の3つになる |
| §25 Testing Strategy | B（軽） | RLS テストを削除。Notion 同期の契約テストを追加 |
| §26 Deployment / Operations | **B（重）** | Cloudflare 構成に差し替え。接続方式の決定事項が不要になる |
| §27 Implementation Phases | **B（重）** | Phase 2・3 を縮小、Phase 5 をほぼ削除、Phase 7 に Notion 同期を追加 |
| §28 Risks / Open Questions | B | Q1・Q9・Q11・Q14 の内容が変わる。新しい判断事項が3件増える（§17） |
| §29 Architecture Decision Log | B | ADR-019（RLS を最初から入れる）を撤回。新 ADR を3件追加 |

### 8.2 PostgreSQL 前提を外して消える決定事項

| 消えるもの | 消える理由 |
|---|---|
| 接続プーリングのモード決定（§26.1） | 接続プーラを使わないので論点そのものが消える |
| RLS 運用規約4点（§6.5：`set local` のみ・全アクセス明示トランザクション化・`force row level security`・未設定時0行） | RLS を採用しないため |
| RLS のプーラ越しテスト（§25.2） | 同上 |
| migration 用ロールとアプリ用ロールの分離（§26.1） | 同上 |
| ADR-019 | 同上 |

**注意：「絶対に守ること」に挙げられていた RLS の3規則は、この判定により不要になる。** ただしこれはオーナーが明示していたルールの変更なので、§17-2 で承認を求める。**承認が取れるまでは §6.5 を消さない。**

---

## 9. そのまま残す設計（A）

**4回のレビューを通した設計の大半は生きている。** 特に次のものは、今回の再評価によって「残す」ではなく「結論を支える根拠」に格上げされた。

| 残すもの | 章 | なぜ残すか |
|---|---|---|
| **Task と ContentItem を分ける** | §5.3 | v1 は 1:1 で運用。後から分けるのは非常に大変で、最初から分けておく判断は正しい |
| **fail closed（判定できないものは拒否）** | §8.4 | 権限も同期も、迷ったら止める。Notion 同期の規則6にも適用した |
| **同時更新の検出（版番号＋409）** | §22 | §6.2-(1) の事故を防ぐ唯一の手段。**Notion では実装できないと判明したため、内部DBを持つ根拠になった** |
| **冪等性（同じ処理を2回実行しても1回分）** | §22 | §6.2-(3) の事故を防ぐ。同じく内部DBを持つ根拠 |
| **Capability による権限判定**（役割名をコードに書かない） | §8.3 §9 | 権限表と現行判定の対応表（§9.3）はそのまま Permission Test の入力になる |
| **業務日付は Workspace の暦日。Domain から `new Date()` を呼ばない** | §6.1 | 定期実行は UTC で動くため、これを守らないと日付判定が1日ずれる。**cron を使う構成では一層重要** |
| **監査ログ（誰がいつ何をどう変えたか）** | §19 | §6.2-(2) のとおり、Notion では代替できない |
| **Layer A の分割**（業務計算はサーバー、表示整形はブラウザ） | §3.3 | 同じ業務計算を2箇所に持たない原則 |
| **Visual / Interaction Contract の維持** | §3.4 | UI を変えないことの定義。画素差0で判定 |
| **Domain Event → Outbox → Job Runner** | §3.2 §23 | 外部APIの遅さを画面に持ち込まない |
| **営業日計算・翌月生成・負荷計算のロジック** | §12 §18 | そのまま移植する |
| **9つの自動処理ルール** | §13.5 | 内容はそのまま。実行基盤だけ変わる |
| **Permission Matrix Test / 画面差分テスト / 固定時刻テスト** | §25 | そのまま。RLS テストのみ削除 |
| **expand / contract 方式のスキーマ変更** | §6.6 | そのまま |
| **移行の Parity 確認**（件数・計算結果・画面の全一致） | §24.6 | そのまま。移行の安全性はここで担保する |
| **外部が落ちても業務が止まらない** | §26.6 | そのまま。設計上の要件として維持 |

---

## 10. 修正する設計（B）

### 10.1 差し替えになるもの

| # | 現在の設計 | 修正後 | 章 |
|---|---|---|---|
| 1 | Source of Truth = PostgreSQL（単一） | **内部DB（D1）＝進捗・担当・権限・履歴 / Notion ＝顧客・契約・社員タスク / Drive ＝ファイル** | §3.2 §5 |
| 2 | Google OAuth を自前で実装しセッション Cookie を発行 | **Cloudflare Access が Google ログインを担い、検証済みの本人情報を受け取る** | §8.2 |
| 3 | Client / ServiceContract を内部の正典として持ち、画面から編集する | **Notion を正典とし、内部は読み取りキャッシュ。編集は Notion で行う（今と同じ作業）** | §5.2 §27 Phase 5 |
| 4 | Managed PostgreSQL ＋ 接続プーラ | **Cloudflare D1（SQLite）** | §6 §26.1 |
| 5 | Workspace 分離を3重（アプリ層・スキーマ層・RLS） | **2重（アプリ層・スキーマ層）＋ 権限テスト** | §6.5 |
| 6 | 移行元は Artifact の `STATE` | **Artifact の `STATE` ＋ 進捗管理スプレッドシート ＋ Notion の3系統** | §24 |
| 7 | 5状態と遷移を画面から設定できる | **v1 は固定値** | §11.5 |
| 8 | staging に匿名化した本番コピー | そのまま（**production のデータを staging へコピーしない**は維持）。加えて **Notion の staging 同期先を分ける**（本番 Notion を staging から書かない=読むだけなので安全だが、同期先の取り違えを防ぐ） | §26.2 |

| 9 | Google Calendar から定例MTGを取り込み、予定の一部を Calendar 由来として扱う | **Calendar を正典にしない。** D1 を正典とし、連携する場合は D1 から Calendar へ書き出す外部表示先とする | §16 |
| 10 | 緊急度・依頼者を持たない（現行UIに無いため設計にも無い） | **緊急度は導出値として自動判定、依頼者は操作ユーザーから自動記録。入力欄は新設しない** | §5.1 §4.5 |
| 11 | ネタストックは設計対象外 | **Notion へ移して保存する。** SOCIAL BASE は当面参照しない | §5.1 |

### 10.2 追記が必要になるもの

| # | 追記するもの | 理由 |
|---|---|---|
| 1 | **Notion 同期の契約**（読むプロパティの列挙・除外規則6件・必須欠落時は停止して通知） | §5.3。実データに記入例・空ステータス・null混在が実在する |
| 2 | **Notion スキーマ変更の検知** | プロパティ名や選択肢が変わると連携が黙って壊れる。起動時と同期時に検証する |
| 3 | **契約金額を運ばない規約** | §7.3-(4)。同じ DB に金額がある |
| 4 | **AI（Claude API）の呼び出し境界** | 台本・企画の下書き。Domain には入れず Integration 側に置く。顧客情報を送る範囲を明示する |
| 5 | **`DB_タスク` の読み取り表示** | 社員タスクを「今日」画面に混ぜて見せる。**書き込まない**ことを規約にする |
| 6 | **業務委託メンバーのアクセス方法** | 会社の Google アカウントを持たない可能性がある（§17-4） |

---

## 11. 削除 / 簡素化する設計（C）

**今回の目的に対して過剰なもの。** 優先順位5（将来の汎用化）のために優先順位1〜4を重くしていた部分である。

| # | 削除・簡素化するもの | 章 | 判断の理由 |
|---|---|---|---|
| 1 | **RLS 一式**（データベース自身が行単位で遮断する仕組み） | §6.5-3 ADR-019 | Workspace は1つ、利用者25人、書き手はアプリ1つだけ。得られる防御に対し、付随コスト（読み取りまで含む全アクセスの明示トランザクション化、`set local` の徹底、DBロール2種、プーラ越しの専用テスト）が大きすぎる。**同じ目的は権限テストとアプリ層1箇所への集約で達成する** |

> **［確定・オーナー承認］RLS の削除は権限チェックを弱めるという意味ではない。** 次の4つは**維持する**。
>
> 1. **server-side permission check** — 認可はすべてサーバー（Worker）側で判定する。UI の非表示・二度押し防止を安全性の根拠にしない
> 2. **fail closed** — 判定できないものは拒否する（設計書 §8.4 の判定順序を維持）
> 3. **Capability ベースの権限** — Role 名をコードに書かない。判定は `hasCapability(member, key, scope)`（設計書 §8.3 / §9 を維持）
> 4. **Audit** — 誰がいつ何をどう変えたかを必ず残す（設計書 §19 を維持）
>
> 削除するのは **PostgreSQL 固有の機構である RLS だけ**であり、権限の設計思想は一切変えない。
| 2 | **Workflow を画面から設定可能にする機能**（保存時の7規則を含む） | §11.5 | 5状態は業務として固定している。設定可能にすると「使用中の状態を消して履歴が壊れる」リスク（§28 R8）を自ら作ることになる |
| 3 | **Scope 評価器のための列**（クライアント単位の権限） | §9.5 | 全 Role が Workspace 単位で足りる。必要になった時点で足す（テーブル変更なしで足せる設計は維持） |
| 4 | **全テーブルへの複合UNIQUE・複合外部キーの機械適用** | §6.1 | Workspace をまたぐ権限昇格を防ぐ仕組みだが、Workspace が1つなら起こり得ない。**`workspace_id` 列は残す**（後から入れるのが非常に大変なため、保険として安い） |
| 5 | **クライアント管理画面の新規作成**（マスタ編集UI） | §27 Phase 5 | Notion が正典。新しい入力先を作ると §1.3 の基準に反する |
| 6 | **Recommendation（提案機能）** | §18 §27 Phase 9 | 負荷計算は残すが、提案と採否の記録は後段。優先順位1〜3に直接効かない |
| 7 | **Realtime（SSE）** | §3.2 | 既に不採用。ポーリングで足りる |
| 8 | **Event Sourcing** | §3.2 | 既に不採用 |
| 9 | **ContentItem の `kind` をマスタ化する検討** | §28.3 | 動画／静止画の2種で足りる。Notion 側に `動画投稿本数` `静止画投稿本数` として既に2種で存在する |
| 10 | **Read Model のページングと ETag の先行実装** | §28.3 LATER-1 | 月あたり62本。件数が増えてから |

### 11.1 削除しないもの（過剰に見えて必要なもの）

念のため明記する。次は「小さい規模だから要らない」と切り捨てない。

- **監査ログ** — 規模と無関係に、誰が承認したかは業務上必要
- **同時更新の検出** — 25人でも同時に触る。むしろ少人数ほど1件の消失が痛い
- **冪等性** — 自動処理を入れる以上、二重実行は必ず起こる
- **業務日付の規約** — cron が UTC で動くため必須
- **画面差分テスト** — UI を変えない約束を機械で守るため

---

## 12. 想定される月額費用

**Architecture Decision の基準は USD ベースの公式料金とする。円換算は参考値であり、判断の根拠には使わない。**

### 12.1 ［事実］推奨構成の公式料金（USD・2026年9月時点）

| 項目 | 課金の形 | 最低月額 | 条件 |
|---|---|---|---|
| **Cloudflare Workers 有料プラン** | 定額＋従量 | **$5 / 月（最低額）** | 月 **1,000万リクエスト** と **CPU 3,000万ミリ秒** を含む |
| **Cloudflare D1** | 従量（Workers 有料プランの枠に含まれる） | **$0**（枠内） | 下の 12.2 参照 |
| **Cloudflare Access（Zero Trust）** | 人数課金 | **$0** | **50ユーザーまで無料。** 超えると **$7 / ユーザー / 月**（上限なしの従量プラン） |
| 定期実行（Cron Triggers） | Workers の機能 | 追加なし | 起動はリクエスト枠から消費される想定。**実装時に実測で確認する** |
| 静的ファイルの配信 | Workers の機能 | 追加なし | 同一オリジンから `ml-editing-board.html` を配る |
| 秘密情報の保管 | Workers の機能 | 追加なし | — |
| **独自ドメイン** | 年額 | **取得しなければ $0** | `*.workers.dev` のままなら費用ゼロ。独自ドメインを使う場合はレジストラの取得費（年額）が別途。**金額は取得するドメイン次第で、今回は未確認** |
| 外形監視・エラー通知 | — | $0 | 無料枠のサービスを使う |
| GitHub Actions（CI） | — | $0 | private リポジトリの無料枠内（月2,000分） |
| Notion / Google Workspace / Slack | 既存契約 | 追加なし | **編集スタッフに Notion の追加席は不要** |

**推奨構成の最低月額は $5。** これに独自ドメインを使う場合の年額が加わる。

### 12.2 ［事実］D1 の含有枠と超過料金

| 項目 | Workers 有料プランの含有枠 | 超過料金 | 無料プラン |
|---|---|---|---|
| 行の読み取り | 月 **250億行** まで | $0.001 / 100万行 | 日 500万行 |
| 行の書き込み | 月 **5,000万行** まで | $1.00 / 100万行 | 日 10万行 |
| ストレージ | **5GB** まで | $0.75 / GB・月 | 5GB |
| DB1つの最大サイズ | 10GB | — | 500MB |
| 時点復元（Time Travel） | **30日** | 追加費用なし | 7日 |
| 1リクエストあたりのクエリ数 | 1,000 | — | 50 |

Workers 側の超過料金は **リクエスト $0.30 / 100万件**、**CPU $0.02 / 100万ミリ秒**。

### 12.3 ［評価］本件の使用量は含有枠に収まるか

**「収まるはず」ではなく概算を置く。** 前提：利用者25人、月の制作物62本（動画54・静止画8）、営業日22日。

| 項目 | 概算 | 含有枠 | 使用率 |
|---|---|---|---|
| リクエスト数 | 25人 × 200回/営業日 × 22日 ≒ **11万** ＋ 定期実行 数千 | 1,000万 | 約 1〜2% |
| 行の書き込み | 62本 × 工程5回 × （本体＋Audit）＋ 同期・通知 ≒ **数千行/月** | 5,000万 | 0.1% 未満 |
| 行の読み取り | 11万リクエスト × 1回あたり最大2,000行 ≒ **2.2億行** | 250億 | 約 1% |
| ストレージ | 年750件 × 数KB ＋ Audit ≒ **数十MB** | 5GB | 1% 未満 |

［評価］**最低額の $5 で収まる。** 桁が2つ違うため、利用が数倍に増えても枠内である。

**ただし超過が起きうる経路が1つある。** 索引の無いクエリや繰り返し処理の誤りで「行の読み取り」が跳ねると、$0.001/100万行でも積み上がる。**課金アラートを設定し、CI に1リクエストあたりのクエリ数の上限確認を入れる。**

### 12.4 円換算・為替・税の扱い

| 項目 | 扱い |
|---|---|
| 請求通貨 | **USD**。円建てではない |
| 円換算 | カードの為替レートと請求日で変動する。**参考値としてのみ扱う**（1ドル160円で $5 ≒ 800円、2026-09-02 の実勢は約159円） |
| 消費税 | 国外事業者からの電気通信利用役務の提供に該当し得る。**扱いは自社の経理で確認が必要**（ここでは断定しない） |
| Zero Trust の追加条件 | ［事実］サービス個別条件に「1席あたり平均 月15万DNSクエリ」の目安があり、超えると追加席の購入を求められ得る。Access のみの利用では通常問題にならないが、条件として存在する |

### 12.5 他サービスの公表価格（**同条件比較ではない**）

**前回レポートに書いた「約6倍」「約20倍」という倍率は削除した。** 含有枠・課金単位・機能範囲が異なり、同条件の比較を証明できないためである。以下は各社の公表価格を並べただけであり、**判断の根拠には使わない。**

| 構成 | 公表価格 | 含まれるもの・注意 |
|---|---|---|
| Cloudflare Workers 有料 ＋ D1 ＋ Access | $5 / 月（最低額） | 12.1・12.2 のとおり。Access は50人まで無料 |
| Vercel Pro | $20 / デプロイ席 / 月 | 席ごとに $20 の利用クレジットを含む。**Hobby プランは非商用のみ** |
| Neon Launch | 従量（月額固定の最低額なし） | CPU 時間 $0.106/CU時、ストレージ $0.35/GB・月、履歴 $0.20/GB・月（最大7日）。**構成により大きく変わる** |
| Supabase Pro | $25 / 月 | **時点復元は追加オプション**で7日 $100 / 月（14日 $200・28日 $400） |
| Google Cloud（Cloud Run ＋ Cloud SQL） | Cloud Run は無料枠あり／Cloud SQL は最小構成で月10ドル台〜 | DB が常時課金。ストレージ・バックアップ・下り通信は別 |

［評価］推奨構成を選ぶ理由は**価格差の倍率ではなく §4.2 の4点**である。すなわち (1) 認証を自前で作らない (2) DB・定期実行・配信・秘密情報が1つの基盤に揃う (3) ローカル開発に Docker と PostgreSQL の導入が不要 (4) 時点復元が追加費用なしで標準。価格はそのうえで最低額が最も低いという事実にすぎない。

---
## 13. 実装難易度

### 13.1 領域ごとの難易度

「大」は判断と検証に時間がかかるもの、「小」は手順が決まっているものを指す。

| 領域 | 難易度 | 主な作業 | 既存計画との差 |
|---|---|---|---|
| 土台（DB・環境3面・CI・ログ・設定管理） | **小** | ローカルは SQLite ファイル1つ。Docker も PostgreSQL 導入も不要 | **軽くなった**（RLS・接続方式・DBロール分離が消えた） |
| ログイン・本人特定 | **小** | Cloudflare Access の設定と、受け取った本人情報の検証 | **大きく軽くなった**（OAuth 実装が不要） |
| 権限（誰が何をできるか） | **中** | 権限表の実装と、現行画面と同じ出し分けの検証 | ほぼ変わらず（§9.3 の対応表がそのまま使える） |
| 工程と進捗（5状態・同時更新・冪等性） | **中** | ここが本体。同時更新の検出と履歴の記録 | 変わらず |
| Notion 同期 | **中** | 読むプロパティの固定、除外規則、スキーマ変更の検知 | **新規**（ただしクライアント管理画面の新規作成より軽い） |
| 締切・営業日・翌月生成 | **中** | 既存ロジックの移植。日付のずれを固定時刻テストで押さえる | 変わらず |
| 自動処理9ルール | **中** | 定期実行と二重実行の防止 | 変わらず |
| Google Drive 連携 | **中** | 運用アカウントでの接続、フォルダとファイルの紐づけ | 変わらず |
| Slack 連携 | **中** | シフト変更の受付。署名検証と再送耐性 | 変わらず（後段） |
| AI（台本・企画の下書き） | **小〜中** | Notion の顧客情報を材料に下書きを作る | **新規**（優先順位が上がった） |
| 画面（UI） | **小** | データの取り方だけ変える。見た目は変えない | 変わらず |
| クライアント管理画面 | **消滅** | Notion を読むだけ | **削除**（既存 Phase 5 の大半） |

### 13.2 Phase の組み替え（提案）

既存の10段階を、優先順位（§1.2）に沿って組み替える。**各段階は「画面からデータまで通して動く」で終える**という原則は変えない。

| 新 Phase | 内容 | 完了条件 | 旧 Phase |
|---|---|---|---|
| **2** | 土台（D1・移行手順・環境3面・CI・ログ・設定と秘密情報） | 空のスキーマに対して CI が全 green | 2（縮小） |
| **3** | ログイン・本人特定・権限 | 現行と同じ画面が出る。権限テストが全 green | 3（縮小） |
| **4** | **Notion 読み取り同期**（顧客・案件・社員タスク） | Notion に案件を1行足すと画面に出る。除外規則6件が効く | 新規（旧5の代替） |
| **5** | 工程と進捗（5状態・同時更新・履歴） | 「今日」画面の主要操作が動く。同時操作で消えない。画素差0 | 4 |
| **6** | 締切・営業日・翌月生成・負荷 | 翌月生成が動き、遅れが自動で出る | 5・6の一部 |
| **7** | 自動処理9ルール（通知） | 誰も開いていない時刻に督促が飛ぶ。二重に飛ばない | 6 |
| **8** | Google Drive 連携 | 素材をリンクから開ける。**備考にファイル名を書く運用が終わる** | 7 |
| **9** | AI（台本・企画の下書き・タイトル整形） | 顧客情報から下書きが出る | 新規 |
| **10** | Slack 連携（シフト変更） | 申請→承認→負荷再計算が通る | 8 |
| **11** | 仕上げと移行 | セキュリティ確認・復元テスト・Parity 確認・カットオーバー | 10 |
| 後段 | 推薦機能 / Notion への進捗の書き出し / 他部署への流用 | — | 9 |

**変更点は3つ。** (a) Notion 同期を前に出した（早い段階で「Notion に足せば画面に出る」が実現する）。(b) クライアント管理画面の新規作成を削除した。(c) AI を独立した段階として明示した（優先順位4に含まれる要件のため）。

---

## 14. 主なリスク

### 14.1 この構成で新しく増えるリスク

| # | リスク | 影響 | 対策 |
|---|---|---|---|
| N1 | **Notion のプロパティ名や選択肢が変わると連携が黙って壊れる。** Notion には「この名前は変えないでください」を機械的に守る仕組みがない | 翌月生成が止まる／案件が画面から消える | 同期のたびに必須プロパティの有無を検証し、**欠けたら同期を止めて通知**（fail closed）。読むプロパティを最小限に列挙。§17-5 で「変更前に一声かける」運用も決める |
| N2 | **Notion に不完全な行が混ざる。** 記入例・ステータス空・数値の空欄が実在する | 案件が抜ける／本数が0になる | §5.3 の除外規則6件。除外した行は「要確認」として画面と通知に出す |
| N3 | **契約金額が同じ DB にある** | 編集スタッフに金額が見える経路ができる | 取得プロパティを列挙し金額を含めない。スタッフ向け応答に含めない。**Notion 側でスタッフに案件DBを開かせない**（推奨構成ではスタッフに Notion 席を渡さないので成立する） |
| N4 | **Cloudflare への依存** | 将来他社へ移すのが多少大変 | SQL とドメイン層を分離。SQLite 固有の書き方を避ける。移行の動機（Workspace 増加・データ量）が出るまで移さない |
| N5 | **RLS を採用しないことによる防御の1枚減** | 権限の実装ミスが直接「見えてはいけないものが見える」になる | 権限判定をアプリ層の1箇所に集約し、そこ以外から読み書きさせない。§25.3 の権限テストを全項目分書く。**§17-2 で承認を求める** |
| N6 | **Cloudflare Access の無料枠は50人まで** | 超えると1人あたり月$7 | 現在25人程度。超えるのは他部署展開時 |
| N7 | スプレッドシート廃止で行き場のない項目が3つあった（緊急度・依頼者・ネタストック） | 廃止すると情報が失われる | **解消済み。** 緊急度は自動判定、依頼者は自動記録、ネタストックは Notion へ移す（§5.1）。**UI 変更は発生しない**。承認は §17-C |
| N8 | **AI の利用料が使った分だけ増える** | 想定外の請求 | 月の上限額を設定する。1回あたりの入力量を制限する |
| N9 | **業務委託メンバーが会社の Google アカウントを持たない可能性** | ログインできない | §17-D で判断。Access は個別のメールアドレス許可にも対応できる |
| N10 | **`getIdentity()` に IdP 側の不変ユーザーIDが含まれるかを公式ドキュメントで確認できていない**（確認できたのは email / name / groups） | 外部から来る値だけで人を同定すると、将来の本人取り違えを防げない | **D1 側の不変な内部ID（`members.id`）を主キーにし、email を主キーにしない**（§4.5）。無効化済みメンバーの email で新規ログインが来たら自動で結びつけず「要確認」で止める（fail closed）。`getIdentity()` の返却内容は**実装時に実測して確認する** |
| N11 | **退職者等のメールアドレスを別人へ再利用する運用** | SOCIAL BASE に限らず、他サービスを含めて本人を取り違える | **再利用しないことを運用ルールにする**（オーナー承認済み）。SOCIAL BASE 側は N10 の fail closed で二重に受け止める |

### 14.2 既存リスク（§28.2）の変化

| 既存 | 変化 |
|---|---|
| R1 CSP によるホスティング移行（運用URLが変わる） | **変わらず残る。** 推奨構成でも新しいURLになる。旧URLを2週間残す方針は維持 |
| R2 移行時のデータ欠落 | **増える。** 移行元が3系統（Artifact・スプレッドシート・Notion）になるため、Parity 確認の対象が増える |
| R3 `trans` の遷移履歴が復元できない | 変わらず（受け入れる） |
| R4 祝日・営業日データの保守 | 変わらず |
| R5 定期実行の遅延 | 変わらず（最大1分程度） |
| R6 Google API のレート制限 | 変わらず。**Notion のレート制限（1秒3回）が新たに加わる**ため、画面表示は必ず内部DBから返す |
| R7 一覧の全件描画 | 変わらず（月62本なので当面問題にならない） |
| R8 Workflow を設定可能にしたことによる破壊 | **消える**（設定可能にしないため） |
| R9 Permission の設定ミス | **RLS が無くなるため N5 に統合。** 権限テストで担保する |
| R10 staging に本番データをコピーする誘惑 | 変わらず（禁止を維持） |

---

## 15. Phase 2 を開始してよいか

### 15.1 判定

**条件付きで「可」。ただし今すぐではない。** 順番は次のとおり。

| 順 | やること | 誰が |
|---|---|---|
| 1 | **本レポートの確認**（特に §17 の判断5件） | オーナー |
| 2 | 承認された内容で `SOCIAL_BASE_SYSTEM_DESIGN.md` を改訂（§8.1 の B に該当する**16章**） | こちら |
| 3 | 改訂版に Senior Review を1回かける（前提が変わったため、レビュー結果を引き継げない） | こちら |
| 4 | **Phase 2 開始** | こちら |

**設計書を改訂せずに Phase 2 へ入るべきではない。** 理由は、現在の設計書が「PostgreSQL が SoT」「RLS を最初から入れる」と書いており、これは正典である。正典と違うものを実装すると、以後どちらが正しいか誰にも分からなくなる。

### 15.2 開始前に決めなければならないこと

§17 にまとめた5件。**このうち3件は新しく発生した判断事項**である（既存 §28.1 の15件には無い）。

### 15.3 改訂後の Phase 2 の中身

| 項目 | 内容 | 既存設計からの変化 |
|---|---|---|
| データベース | Cloudflare D1。空のスキーマとスキーマ変更の手順 | PostgreSQL → SQLite |
| スキーマ | 進捗・担当・締切・権限・履歴・自動処理・冪等キー・Notion キャッシュ。**`workspace_id` 列は残す** | テーブル数を削減。複合外部キーの機械適用をやめる |
| 環境3面 | local（SQLiteファイル）/ staging / production | **Docker と PostgreSQL の導入が不要になった** |
| CI | lint → 型 → 単体 → DB結合 → 権限 → API → E2E → 画面差分 | **RLS テストを削除** |
| ログ | 構造化ログ＋`request_id` | 変わらず |
| 設定・秘密情報 | Cloudflare の秘密情報保管を使う | 変わらず（置き場所が変わるだけ） |
| RLS | **入れない** | 削除 |
| 認証の土台 | Cloudflare Access の設定と、受け取った本人情報の検証 | **新規**（Phase 3 から前倒しできる軽い作業） |
| 完了条件 | **空のスキーマに対して CI が全 green** | 変わらず |

---

## 16. SOCIAL BASE は最終的にどう動くのか

専門用語を使わずに、実際の1日で説明する。

### 16.1 編集スタッフ（りりか・つかさ・ゆかり・ももこ・業務委託の方）の1日

```
朝、SOCIAL BASE を開く（会社の Google アカウントで自動ログイン）
  ↓
「今日」の画面に、自分が担当の動画だけが締切の近い順で並んでいる
  ・並べ替えも探すこともしない。開いた時点でその順で出ている
  ↓
1本目を開くと、必要なものがその場に揃っている
  ・撮影された動画素材のリンク（Drive のファイルへ直接。名前で探さない）
  ・カバー画像のリンク
  ・参考動画のリンク
  ・社員からの指示メモ
  ↓
編集する（今と同じ作業）
  ↓
終わったら「編集完了」を押す
  ↓
これで終わり。スプレッドシートには何も書かない。Slack で報告もしない
```

押した瞬間に、機械側で次が同時に起こる。

- 進捗が「確認中」になる
- 社員の画面に「確認待ち」として出る
- 「今月4本のうち3本目が完了」と数え直される
- 「りりかが 9/3 14:20 に編集完了にした」と履歴に残る

### 16.2 社員（なつみ・友美）の1日

```
朝、SOCIAL BASE を開く
  ↓
「今日」の画面に、今日やることが3種類まとまって出ている
  ① 確認待ちの動画（スタッフが編集を終えたもの）
  ② 遅れている・止まっている動画（機械が自動で見つけたもの）
  ③ 自分の業務タスク（企画・台本・指示出し）
     → これは Notion に入力したものがそのまま出ている。二重入力はしない
  ↓
確認待ちを開いて中身を見て、「承認」を押す → 「投稿待ち」へ
  ↓
新しい動画を追加するときは、タイトルを入れて担当を選ぶだけ
  ・何本目かは入力しない（自動）
  ・締切は営業日から自動で計算される（手で入れることもできる）
  ↓
台本や企画の下書きが必要なときは、SOCIAL BASE から AI に頼む
  ・材料は Notion の顧客情報（USP・ターゲット・訴求・不安）を機械が自動で読む
  ・出てきた下書きを人が直して使う
```

**新しいクライアントが増えたときは、今までと同じように Notion に1行足すだけ。** SOCIAL BASE 側の作業はない。翌日には画面に出る。

### 16.3 社長の1日

```
SOCIAL BASE を開く
  ↓
・今月、全クライアントで何本出す約束か / 何本終わったか
・遅れているものはどれか、誰の手元で止まっているか
・誰に仕事が偏っているか
  ↓
これを見るだけ。入力はしない
```

### 16.4 誰も操作していない時に動くこと

**ここが Artifact のままでは実現できない部分である。**

| いつ | 何が起こるか |
|---|---|
| 毎晩 | Notion を読んで、案件・契約本数・社員タスクを取り込む |
| 毎朝 | 締切と遅れを再計算し、遅れているものに印を付ける |
| 投稿予定日の14日前・7日前・3日前 | 準備の督促を出す |
| 毎月12日 | 分析のサイクルを始める |
| 締切を過ぎたとき | 通知を出す |
| 祝日データが尽きる90日前 | 「登録してください」と通知する |
| 自動処理が止まったとき | **止まったことを別の仕組みが検知して通知する** |

朝、誰かが画面を開くより先に、機械が状況を整えている。**「金曜15分のヒアリングで初めて遅れに気づく」が「気づいた時にはもう印が付いている」に変わる。**

---

## 17. 私が今判断する必要があること

**技術的な実装詳細はこちらで決めた。判断をお願いするのは次の5件だけである。** それぞれ推奨案を書いた。

### 17.1 今回の再評価で新しく発生した判断（3件）

| # | 判断していただきたいこと | 推奨 | 決めないとどうなるか |
|---|---|---|---|
| **A** | **役割分担と費用の承認。**<br>・顧客 / 契約 / 運用ルール / 一般社員タスク / ネタストック → **Notion のまま**（正典）<br>・素材ファイル → **Google Drive のまま**（正典）<br>・動画1本ごとの進捗・担当・締切・権限・操作履歴 → **SOCIAL BASE 内部（D1）**<br>・費用は **最低 $5 / 月**（Cloudflare Workers 有料プラン。D1 と Access 50人分を含む）。独自ドメインを使う場合はその年額が別途 | **この方針で進める。** 理由は §4.2 の4点（認証を自前で作らない／基盤が1つに揃う／ローカル開発に Docker と PostgreSQL が不要／時点復元が標準）。**価格の安さは理由の4番目であって1番目ではない** | 設計書を改訂できず Phase 2 に入れない |
| **B** | **「RLS」という防御を1枚外すことの承認。** これは「絶対に守ること」に挙がっていたルールの変更なので、勝手には外さない。<br>外す理由：Workspace が1つ・利用者25人・書き手はアプリだけという条件では、得られる安全性に対して手間（読み取りまで含む全アクセスを特別な手順で包む必要が出る）が大きすぎる。<br>代わりに「権限の判定をアプリの1箇所に集め、権限表の全項目を機械テストする」で守る | **承認済み。外す。** ただし **server-side permission check / fail closed / Capability ベースの権限 / Audit は維持する。**「権限チェックを弱める」ではない。将来他部署へ本格展開するときに RLS を再検討する | 設計書 §6.5 を残したまま実装することになり、Phase 2 の作業が重くなる |
| **C** | **スプレッドシート廃止で行き場がなくなる3項目の扱い。**（現行 FIX済み UI にこの3つは存在しない）<br>① **緊急度**（高／中／低）<br>② **依頼者**（誰が依頼したか）<br>③ **ネタストック**（別タブ2枚・合計198行） | **① 入力欄を新設せず、締切・現在工程・遅延状態から SOCIAL BASE が自動判定する**<br>**② 入力欄を新設せず、作成者または操作ユーザーから自動記録する**（§4.5）<br>**③ Notion へ移行して保存する。現時点では FIX済み UI に画面追加しない**<br>→ **この3つとも UI 変更が発生しない。** 進捗管理シートは廃止できる | スプレッドシートを廃止できない、または情報が失われる |

**C について。** ①②は手入力項目を増やさないため §1.3 の基準（入力を増やさない）を満たす。③は保管場所が Notion に移るだけで、社員の作業は変わらない。**将来これらを画面に出したくなった場合のみ、UI 変更の相談として別に扱う。**

### 17.2 前回から持ち越しの判断

| # | 判断していただきたいこと | 推奨 | 状態 |
|---|---|---|---|
| **D** | **アカウント運用の2件。**<br>① **業務委託の方のログイン方法。** シート上に「委託」名義の担当が実在する。会社の Google アカウントを持たない場合どうするか<br>② **退職者等のメールアドレスを別人へ安易に再利用しないというルール。** SOCIAL BASE に限らず、他サービスを含めた本人取り違えの防止のため（§14-N11） | **① その方のアドレスだけを個別に許可する**（会社アカウントを新規発行しなくてよい。契約終了時は許可を消すだけ）<br>**② 再発行しないことをルールにする。** どうしても再発行する場合は、SOCIAL BASE 側で先に該当メンバーを無効化する<br>**共有ログインは社員・業務委託とも禁止**（1人1アカウント） | **新規**（§28.1 に無い） |
| **E** | **Notion の運用ルール2件。**<br>① `DB_案件` のプロパティ名・選択肢を変える前に一声かけていただく<br>② `DB_案件` に構造化された項目を2〜3個追加してよいか（工程数・撮影のタイミングなど。現在は「メモ」欄の文章にしか無い） | **①をルールにする。②を許可いただく。** どちらも Notion 上の作業で、プログラム変更は不要 | **新規**（§28.1 に無い） |
| Q2 | **権限表（設計書 §9.2）の内容でよいか** | 表のとおり。運用開始後も画面から変更可 | 前回から未回答。**このまま持ち越し** |
| Q8 | **新旧システムの切り替え日時** | 金曜の業務終了後。土日で確認し月曜から新環境 | 前回から未回答。**移行は最終段階なので、いま決めなくても Phase 2 は開始できる** |
| Q6 | **投稿予定日を Google カレンダーへ書き出すか** | v1 では書き出さない（Calendar は正典にしないため、書き出しは任意の追加機能） | 設計書のまま未決。**Phase 2 には影響しない** |
| Q11 | **月額のかかるサービス構成と概算費用** | **回答済み。§12 のとおり最低 $5 / 月** | 上記 A に統合 |
| Q1 | **ログインを会社の Google アカウントにしてよいか** | Google。ただし**実装方法が変わった**（自前で作らず Cloudflare Access に任せる）。使い勝手は同じ | 上記 A に統合。**別途の可否判断は不要** |

### 17.3 実装側で決めたこと（判断不要）

参考までに列挙する。すべて技術的な選択で、業務・費用・使い勝手に影響しない。

- 内部データの置き場を Cloudflare D1（SQL が使えるデータベース）にする
- Notion からの取り込みは片方向（読むだけ）にする
- 認証は Cloudflare Access（`ctx.access`）に任せ、**JWT 検証を自前実装しない**。認可は D1 側で判定する（§4.5）
- 操作者を UI から受け取らず、Identity 由来の本人を使う（§4.5）
- 5つの進捗状態は固定値にする（画面から編集できるようにはしない）
- Notion の不完全な行を除外する規則6件（§5.3）
- `契約金額` と `先方担当者` を取得対象に入れない
- 5状態と遷移、同時更新の検出、履歴の記録、営業日計算は既存設計のまま使う
- Phase の順序を組み替え、Notion 同期を前倒しし、クライアント管理画面の新規作成を削除する
- Notion 側の「担当」と SOCIAL BASE 側の「担当」を **案件担当 / 編集担当** と呼び分ける（§5.4）

---

## 変更履歴

| 日付 | 版 | 内容 |
|---|---|---|
| 2026-09-03 | v1 | 初版。PostgreSQL 前提の再評価。Notion / Drive / Artifact ランタイム / スプレッドシートの実地確認に基づく |
| 2026-09-03 | v2 | オーナー指摘を受けた5点の修正。①Artifact 判定を「Claude の Connector/MCP」と「公開 Artifact の Runtime 能力」に分離し、外部通信・永続ストレージ・本人識別・定期実行の4軸で判定（§7）②Notion の評価を［事実］と［評価］に分離し、結論を「トランザクションデータの正典には採用しない」に改めた（§6）③料金を USD ベースの公式料金・含有枠・超過料金・為替税・ドメイン費に分解し、倍率比較を削除（§12）④Identity を Cloudflare 標準の `ctx.access` / `ctx.access.getIdentity()` 方式に確定し（JWT 検証を自前実装しない）、Audit への自動記録を設計（§4.5）⑤SoT 表を確定し、Notion と D1 の二重更新が無いことを1件ずつ確認（§5.1 / §5.4）。章の分類の数え違い（B は15章ではなく16章）を訂正 |
| 2026-09-03 | v3（確定） | オーナーが A〜E を正式承認。①Identity を Cloudflare 標準の `ctx.access` / `ctx.access.getIdentity()` に確定し、Access JWT の自前検証を廃止（§4.5）②Member の同定を D1 の不変な内部ID（`members.id`）に確定し、email を主キーにしない規約を明記（§4.5）③`sub` がメールアドレスに紐づくという記述を削除（Zero Trust organization から削除・再追加されたユーザーは異なる `sub` になるため、当該説明は成り立たない）。メールアドレスを別人へ安易に再利用しないという運用ルールは維持（§14-N11）④RLS 削除は権限チェックの弱体化を意味しないことを明記（server-side permission check / fail closed / Capability / Audit は維持） |
