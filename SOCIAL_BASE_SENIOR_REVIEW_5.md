# SOCIAL BASE Senior Review 5（D1 移行版）

- 実施日：2026-09-03
- 対象：`SOCIAL_BASE_SYSTEM_DESIGN.md` v6（PostgreSQL → D1、Source of Truth の分割、Cloudflare Access への移行を反映した版）
- 観点：独立した Senior Backend / Platform Engineer として、**v6 の改訂が持ち込んだ欠陥**を探す
- 分類：BLOCKER / HIGH / MEDIUM / LOW
- 前提：**BLOCKER または HIGH が残る場合 Phase 2 に進まない**

## 1. レビューの前提と範囲

### 1.1 何を見たか

v6 で変更した16章を中心に、**改訂が既存の設計と矛盾していないか**を見た。過去4回のレビューで合意済みの設計思想（Task と ContentItem の分離、fail closed、Capability、同時更新の検出、冪等性、業務日付、Audit）は再検討しない。**見たのは「D1 と Notion に移したことで壊れた箇所」である。**

重点を置いた観点は4つ。

1. **原子性** — PostgreSQL のトランザクションを前提にした記述が、D1 のバッチで成立するか
2. **参照整合性** — Notion 由来の写しが業務データから参照されている構造に穴がないか
3. **認可** — RLS を外したことで、サーバー側の判定に抜けが生まれていないか
4. **内部矛盾** — 同じ対象について章をまたいで違うことを書いていないか

### 1.2 結果の要約

| 分類 | 件数 | 状態 |
|---|---|---|
| **BLOCKER** | 2 | **すべて解消** |
| **HIGH** | 10 | **すべて解消** |
| MEDIUM | 5 | すべて解消（うち2件は Phase 2 での実測項目として明記） |
| LOW | 2 | 解消 |

## 2. 判定

**BLOCKER 0 / HIGH 0。Phase 2 の開始条件を満たす。**

ただし §8 に挙げる**3件の未検証事項**は Phase 2 の最初に実測すること。1件目（Access を適用した Worker で cron が発火するか）が外れると自動処理の設計が根本から変わるため、**これを Phase 2 の最初の作業とする**。

## 3. BLOCKER

### 3.1 BLOCKER-1 一般社員 Task の保存方針が章をまたいで矛盾していた

- **所在** — §5.6 / §15.6 / §21.5
- **内容** — §5.6 と §21.5 は「D1 に写しを持たず、必要時に Notion を読んで表示する」と書き、§15.6 は「画面表示は必ず D1 の写しから返す。リクエストごとに Notion を呼ばない」と書いていた。**両立しない。**
- **なぜ BLOCKER か** — Phase 4 の実装方針が決まらない。かつ「必要時に読む」を選ぶと、Notion の**毎秒3リクエスト**の制限に「今日」画面が直接ぶつかる。現行 UI は即座に描画されるため、体感が明確に悪化する。
- **解消** — **`staff_task_mirror`（読み取り専用の写し）を D1 に持つ**方針へ統一した。§6.4 にテーブルを定義し、§5.6・§15.6・§21.5 の記述を揃えた。表示はこの写しから返し、Notion へは書かない。同期失敗時は写しが残るため表示は続き、最終同期時刻が24時間より古い場合に注記を出す（§26.5）。

### 3.2 BLOCKER-2 Notion 同期の削除が業務データを壊し得た

- **所在** — §15.6 / §5.6 / §6.4
- **内容** — 同期を「スナップショット置換」と書いていた。しかし `content_items` は `service_contract_id` と `client_id` で写しを参照している。**Notion 側で案件が終了・除外されただけで写しの行が消えると、外部キー違反で同期が失敗するか、連鎖削除で過去の制作実績を失う。**
- **なぜ BLOCKER か** — Notion 側の日常的な操作（案件のステータスを「終了」にする、事業ラインを直す）が、**SOCIAL BASE の実データ喪失につながる。** 移行後に静かに起こり、気づくのは過去の実績を見ようとしたときである。
- **解消** — **「スナップショット突き合わせ（追加・更新・archive）」に変更し、写しを削除しない規約にした。** §15.6 に Notion 側の状態と写しの扱いの対応表を追加し、§5.6 に「写しは削除しない」を明記した。`notion_page_id` で同定するため、Notion 側で復帰した場合は同じ行に戻る。archive された案件に紐づく `content_items` が残ることを §25.2 の回帰テストで固定した。

## 4. HIGH

### 4.1 HIGH-1 写しの同定キーがスキーマに無かった

- **所在** — §5.2 と §6.4 の不一致
- **内容** — §5.2 は `Client` / `ServiceContract` に `notion_page_id` と `synced_at` を持たせると書いたが、**§6.4 のテーブル定義には両方とも無かった。** 同期の同定キーが決まらない。
- **解消** — §6.4 に `notion_page_id`（`unique(workspace_id, notion_page_id)`）と `synced_at`、`archived_at` を追加。§5.2 の列と一致させた。

### 4.2 HIGH-2 `content_items.created_by` が無く「依頼者の自動記録」が実装できなかった

- **所在** — §8.6 / ADR-029 が要求、§6.4 に無い
- **内容** — 「依頼者は入力させず、作成者から自動記録する」と決めたのに、**保存先の列が定義されていなかった。**
- **解消** — §6.4 の `content_items` に `created_by` を追加し、`workspace_members(id, workspace_id)` への複合外部キーを張った。

### 4.3 HIGH-3 `integration_sync_issues` が参照だけされて定義が無かった

- **所在** — §15.6 が参照、§6.3 に無い
- **内容** — 同期の除外規則3・6 で止めた行を「要確認」として残す先として `integration_sync_issues` を挙げていたが、**テーブルが定義されていなかった。** 定義が無いまま実装すると「止めたが記録しない」＝**黙って案件が抜ける**状態になり、規則を置いた目的が失われる。
- **解消** — §6.3 に `integration_sync_issues`（`provider / source_kind / source_ref / reason / detail / first_seen_at / last_seen_at / resolved_at`）を定義した。

### 4.4 HIGH-4 PostgreSQL の配列型が残っていた

- **所在** — §6.3 `integration_connections.scopes text[]`
- **内容** — `text[]` は PostgreSQL の配列型で、**SQLite に存在しない。** 型の一括置換（`jsonb` → `json` など）では拾えていなかった。
- **解消** — `scopes json` に変更。§6.1 の型対応表の規約（JSON 文字列、検索キーは列にする）に従う。

### 4.5 HIGH-5 「契約内容の変更」が競合検出の対象に残っていた

- **所在** — §22.1
- **内容** — 楽観ロックの表に「契約内容の変更 → `service_contracts.version`」が残っていた。**契約は Notion が正典で SOCIAL BASE では変更しない**（§5.6 / ADR-027）ため、対応する Command が存在しない。
- **解消** — 該当行を「該当なし」に変更し、`service_contracts.version` は**同期の世代管理に使い、人の編集の競合検出には使わない**ことを明記した。§6.4 の制約欄にも同じ注記を入れた。

### 4.6 HIGH-6 対応する操作が無い Capability が権限表に残っていた

- **所在** — §9.1 / §9.2 `contract.manage`、および `client.manage` の定義
- **内容** — `contract.manage`（契約の月本数・工程数・担当の変更）は、契約が Notion 正典になったため**v1 で要求する API が存在しない。** また `client.manage` の定義が「クライアントの追加・変更」を含んでいたが、追加と名称・契約の変更は Notion 側の操作になった。**権限表と実装が食い違う状態は、権限レビューを無意味にする。**
- **解消** — `contract.manage` に「**v1 では対応する操作が無い**（キーは将来のために残すが、これを要求する API を作らない）」を明記。`client.manage` の定義を「**SOCIAL BASE 側のクライアント属性**（Drive設定・撮影日・企画ステータス・資料フロー）の変更」に限定し、追加・名称・契約は Notion であることを明記した。**キーは削除していない**（§9.2 / §9.3 は合意済みの設計であり、不要に書き換えない方針）。

### 4.7 HIGH-7 email の一意制約が fail closed 規約を担保していなかった

- **所在** — §8.2 規約2 / §6.2 `users`
- **内容** — 「`unique index on (lower(email))` は**有効な User の範囲で**張る」と書いていた。一方 §8.2 規約6 は「無効化済み User の email で新規ログインが来たら自動作成せず止める」と定めている。**有効な行だけに UNIQUE を限定すると、退職者の email で2人目の User を作れてしまい、規約6 をアプリのバグ1つで破れる。** RLS を外した以上、こうした構造的な防御を緩めるべきではない。
- **解消** — **無効化済みを含む全 User に対して `unique(lower(email))` を張る**ことに変更した（§8.2 規約2 / §6.2）。これにより「退職者の email で2人目を作る」が**制約で構造的に不可能**になり、再登録は既存行の復帰として明示的に扱うしかなくなる。

### 4.8 HIGH-8 Audit の不変性を DB レベルで担保する手段が無くなっていた

- **所在** — §6.5 / §19
- **内容** — §19 は「操作履歴はあとから書き換えない」と定めているが、RLS を外した結果、**DB 側でそれを強制する仕組みが1つも無くなっていた。** アプリのバグや誤った運用スクリプトで履歴が書き換わり得る。
- **解消** — **`audit_logs` と `workflow_run_transitions` に `before update` / `before delete` のトリガーを張り、`raise(abort, ...)` で止める**ことを §6.5 に規約として追加した。SQLite のトリガーで実装できるため追加コストは小さい。**「RLS が無くても履歴は書き換えないを DB で守れる」**を明記し、トリガーが効いていることを §25.2 のテストに追加した。

### 4.9 HIGH-9 Notion の担当者を SOCIAL BASE の人に対応づける規則が無かった

- **所在** — §15.6 / §6.4 `staff_task_mirror`
- **内容** — `DB_タスク.担当` は Notion のユーザーである。`assignee_member_id` として保存する設計にしたが、**対応づけの規則が書かれていなかった。** 規則が無いまま実装すると、名前の一致などで**他人に割り当てる**危険がある。
- **解消** — **email で `users` を引いて `workspace_members` を解決し、解決できない場合は `assignee_member_id` を NULL にして Notion 上の氏名の文字列だけを表示する**規約を §15.6 に追加した（推測で他人に割り当てない = fail closed）。`assignee_member_id` を NULL 許容にした。

### 4.10 HIGH-10 同期対象外の案件を指す社員タスクで同期が止まり得た

- **所在** — §15.6 / §6.4 `staff_task_mirror`
- **内容** — `DB_タスク.関連案件` は Lステップ運用など**同期対象外の案件**を指すことがある（実データに2件存在）。外部キーを張った `related_contract_id` にそのまま入れようとすると**外部キー違反で同期全体が失敗する。**
- **解消** — 同期対象に存在しない、または archive 済みの案件を指す場合は **`related_contract_id` を NULL にし、タスク自体は表示する**規約を §15.6 に追加した。**外部キー違反で同期を止めない。** §25.2 のテストに追加した。

## 5. MEDIUM

### 5.1 MEDIUM-1 認可の読み取りが原子的単位の外にあった

- **所在** — §22.6 規約3
- **内容** — 「認可チェックはバッチの前に行い、`version` 条件を併せて持たせる」と書いていた。しかし **`version` 条件は対象行の変化しか検出しない。** 権限が失効した直後の書き込みは、対象行が変わっていなければ通ってしまう。PostgreSQL のトランザクション内で認可を読めば防げていたものが、D1 のバッチでは防げない。**これは D1 化による実質的な後退である。**
- **解消** — **書き込み文そのものに認可述語を埋め込む**規約に変更した（§22.6 規約3）。`update ... where id = :id and version = :v and exists (select 1 from member_roles ... where capability_key = :cap)` の形にすることで、**認可の評価が書き込みと同一の原子的単位に入る。** 0行なら 403 か 409 のいずれかとして扱う。
- **補足** — 実害の窓は数ミリ秒であり、Access を通過した社内の人物に限られるため、リスク自体は小さい。ただし**設計上の後退を放置しない**ために修正した。

### 5.2 MEDIUM-2 Access を適用した Worker で cron が発火するかが未検証

- **所在** — §27 Phase 2
- **内容** — Access は**リクエストがコードへ届く前にエッジで評価される**。cron トリガーは HTTP リクエストではないため発火するはずだが、**公式ドキュメントで確認できていない。** 発火しなければ自動処理（督促・翌月生成・同期）が一切動かず、優先順位3（漏れ・遅れの防止）が成立しない。
- **解消（対応）** — §27 Phase 2 に「**Phase 2 で実測して確認する前提**」の表を設け、**これを Phase 2 の最初の作業と明記した。** 外れた場合の影響も書いた。設計を推測の上に置かない。

### 5.3 MEDIUM-3 `workers.dev` で Access が使えるかが未検証

- **所在** — §27 Phase 2 / §28.1 Q9
- **内容** — 独自ドメイン（Q9）は未決である。`*.workers.dev` で Access が使えなければ、**Q9 が Phase 2 の前提条件に繰り上がる。**
- **解消（対応）** — Phase 2 の実測項目に加え、外れた場合に Q9 が前提条件になることを明記した。

### 5.4 MEDIUM-4 新規 Capability であることが現行対応表に書かれていなかった

- **所在** — §9.3
- **内容** — `staff_task.read` を追加したが、§9.3（現行判定との対応）は**現行の分岐を全列挙する表**であり、対応する現行の分岐が無い新規 Capability が混ざると「移行で挙動が変わっていないか」の検証がぶれる。
- **解消** — §9.3 に「**`staff_task.read` は現行に対応する分岐が無い新規 Capability である**」を明記した。移行後に増える機能であり、現行の挙動を再現する対象ではない。

### 5.5 MEDIUM-5 新しい規約に対応するテストが無かった

- **所在** — §25.2
- **内容** — HIGH-7（email UNIQUE）・HIGH-8（Audit トリガー）・BLOCKER-2（削除しない）・HIGH-9/10（解決規則）を規約として書いても、**テストが無ければ実装で守られる保証が無い。**
- **解消** — §25.2 に次を追加した。
  - 無効化済み User の email で2人目を作れないこと
  - `audit_logs` / `workflow_run_transitions` の UPDATE / DELETE が失敗すること
  - Notion 側から案件が消えた応答で**行が archive されるだけで残り、紐づく `content_items` が失われないこと**
  - 同期対象外の案件を指す社員タスクが `related_contract_id` = NULL で取り込まれ、同期が止まらないこと
  - 解決できない担当者が他人に割り当てられないこと

## 6. LOW

### 6.1 LOW-1 v1 で適用しない節に注記が無かった

- **所在** — §16.2〜§16.5
- **内容** — 章の冒頭に「v1 では Calendar を扱わない」と書いたが、各節には注記が無く、途中から読むと有効な仕様に見える。
- **解消** — §16.2 に「**［v1 では適用しない］**」の注記を追加した。

### 6.2 LOW-2 Domain Model とスキーマの列が一致していなかった

- **所在** — §5.2 / §6.4
- **内容** — `archived_at` の有無が §5.2 と §6.4 で食い違っていた。
- **解消** — 一致させた。

## 7. 確認したが問題なしと判断した点

**指摘しなかったものも記録する。** 見落としではなく判断であることを残すため。

| # | 確認した点 | 判断 |
|---|---|---|
| 1 | 楽観ロック（`version` 条件付き UPDATE で0行なら 409） | **D1 でそのまま成立する。** 読んでから決める必要がなく、バッチの制約と相性が良い。むしろ PostgreSQL 版より単純になった |
| 2 | `period_key` の部分ユニーク2本 | SQLite も UNIQUE で NULL を互いに異なる値として扱い、部分インデックスに対応する。**設計はそのまま成立する** |
| 3 | Job の claim を条件付き UPDATE 1文で行う設計 | SQLite は書き込みを直列化するため、`FOR UPDATE SKIP LOCKED` 無しで二重取り出しを防げる。**`returning` で取れた行だけを処理すれば足りる** |
| 4 | 複合 UNIQUE と複合外部キーによる Workspace 境界 | **D1 でも同じように張れる。** RLS を外した分、この層の重みが増したため**簡素化しなかった**（Reassessment では簡素化候補に挙げていたが、D1 移行に必要な変更ではなく、承認範囲外でもあるため適用しない） |
| 5 | 業務日付を Workspace timezone の暦日とする規約 | cron が UTC で動く構成では**さらに重要になった。** 変更不要 |
| 6 | Task と ContentItem の分離、`content_item_tasks` の `unique(task_id)` | 変更不要 |
| 7 | Outbox パターン | バッチに入れれば原子性は保たれる。**保証（§23.3）の記述だけ「同一のバッチ」に直した** |
| 8 | `assets` が Drive のファイルを複製しない設計 | Drive をファイル本体の SoT とする方針と整合。変更不要 |
| 9 | 緊急度を列に保存しない判断 | 保存すると「人が直せる値」に見え、二重更新の入口になる。**保存しないほうが安全**。同意 |
| 10 | Notion のプロパティを列挙して取得する規約（`契約金額` を持ち込まない） | 財務情報を最初からシステムへ入れない方針として妥当。**API のスキーマ検証で保証する記述まで含めて適切** |

## 8. Phase 2 開始の可否

### 8.1 判定

**開始してよい。BLOCKER 0 / HIGH 0。**

### 8.2 ただし Phase 2 の最初に実測すること（未検証事項）

| # | 実測すること | 外れた場合 |
|---|---|---|
| 1 | **Access を適用した Worker で cron トリガーが発火するか** | **自動処理の設計が根本から変わる。最優先で確認する** |
| 2 | `*.workers.dev` で Access が使えるか | Q9（独自ドメイン）が Phase 2 の前提条件に繰り上がる |
| 3 | `ctx.access.getIdentity()` に IdP 側の不変IDが含まれるか | 含まれなくても設計は成立する（`users.id` で同定しているため）。含まれるなら照合を強める |

### 8.3 Phase 2 の完了までに決めておくべきオーナー判断

| # | 論点 | 必要な時期 |
|---|---|---|
| Q2 | 権限表（§9.2）の内容でよいか | **Phase 3 の Permission Matrix Test の期待値になる** |
| Q13 | 移行時にスタッフの「動画追加」「担当変更」「投稿予定日の変更」を全員可のままにするか | Q2 と同時に決めるのが自然 |

**この2件以外は Phase 2 の進行を妨げない。**

## 変更履歴

| 日付 | 版 | 内容 |
|---|---|---|
| 2026-09-03 | v1 | 初版。設計書 v6（D1 移行版）に対する第5回レビュー。BLOCKER 2 / HIGH 10 / MEDIUM 5 / LOW 2 を検出し、すべて設計書側で解消した |
